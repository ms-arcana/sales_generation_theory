# 第12.3版 ── A26 と A27

版 `c881f80`（tag v12.2-3）／`sales_logic.py` sha256[:12] `6d871696b492`／回帰 **147項目 全通過**（132 → 140 → 147）
**コードとしては入った。効果は次の走行でしか測れない**（→ `predict_v12_3.md` に予測を置いた）。

---

## A26 ── R10a の比較対象を差し替えた

### 何が壊れていたか

R10a は「④で毎年来ると言っておきながら、⑥がその問題を同じ周期で作り直すな」という規則なのに、
実装は **⑥の課金周期**と比べていた。課金周期は「問題が残るか」と直交しているので、両方向に誤る。

第12.2版の実測が両方を出した。

```
偽陰性  単発と申告した4件 → R10a_NOT_PERIODIC（info）で比較対象外。
        問題が翌年戻るかは一切検査されない
偽陽性  E2-P2・R2-P2 は本文で「翌年以降に同じ手数を積み直しません」と書いているのに、
        課金が年額というだけで停止
```

そして**生成器のほうが正しい基準で答えていた** ── 8体中6体が課金周期ではなく〈提案後に問題が残るか〉で書き、
E2-P1 は自己申告で「判定基準が規定されていないため、納品物が校内に残ることをもって満たしたとみなした」と述べた。

### 直したもの

```
s6_residual_period_months : ④で問題化した事象が、本提案の実施後も再発する周期（月）。
                            仕組み・様式・型が買い手側に残って再発しなくなるなら 0。

R10a: s4_declares_repetition ∧ s4_period_months ≠ 0
      ∧ s6_residual_period_months ≠ 0
      ∧ s6_residual_period_months ≤ s4_period_months   →  停止
```

| 場合 | 出るもの |
|---|---|
| 残存周期が未宣言 | `R10a_RESIDUAL_UNDECLARED`（**要判断**）。**課金周期へは落とさない** ── 落ちる先が誤った物差しだから（A25 の教訓） |
| 残存 0、または ④の周期より長い | `R10a_RESIDUAL_OK`（註記） |
| かつ課金は ④の周期以下で反復 | `R10a_CHARGE_PERIODIC`（註記）。**旧 R10a なら止まっていた形＝偽陽性の正体を数えられるように残した** |
| 残存 ≤ ④の周期 | `R10a_REPRODUCES_PROBLEM`（停止） |
| ④の周期が 0 | `R10a_NOT_PERIODIC`（註記）。残存周期を問わず対象外 |

**指示文は変えていない。** 第12.2版が示したのは、ずれていた二つのうち**正しかったのは指示のほう**だということ。
規則文（`messages.rules`）だけを検査と揃え、⑥の枚仕様に一行足した ──
〈本提案の実施後にその事象が再発するか。しないなら、何が買い手側に残るからそうなのかを書く。
これは課金の周期とは別の話である〉。

`s6_period_months`（課金周期）は R10a から外れて参照ゼロになったが、
上記 `R10a_CHARGE_PERIODIC` の註記で使うため欄は残した。

### 回帰（両方向を入れた）

```
偽陰性側  単発の契約でも、問題が同じ周期で戻るなら停止する      （旧 R10a は素通りさせていた）
偽陽性側  毎年課金でも、問題が残らないなら通る                （旧 R10a はここで停止していた）
          かつ、旧 R10a の偽陽性だったことが註記に残る
未宣言    課金周期で代用せず要判断へ
④が0     残存周期を問わず対象外
④が反復を問題化していない  そもそも検査しない
```

### いま測れないこと

第12.2版の走行は `s6_residual_period_months` を持たないので、新しい物差しで測ると
`R10a` の停止は 2 → **0**、代わりに `R10a_RESIDUAL_UNDECLARED` が **6件の要判断**になる。
**これは設計どおりで、A26 の効果ではない。** 効果は次の走行でしか測れない。

---

## A27 ── 必須要素（導出）と字数上限（較正）の優先順位を規定した

### 何が壊れていたか

⑥には必須要素が **10〜11個**点灯するのに、出力仕様は「各枚 200〜450字」。**1要素あたり41字。**
どちらを優先するかが書かれていないので、生成器が毎回自分で決めていた。

```
E1-P1 ⑥計11 ＝ 本文 7 ＋ 運搬 4       E2-P2 ⑥計11 ＝ 本文 8 ＋ 運搬 3
E1-P2 ⑥計11 ＝ 本文 7 ＋ 運搬 4       R1-P1 ⑥計11 ＝ 本文 7 ＋ 運搬 4
E2-P1 ⑥計10 ＝ 本文 7 ＋ 運搬 3       R1-P2 ⑥計11 ＝ 本文 7 ＋ 運搬 4
R2-P1 ⑥計10 ＝ 本文 7 ＋ 運搬 3       R2-P2 ⑥計11 ＝ 本文 8 ＋ 運搬 3
```

第12.2版の実測 ── **8体中5体**が「収まらないので要素を優先した」と自己申告し、
うち**3体**が「どちらを優先するかの規定がない」と名指しした。
そして**上限を守った2セルは、要素のほうを落としていた**（「別紙とするにとどめた」「量を落とした」）。

**さらに悪いことに、要素が本文に在るかは一度も検査していなかった。**落ちは `self_report` にしか出ない。

### 直したもの ── 優先順位は §5 の規律から導かれる

このプロジェクトは既に**導出表と較正表を分け、較正の側を譲らせる**と決めている（較正台帳）。
この衝突はまさにその形である。

| | 出所 |
|---|---|
| ⑥の必須要素の集合 | **導出**。Π と規則から `blocks_on` が出す |
| 字数上限 200〜450字 | **較正**。「そのままスライドに貼れる」という、観測されていない前提 |

したがって **要素が優先し、字数が譲る**。これは選択ではなく、既に採用済みの規律の適用である。

```
提示仕様（全アーム共通）
  ※ この字数は目安である。上の【必ず入れる要素】と衝突したら、要素を優先し、字数を超えてよい。
    逆に、字数に収めるために要素を落としてはならない。落とした場合は s6_omitted_blocks に必ず出すこと。
```

そのうえで、**落ちを隠させない欄を作った**（A23 の紙側・A24 と同じ処置。
Arm 0 の実測「欄そのものが指示として働く」8/8 に依る）。

| 申告 | 出るもの |
|---|---|
| 空の配列 | `A27_NO_OMISSION`（註記） |
| 非空 | `A27_BLOCK_OMITTED`（**停止**）。字数を破ることは違反ではないが、**要素を落とすことは違反である** |
| 未申告 | `A27_OMISSION_UNDECLARED`（要判断）。黙って通さない |
| 点灯していない要素を挙げた | `A27_OMISSION_UNMATCHED`（要判断）。申告と決定表のずれ |

**この非対称（字数は破ってよい／要素は落としてはいけない）が、規定されていなかった優先順位そのもの**である。

### あわせて決めたこと ── ⑥の字数を採点の根拠から外す

第12版のアーム比較は ⑥字数を根拠の一つに使っていた（「枠を与えると枠まで書く」）。
しかし ⑥字数は**一度も検査対象ではなく**、第12.2版の再走行では同じ指示で 450〜1506 に振れた。
**検査していないものをアームの差の根拠にしていた。** 以後、アームの差は**停止コードだけ**で読む。
字数はレポートの読み物として残すが、根拠にはしない。

---

## 採らなかった案 ── ⑥を〈本文〉と〈運搬の道具〉に分ける

⑥の必須要素13個は、性格が二つに割れる。

| 説得の本文（買い手に読ませる論証） | 運搬の道具（買い手が次の座席へ運ぶために使う） |
|---|---|
| 試せる仕掛け／段階導入と移行支援／カテゴリ内で自社が残る根拠／最終裁定点の基準で読める量／読む座席ごとの量／消す部分の対応表／誰がいつどの費目で減らすか／⑤の条件の自社適用 | 起案理由文／上申用サマリ／人間記入スロット／要件記述（層A）／下向き説明資料 |

字数上限を**本文にだけ**掛ければ、1要素あたり 41字 → **56〜64字**になる。
そして後者はもともとスライド本文ではなく、モデルが既に持っている**可搬層**（Star & Griesemer の
boundary object、「起案理由欄に貼れる3〜5行」＝ standardized form 型）に当たる。

**これは採らなかった。** 理由は二つ。

1. 優先順位の規定（上記）だけで、A27 が名指しされた問題 ──「規定がない」── は解ける
2. この分離は⑥の**構造**を変えるので、Σ が Π₁ の階段から導出されるという約束との関係を先に詰める必要がある
   （枚は増えないが、⑥の中に型が増える）

**次の走行で `A27_BLOCK_OMITTED` が複数出たら、その分離へ進む根拠になる。**
出なければ、優先順位の規定だけで足りたということ。

---

## 版と次

```
git      c881f80（tag v12.2 の3つ先）
sales_logic.py sha256[:12]  6d871696b492
回帰      147項目 全通過（132 → 140 → 147）
指示文    prompts8_v12_arm{0,1,2}.json を再生成。A26・A27 とも 8/8 で届いていることを走行前に確認
```

**次は Arm 1 の8体を再走行して測る。** 予測は `predict_v12_3.md` に置いてある。
前回と違い、**指示文に届いているかを走行前に確認済み**（第12.2版で踏んだ配管の罠の再発防止）。
