# ν 記入仕様書 ── 業界セルの入力を組むための唯一の規定

第12.8版（`sales_logic.py` sha256[:12] `28181539e212`）。
**この文書は N₆ の四つ組（担体・定義域・充足条件・強さ）に従って書かれている。**
定義域に無い値を書くと `AXIS_VALUE_UNKNOWN` で申し送られ、既定値へ黙って落ちる。**必ず定義域の中から選ぶこと。**

---

## 0. あなたの仕事

割り当てられた**1業界**について、営業資料生成モデルの入力 ν を組む。
出力は **JSON 1個**。日本語の説明文ではなく、**機械が読む構造**を返すこと。

ν は「買い手側」と「商材側」に分かれる。

```
買い手側（1業界に1つ。2商材で共有する）   座席列 J・執行座席 W・拒否権 V・文脈・上位者
商材側（商材ごとに1つ）                  τ（使える日付と量）・M（買い手が取りうる他の手段）
```

商材は2つ。**どちらも同じ買い手が相手である。**

| | 商材 | 中身 |
|---|---|---|
| `IT` | ITソリューション | 需要予測・要員配置の最適化システム（SaaS＋初期設定）。初年度 1,400万〜3,200万円。導入から効果が数字に出るまで約6か月 |
| `CONSUL` | コンサルティング | 業務改革コンサルティング（現場工数の可視化と生産性改善）。3か月〜6か月、280万〜850万円 |

---

## 1. 素材

`ind/<あなたのコード>.json` に、この業界の下調べがある。**まずこれを読むこと。**

| 欄 | 使い道 |
|---|---|
| `buyer_sketch` | 買い手の実像。座席列・決裁段数・資金の出所・判断軸がここに書いてある |
| `t_forms` | **④で使える日付**。`concrete_date` に実在の制度日付が入っている。τ はここから作る |
| `d_default` | **⑤で使える消去次元**。`primary` / `secondary` と、その業界で D1 を向けてはいけない理由 |
| `products` | IT / CONSUL について、前回の判定（妥当／条件付き／不成立）と `line_4` `line_5` の実例 |
| `model_gaps` | 前回この業界で見つかったモデルの穴 |

**ただし前回の判定に引きずられないこと。**あなたの仕事は判定ではなく、**入力を正確に組むこと**である。
前回「不成立」と書いてあっても、いま組むべき ν は組む。停止するかどうかは機械が決める。

---

## 2. 出力する JSON の全体

```json
{
  "code": "I01",
  "industry": "製造・メーカー",
  "segment": "中堅メーカー（設備投資稟議・4M変更管理）",
  "buyer": { ... §3 ... },
  "products": { "IT": { ...§4... }, "CONSUL": { ...§4... } },
  "notes": "組むときに迷った点・仕様に規定が無かった点を書く（自由記述）"
}
```

`segment` は買い手の性格を一行で。座席列の形が分かる書き方にする。

---

## 3. `buyer` ── 買い手側（2商材で共有）

```json
"buyer": {
  "context": {"業態":"…", "規模":"…", "商圏":"…",
              "拘束の所在":"…", "執行座席の同型":"…", "暦の同型":"…"},
  "upstream": ["…"],
  "downstream": [],
  "seats": [ {…}, … ],
  "veto": ["…"],
  "executors": [ {…}, … ],
  "procedural": false,
  "downward": false,
  "E_reader": "手段を知らない",
  "E_judge": "手段を知らない",
  "LT_months": 6,
  "gamma_pre": {"①": "…"}
}
```

### 3.1 `context`

`業態` `規模` `商圏` は買い手の属性。**τ の `scope.keys` はここに書いた key と value に完全一致で照合される。**
綴りが1文字でも違えば適用対象の照合が落ちる。

残る3つは**構造キー**（Π₃ の同所律）。事例が効くかどうかはこの3つの一致で決まる。所轄庁や系列は書かない。

| キー | 定義域 | 意味 |
|---|---|---|
| `拘束の所在` | `制度` / `上位者` / `買い手の資源` | ④の日付を誰が握っているか |
| `執行座席の同型` | 自由記述 | 費目を実際に減らす座席の形（例「法人事務局が人件費と業務委託費を持つ」） |
| `暦の同型` | 自由記述 | 決定が締まる暦の形（例「外部機関の審査日で締まる」「自分で決める」） |

### 3.2 `seats` ── 座席列 J

**起案する座席から順に並べ、配列の最後を最終裁定点（j\*）にすること。**

```json
{"name":"生産技術課長", "kappa":["実務性"], "chi":"現場で回るか",
 "gamma":"単独", "omega":"社内", "reads":true,
 "form":["工程能力指数","設備総合効率","4M変更"], "origin":"組織"}
```

| 欄 | 定義域 | 規定 |
|---|---|---|
| `name` | 自由 | 実在する役職名。個人名は書かない |
| `kappa` | **`実務性` `説明可能性` `財源` `価格` `政治的可視性` のみ** | その座席が何で判断するか。集合（複数可） |
| `chi` | 自由 | その座席の問い（一行） |
| `gamma` | `単独` / `合議` | 合議なら最も保守的な一人が止める |
| `omega` | `社内` / `社外` | |
| `reads` | `true` / `false` | この資料を自分で読むか。読まなくても最終裁定点なら基準は要求される |
| `form` | 自由（語の配列） | **その座席の文書様式に既にある語。**この語で書かれていないものは議題にならない |
| `origin` | `制度` / `組織` / `個人` | 誰がこの座席を置いたか |

**必ず守る2つの規定（破ると `R18_*` で停止する）**

- `origin` が `制度` の座席は **`kappa` をちょうど1つ**にする。制度が置いた座席は所掌が定義されているので複数の基準を持てない。
- `origin` が `制度` で `reads` が `true` の座席は **`form` を空にしない。**

**κ の到達可能性。**⑥に置く量は、最終裁定点の κ で読める必要がある。表は次のとおり（左が置いた基準、右が届く先）。

```
実務性       → 実務性
価格         → 価格 / 財源
財源         → 価格 / 説明可能性 / 財源
説明可能性   → 価格 / 政治的可視性 / 説明可能性 / 財源
政治的可視性 → 政治的可視性 / 説明可能性
```

**座席数は実像どおりにする。**2〜4が普通。多ければ多いほど資料は通りにくくなるが、それは測定対象であって調整するものではない。

### 3.3 `veto` / `upstream` ── 止める側と、上に立つ側

`veto` は名前だけの配列。労働組合・監査・法務・情報セキュリティ委員会など。**無ければ空配列。**

`upstream` は**買い手の上位にいる当事者**。所管官庁・親会社・元請・納入先OEM・卸・本部・認証機関など。

> **ここが一番よく落ちる。**τ の `binders` と、手段 M の `D6a` / `D6c` の拘束者は、
> **`upstream` に同じ文字列で載っていなければ停止する**（`A7_BINDER_ABSENT` / `A8_BINDER_NOT_ABOVE`）。
> 「納入先OEMの購買部門」と書いたら、`upstream` にも一字一句同じ「納入先OEMの購買部門」を入れること。

### 3.4 `executors` ── W：決めないが費目を実際に動かす座席

```json
{"name":"工場長", "accounts":["外注加工費","派遣費"], "willing":null, "kappa":["実務性"]}
```

`accounts` は**実在の費目名**。⑥はこの費目を名指しで減らす行為を書かされる。
`willing` は `null`（未聞取り）/ `true` / `false`。`false` は「権限はあるが実行を拒む」。
**`executors` が空だと κ_n が動かない。**必ず1つ以上置く。

### 3.5 その他

| 欄 | 定義域 | 規定 |
|---|---|---|
| `procedural` | `true`/`false` | 手続が決定を支配する買い手か（官公庁・規制業種） |
| `downward` | `true`/`false` | 承認が上から下へ降りる形か |
| `E_reader` / `E_judge` | **`困っていない` `手段を知らない` `比較検討中` `うちも知っている` のみ** | 読み手／裁定者が、この手段をどこまで知っているか |
| `LT_months` | 整数 | 着手から効果が立ち上がるまでの月数（買い手側の事情） |
| `gamma_pre` | `{"①":"…"}` 等 | **この資料より前に、すでに双方で確認済みの段。**訪問ヒアリングで確認済みなら `①` を入れる。無ければ `{}` |

---

## 4. `products.IT` / `products.CONSUL` ── 商材側

```json
{
  "tau": [ {…}, … ],
  "M":   [ {…}, … ]
}
```

**商材座標（ν θ σp ω α β）は与えられているので書かない。**あなたが書くのは τ と M だけ。

### 4.1 `tau` ── ④で使える日付と量

`ind/<コード>.json` の `t_forms` の `concrete_date` を**そのまま使う**。作り話の日付を置かない。

```json
{"form":"A", "d":"2027-12-31", "src":"契約", "known":"既知",
 "q":"標準保守終了までに移行判断が済んでいない基幹モジュールの数",
 "q_kappa":"実務性", "q_recast":true,
 "q_source":"買い手データ", "q_low":8, "q_high":14,
 "scope":{"keys":[["業態","中堅メーカー"]], "applied_from":"2025-04-01",
          "source":"2026-07-10 SAPジャパン公表の保守方針から取得"},
 "binders":["SAP"], "q_receipt":"2026-07-10 情報システム課より受領。稼働モジュール一覧",
 "decision":true, "windows":1}
```

| 欄 | 定義域 | 規定 |
|---|---|---|
| `form` | `A` `B` `C` `D` `Ea` `Eb` `Ec` `Ed` | 時間の形式。`t_forms` の `T-A 閉扉` なら `A` |
| `d` | ISO 日付 | **実在の日付。**`t_forms.concrete_date` から取る |
| `src` | **`法令` `公的暦` `自然・需要` `契約` `売り手都合` のみ** | その日付を供給しているもの |
| `known` | `未知` / `既知` | 買い手がその日付を知っているか |
| `q` | 自由 | **その日付までに確定していない量**。「〜までに〜していない〜の数」の形で書く |
| `q_kappa` | κ の5つから1つ | その量がどの基準で読めるか |
| `q_recast` | `true`/`false` | 最終裁定点の基準への両替経路を同じ枚に書けるか |
| `q_source` | **`買い手データ` `公開統計` `売り手データ` のみ** | |
| `q_low` / `q_high` | 数値 | 量の範囲 |
| `scope` | 下記 | **省略不可。**空なら τ ごと棄却される |
| `binders` | 名前の配列 | `src` が `契約` のとき**必須**。その日付を握っている当事者 |
| `q_receipt` | 文字列 | `q_source` が `買い手データ` のとき**必須**。「YYYY-MM-DD 誰から受領。何を」 |
| `decision` | `true`/`false` | **その日が決定の締まる日か**（結果が現れる日ではないか） |
| `windows` | 整数 | 同じ拘束者が持つ並行する窓の数。年1回なら 1 |

`scope` は τ の適用対象。`keys` は `[["属性名","値"], …]` で、**`buyer.context` の同名キーと完全一致**する必要がある。
`source` は「誰がいつどの条文・公表資料から取ったか」。

> **`applied_from` は原則 `null` にすること。**
> これは「その区分へ**これから**適用が始まる日」の欄である。**過去の日付を書くと τ ごと棄却される**
> （`A8_ALREADY_APPLIED`）―― 既に効いている制度は「閉扉」を作らないから。
> 制度の公布日・施行日をここに書かないこと。それは `source` に文章で書く。

**τ は 1〜3 個。**`decision:true` のものを最低1つ入れること。日付は今日（2026-08-09）より後にする。

### 4.2 `M` ── 買い手が取りうる他の手段

⑤はこの中から2つを選んで消去する。**`M0`（今のまま／来期送り）は必ず入れる。**

```json
{"name":"生産技術部と情報システム課で内製する", "mtype":"内製",
 "dims":["D5"], "order":["D5"], "binders":[["D5","工場長"]], "cost_to_buyer":null}
```

| 欄 | 規定 |
|---|---|
| `name` | その業界で実際に挙がる代替案 |
| `mtype` | **`M0` `内製` `既存外注` `競合` `取引上位者の指定` のみ** |
| `dims` | 消去次元。**`mtype` ごとに使える次元が決まっている**（下表）。`M0` は空 |
| `order` | `dims` の適用順（配列） |
| `binders` | `[["次元","拘束者"], …]`。`D6a` と `D6c` を使うなら**必須** |
| `cost_to_buyer` | 買い手にとっての費用。**0 なら D6 系でしか消せない**。不明なら `null` |

```
内製              D2 / D5 / D6b
既存外注          D2 / D4 / D6b / D6c
競合              D1 / D2 / D3 / D4 / D6a / D6b / D6c / D7a / D7b / D7c / D7d
取引上位者の指定   D6c
```

次元の意味（`order` の先頭＝主位置は、`内製`→`D5`、`既存外注`→`D2` か `D6c`、
`競合`→`D7a`〜`D7d`、`取引上位者の指定`→`D6c` にすること）。

| | 意味 | 拘束者 |
|---|---|---|
| `D1` 能力 | その打ち手ではやり切れない | ― |
| `D2` 帰責 | 失敗したとき誰が責任を負うかの切れ目 | ― |
| `D3` 権限 | それを選ぶ権限がその座席に無い | ― |
| `D4` 手続 | 買い手が通すべき手続を通せない | ― |
| `D5` 資源配分 | 採ると別の仕事から人手・枠・金が移る | **必須** |
| `D6a` 制度 | 制度が許していない | **必須** |
| `D6b` 市場 | 市場に供給がない | ― |
| `D6c` 上位者 | 上位者の承認がなければ動かせない | **必須** |
| `D7a`〜`D7d` | 事例／登録／責任分界／価格開示 | ― |

**`D5` の拘束者は特別**。`binders:[["D5","座席名"]]` が必須で、その座席は
**`executors` に居る名前**でなければならず、**最終裁定点そのものであってはならない**
（枠を決める権限が読み手自身にあるなら、それは拘束ではなく選択だから）。

**`D6a` / `D6c` の拘束者は `upstream` に載っている名前**にすること（§3.3）。

### 4.3 売り手が実際に持っている物 ── 使えない次元がある

次元は**売り手が実際に持っている物**に裏づけられていないと使えない。持たずに使うと停止する。

| | ITソリューション | コンサルティング |
|---|---|---|
| 登録・認証 | ISMS・個人情報保護（有効期限 2028-03-31） | **無し** |
| 責任範囲の契約明記 | できる | **しない** |
| 価格構成の開示 | しない | できる |
| 上位者の承認実績 | 4件・実測 30〜75日 | 3件・**実測日数を記録していない** |
| 接触可能母数・歩留まり | **持たない** | **持たない** |
| 実名事例 | 3件（拘束の所在＝制度／上位者／買い手の資源） | 2件（上位者／買い手の資源） |

ここから**機械的に決まる**こと。

| 次元 | IT | コンサル |
|---|---|---|
| `D6a` 制度 | 使える | **使えない**（登録が無い） |
| `D6b` 市場 | **使えない** | **使えない**（どちらも母数・歩留まりを持たない） |
| `D6c` 上位者 | 使える | **使えない**（実測日数が無い＝`R7_D6c_HALF`） |
| `D4` 手続 | 使える（登録が前例になる） | **事例の構造キーが一致する業界でのみ**（`R4_NO_PRECEDENT`） |
| `D7b` `D7c` | 使える | 使えない |
| `D7d` | 使えない | 使える |

**コンサルの M で `D4` を使うときは、注意して使うこと。**
売り手の事例2件の構造キーは次のとおりで、担当業界の `context` と3つとも一致しなければ前例にならない。

```
□□食品株式会社   拘束の所在=上位者        執行座席の同型=本部が販促費と人時を分けて持つ   暦の同型=卸との定番改訂商談で締まる
◇◇工業株式会社   拘束の所在=買い手の資源   執行座席の同型=経営者が人件費を単独で動かす     暦の同型=自分で決める
```

一致しないなら `D4` を外し、`D2`（帰責）・`D3`（権限）・`D5`（資源配分）・`D1`（能力・競合にのみ）で組む。
**外したことを `notes` に書くこと。**「この業界でコンサルが使える次元は何か」がこの実験の測定対象である。

**`d_default` を読むこと。**その業界で D1（能力の否定）を向けてはいけない理由が書いてある。
向けると侮辱になり階段が止まる。`primary` に挙がっている次元を主に使う。

**M は 3〜5 個**（`M0` を含む）。IT と CONSUL で M は違ってよい（同じでもよい）。

---

## 4.4 書いたら必ず機械にかける

`nu/<コード>.json` を書いたら、リポジトリ直下で次を回すこと。

```
python3 check_nu.py <コード>
```

IT とコンサルの2件について、Σ・座席数・τ の生存数・停止の一覧が出る。
停止には **【入力の不備】** か **【モデルの言い分】** の札が付く。

- **【入力の不備】** ―― 直す。直したらもう一度回す。
- **【モデルの言い分】** ―― **直さない。**`notes` にそのまま書く。

**「生成 ○」になるまで直すのが目的ではない。**入力の不備がゼロになるまで直すのが目的である。
モデルの言い分で止まるなら、止まったまま出す。

---

## 5. 守ること

- **実在するものだけ書く。**日付・費目・制度名・座席名は、`ind/<コード>.json` の下調べか、あなたが実際に知っている実務から取る。作り話をしない。分からないものは `notes` に書く。
- **定義域の外の値を書かない。**上の表で「〜のみ」と書いてある欄は、必ずその中から選ぶ。
- **調整しない。**「これだと停止しそうだから座席を減らす」ということをしない。停止するなら停止するのが正しい入力である。
- **規定が無くて困ったら `notes` に書く。**それがこの実験のいちばん重要な出力である。仕様の穴は、走行ではなく記入の段階で先に出る。

出力は JSON のみ。前置きも後書きも要らない。
