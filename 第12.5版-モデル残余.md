# 第12.5版 ── モデル層の残余4件と、配管の刻み漏れ

版 `b83e222`（tag v12.5）／`sales_logic.py` sha256[:12] `f46e6fb20d8f`／回帰 **173項目 全通過**（158 → 173）
**8セルの決定表は不変**（Σ・ブロック・規則とも as-run からの差は第12.1版の ν 修正と R10a 規則のみ）。

下流を盲検する前に、モデル層に開いていた4件を閉じた。
**4件のうち2件は、直し方が分からなかったのではなく、問いのほうが間違っていた。**

---

## 1. `C_move` と ⟨θ, σp⟩ ── 合成規則は要らなかった

### 何が開いていたか

第10版が `C_move`（乗り換えの重さ）を θ（分割試用可能性）と σp（手続切替コスト）に割ったが、
**旧軸を消さずに残し、合成規則も書かなかった**。`ν` と同じ二重の真実である。
「SPEC が合成規則を与えていないので機械的に直せない」として開いたままだった。

### 見たこと

`C_move` の参照を全数走査した。**駆動しているブロックは2つだけ**である。

```
B_trial      ← A == "使えば分かる" ∨ C_move == "すぐ試せる"
B_migration  ← C_move == "大仕事"
```

そして**その2つが、ちょうど半分ずつに対応している。**

| ブロック | 何を決めているか | どちらの半分 |
|---|---|---|
| `B_trial` 試せる仕掛け | 試させないと分からないか／そもそも試せるか | ν ∨ **θ** |
| `B_migration` 段階導入と移行支援 | 乗り換えの手続コスト／段階を踏めるか | **σp** ∨ θ |

**合成された値を使う場所が無いのだから、合成規則も要らない。**
各ブロックを、それを実際に決めている座標へ繋ぎ直せばよい。

```python
def trial_of(n):      return nu_of(n) == "使えば分かる" or n.prod.theta in ("完全分割", "段階分割")
def migration_of(n):  return n.prod.sigma_p == "高" or n.prod.theta == "不可分"
```

**8セルすべてで現行の点灯を再現する**（`C_move` の値を壊しても点灯が動かないことを回帰に入れた）。
食い違ったときは `C_MOVE_AXIS_CONFLICT` を申し送る（`NU_AXIS_CONFLICT` と同じ形）。
8セルでは一度も出ない ── だから点灯が変わらない。

> **この形は3例目である。**「二つの真実があって照合が無い」は ν（G4）・C_move で2回、
> そのどちらも**古い側が黙って引かれていた**。座標を足すときに旧軸を消すのを忘れる、
> という同じ手続きの落ちである。

---

## 2. cost 単調性（Π₁ 第2式）── 探していた対象が存在しなかった

### 何が開いていたか

第8版 §6：「cost 単調性は依然として検査対象を持たない。**Π でも解消しない唯一の残件である。**」
第10版 §12.3：「`eliminable(Mᵢ) ⟺ cost(Mᵢ)>0 ∨ σp+τ ≥ 2`。**これで初めて検査対象が与わる。閉じた。**」
第12.4版の残タスク表：「第10版で対象を得たが、⑥の検査には入っていない／開いている」

三つとも的を外していた。

### 第2式は第1式の系である

$$\Gamma_s = \Gamma_s^{\mathrm{init}} \cup \{\phi_s\}, \qquad
\max_{\phi\in\Gamma_s}\mathrm{cost}(\phi) \le \max_{\phi\in\Gamma_{s'}}\mathrm{cost}(\phi)\quad(s<s')$$

Γ が単調に積むなら Γ_s ⊆ Γ_s′ であり、**上限は自動的に非減少になる**。
書かれたままの第2式に独立の内容は無い。
**「検査対象を持たない」のは欠落ではなく、そう書いたことの帰結だった。**
第8版は存在しない対象を探し、第12.4版までそれを残件として引き継いでいた。

### そして第10版が与えたのは、別の cost である

| | 何の値段か | 実装 |
|---|---|---|
| Π₁ の `cost(φ)` | **承認の値段。**買い手がその命題を認めるのにいくら払うか | ―― |
| A19・第10版の `cost(Mᵢ)` | **対抗手段が買い手に掛ける費用。**タダの手段は帰責では消せない | `check_cost` |

**同じ名前の二つの関数を、二版にわたって二度取り違えていた**（第10版 §10.4 と §12.3）。
A19 が対象を与えたのは後者であって、第2式ではない。

### 実質はどこにあるか ── Σ が接尾辞であること

段の順序は **cost の順序そのもの**である（論証は無料で差し出せるものから始まり、有料のもので終わる）。
Σ から落ちる段は「**資料の外で既に承認済み**」（Γ^pre）という断定である。
承認は cost の順に積むのだから、

> **高い段を承認済みにして、安い段を残すことはできない。**

```
Σ は ⟨①②③④⑤⑥⟩ の接尾辞である
```

**これは検査できる。** 実装は `is_suffix` / `check_staircase`、対象は Σ の形。

### そして、唯一これに引っかかるのが `sigma_prod` だった

```
sigma_prod（低額 × 年次以下）  →  {①, ④, ⑥}
```

②③を残したまま⑤を承認済みにしている。**接尾辞でない。**
SPEC §6 X1 は `sigma_prod` を「根拠のない経験的ヒューリスティック」と自認していたが、
**根拠が無いだけでなく、Π₁ と衝突していた。**

〈導出 > 較正〉の規律（A27 で採ったのと同じもの）により、**原理が勝ち、較正表が譲る**。
この縮退は採らず、`PI1_SIGMA_NOT_SUFFIX` を申し送って `sigma_read` に戻す。

---

## 3. `S1`/`S2`/`S3` ── 削除されていなかった

SPEC §12.2 は「S1・S2・S3 は削除した。σ_prod にしか繋がらず、その σ_prod は根拠なしと自認済み」と書いた。
**削除されていない。** `sigma_prod` が現に引いている。
参照が一箇所しかなく、**8セルでは一度も発火しない**ので、消えたように見えていただけである
（25業界へ広げれば発火する）。

そして削除は正しい処置でもなかった。**根拠が無い表は、捨てるのではなく較正台帳へ載せる** ──
それがこのプロジェクトの規律である。よって：

- `sigma_prod` を**較正表として台帳に登録**（`EXPR_OK` / `START` / `ACQUIRE` / `ISO_KEYS` / `BINDER_SRC` の隣）
- **未較正の業界では縮退させない**（`uncal` を通す）
- §2 のとおり `{①,④,⑥}` は較正済みの業界でも採らない

結果として `sigma_prod` が実際に Σ を縮退させる場合は残っていない
（`{①,⑥}` は低関与＝生成しないの標識であって縮退ではない）。
**削除と等価な状態に落ち着いたが、「参照が無いから消えた」ではなく「原理に反するから採らない」という理由で**そうなっている。

---

## 4. `START` ── 較正台帳が Σ に掛かっていなかった

較正台帳の規律は「未較正の業界では、較正定数に依存する判定を停止から降格へ落とす」。
しかし `apply_calibration` が触るのは **findings だけ**で、**Σ の縮退は素通りしていた。**

```
未較正の業界（例：化学）
  旧  Σ = ②③④⑤⑥（START で縮んだまま）＋ SIGMA_UNCALIBRATED の申し送りが出るだけ
  新  Σ = ①②③④⑤⑥（縮退させない）
```

Σ の縮退は「**その段は資料の外で既に成立している**」という断定である。
較正していない業界でそれを断定する根拠は無い。

**安全な向きに倒れる。** 段が増えるだけで、落ちた段の Γ^pre 不足による停止（A10 / `R8_PRE_MISSING`）も出なくなる。
対面で飛ばしてよい段（`talk_guide`、これも `START` を引く）も出さない。

> 較正台帳を作ったとき、**降格の対象を findings だけだと思っていた。**
> 較正表が効くのはそこだけではない ── **表が値を決めている場所すべて**である。
> `START` は判定ではなく**構成**（何枚書くか）を決めていたので、台帳の網から落ちていた。

---

## 5. 配管 ── 刻むのを呼び出し側の作法に任せていた

第12版で踏んだ罠（決定表が古いまま指示文へ入り、R10a が 0/8 で欠落）に対して、
第12.1版は `stamp.py` を書いた。しかし塞がったのは半分だった。

```
regen_v12.py 経由なら刻む
cells8_v10.run("decisions8_v10.json") を直接叩くと刻まない  ← 同じ罠がもう一度踏める
```

**刻むのを呼び出し側の作法に任せると、作法を知らない経路が必ず一つ残る。**

| | 直した形 |
|---|---|
| 書く | `cells8_v10.run()` の**中で**刻む（`stamp.dump_stamped`）。書き出し口を一本にした |
| 読む | `stamp.load()` が刻みの有無を吸収する。全読み手（`prompts8_v10/v11`・`report8_v10`・`validate8_v2/v11/v12`）を通した |
| 照合 | **`assert_fresh()`** ── 刻んでも**照合しなければ気づけない**。指示文を組む前に、決定表の刻みと現在の `sales_logic.py` のハッシュを突き合わせて、食い違えば止める |

`stamp()` は二度掛けても入れ子にならない（`unwrap` を通す）。
`decisions8_v10.json` は as-run なので刻み無しのまま残し、読むときに警告だけ出す（止めない）。

**三つ目が本体である。**罠は「刻まれていない」ことではなく「**古い表が指示文へ入ったのに誰も気づかない**」ことだった。

---

## 6. 版と、次

```
git      b83e222（tag v12.5）
sales_logic.py sha256[:12]  f46e6fb20d8f   （1226 → 1558 → 1668 行）
回帰      173項目 全通過（158 → 173）
決定表    decisions8_v12.json を再生成 ―― 8セルの Σ・ブロック・規則に変化なし
指示文    prompts8_v12_arm{0,1,2}.json を再生成。A26 8/8 ・A27 8/8 ・A28 8/8 で届いていることを確認
復元往復  コード束5本を作り直し、空ディレクトリへ復元して 173項目 全通過を確認
```

**モデル層は閉じた。** 次は Arm 1 の8体を走らせて A26・A27・A28 を測る
（予測は `predict_v12_3.md` と 第12.4版の設計書に先置き済み。約37分・50万トークン）。

### 訂正した過去の記述

| 文書 | 書いてあったこと | 訂正 |
|---|---|---|
| SPEC8 §6 | cost 単調性は「Π でも解消しない唯一の残件」 | 存在しない対象を探していた。第2式は第1式の系 |
| SPEC8 §10.4 | A19 で「残件がここで対象を得た」 | 別の cost（対抗手段の費用） |
| SPEC8 §12.3 | σp+τ で「閉じた」 | 同上。二版にわたって二度取り違えた |
| SPEC8 §12.2 | S1・S2・S3 は「削除した」 | 削除されていない。較正表として台帳へ載せた |
