# 第14.3版 ―― A55「⊥ の扱いが二つに割れていた」

版：タグ **`v14.3`**／`sales_logic.py` sha256[:12] **`4ce41223bae8`**（合流後）
回帰 **344＋13＋20 = 377項目 全通過**（合流後）／`audit_requirements` **6**／`audit_symbols` **0**
`audit_model.py` **(2)(3)(4)(5)(7) すべて「なし」** ―― **(2) の債務は返済済み（208 = 208）**

VS Code の第14.1版 §10・§12 が挙げた二つ（#2・#3）に対応した版です。
**二つは別々の指摘ですが、同じ型です。**まとめて **A55** として台帳に採番しました。

---

## 1. 何が起きていたか

### (a) ⊥ を、別の欄で代用していた

```python
dcd = dec.s6_decide_date or dec.s6_start_date   # 旧走行は欄が一つしかない
```

決定日が ⊥ のとき、**着手日を決定日の代理**にしていました。
`R12b`（④の期限を越えたか）は決定日についての述語なので、
着手日を入れると「まだ決めてもいない紙」を「期限を過ぎた紙」として**停止**させます。

VS Code の実測 ―― 25業界の採点で `R12b` 停止 **9件のうち3件がこの形**。
**うち2件は、買い手が「進める」と答えた紙**でした。**誤停止です。**

### (b) ⊥ を決める述語が、二つあった

`check_decidable` の4箇所が `in UNIT_UNKNOWN` を**生で**引いていました。
この表は語の一覧（`""`・`"未定"`・`"不明"`…）にすぎず、
**営業が埋める記入欄 `【　　　】` を含みません。**`is_bottom` は含みます。

| 申告 | 旧 | 新 |
|---|---|---|
| 単位欄の片方が記入欄 | `R20_UNIT_MISMATCH`（**誤停止**） | `R20_UNIT_UNDECLARED`（申し送り） |
| 単位欄の両方が記入欄 | **何も出ない** | `R20_UNIT_UNDECLARED` |
| 分母が記入欄 | **何も出ない** | `R20_DENOMINATOR_MISSING` |
| 旧出所欄が記入欄 | `A49_SOURCE_MERGED`（誤診） | `R20_SOURCE_UNDECLARED` |
| 割った出所欄が記入欄 | `A28_SOURCE_UNKNOWN`（誤診） | `R20_SOURCE_UNDECLARED` |

**これは A44 の実装で一度直した型です。**
第13.6版に「`R20` の `is_bottom` が記入欄を値と読んだ（停止 0→21件）」を直したとき、
**値の欄だけ直して、単位・分母・出所の欄を直していませんでした。**

---

## 2. VS Code の報告に無かった2箇所目

`grep` で当てたところ、**同じ代入が `check_gates`（A41）にもありました。**

```
sales_logic.py:1079    dcd = dec.s6_decide_date or dec.s6_start_date        ← check_gates（未報告）
sales_logic.py:2345    dcd = dec.s6_decide_date or dec.s6_start_date        ← check_dates_v7（報告あり）
```

窓（買い手の内側の締め）を越えたかどうかも、**決定日についての述語**です。
着手日はその代理になりません ―― 着手日は `A41B_START_BEFORE_GATE` で**別に**縛られています。
一つの欄を二つの述語に使えば、片方は必ず誤ります。

両方に同じ処置を当てました。受け皿はそれぞれ
`R12b_START_UNDECLARED`（既存）と `A41_GATE_UNCHECKED`（既存）で、**黙って通ることはありません。**

---

## 3. 直した6箇所

```python
# check_gates（1079）
d0 = None if is_bottom(dec.s6_decide_date) else iso_date(dec.s6_decide_date)

# check_dates_v7（2345）
dcd = None if is_bottom(dec.s6_decide_date) else dec.s6_decide_date
if is_bottom(dec.s6_decide_date) and not is_bottom(dec.s6_start_date):
    j.append(Judgment("A37_DECIDE_UNDECLARED", str(dec.s6_start_date)))

# check_decidable（4箇所）
if is_bottom(pu) or is_bottom(ru):   ...
if is_bottom(den):                   ...
if is_bottom(legacy):                ...
if is_bottom(v):                     ...
```

`is None` を `is_bottom` に替えたので、**記入欄で書かれた決定日も ⊥ として扱います**
（`is None` では拾えませんでした ―― これも同じ型です）。

`UNIT_UNKNOWN` の定義に註を置きました。**この表を直接引かないこと。**

---

## 4. 検分 ―― 手元では一件も出ない

**手元の10件の生成物で、旧版と新版の採点が完全に一致しました。**

```
旧 停止合計 66 ／ 新 66
旧 申し送り 76 ／ 新 76
差分: なし
```

理由は入力側にあります。

```
18件の生成物すべてが決定日を申告している    （18/18）
量の行 23 のうち、単位・分母・出所が記入欄のものは 0 行
```

**第13.6版以降の生成スキーマが `s6_decide_date` を required にしているからです。**
つまり **この欠陥は、私の8セル走行では原理的に見えませんでした。**
見えたのは 25業界側の紙（第11版までの形を含む31件のうち14件が同じ形）だけです。

> **n=1 の走行を疑うべき理由が、もう一つ増えました。**
> 「出ないこと」は「無いこと」ではなく、**入力側の定義域が閉じているだけ**のことがあります。
> `audit_model.py` (6) の未発火170コードの仕分けで (a) と (b) を分けるのが効くのは、これが理由です。

---

## 5. 回帰

**`test_v14_3.py`**（20項目・新規）。VS Code 側が `test_sales_logic.py` を触っているので別ファイルです。

```
A55-a  決定日が ⊥ → 停止しない／R12b_START_UNDECLARED・A37_DECIDE_UNDECLARED が出る
       決定日を書けば従来どおり停止（検査を弱めていないことの対照）
       決定日が記入欄でも ⊥（is None では拾えない）
A55-b  check_gates も同様／A41B_START_BEFORE_GATE は生きている
A55-c  単位・分母・出所の5通り＋対照2件（埋まっていれば何も出ない／連結は従来どおり A49）
A55-d  check_decidable の生 `in UNIT_UNKNOWN` は 0箇所
       モジュール全体でも `is_bottom` の中の1箇所だけ
```

**`test_sales_logic.py` の3項目が落ちました。**その3つが**欠陥そのものを固定していた**からです。

```
R12b ⑥の着手日が④の着手期限より後なら停止      ← 着手日を決定日の代理にする前提
日付 読めない書式は比較せず要判断へ              ← 同上
日付 読める書式なら従来どおり停止                ← 同上
```

決定日を書く形に直し、**代用しないことを見る項目を1件足しました**（計 315）。

---

## 6. A54 の13件目（未然）―― 註釈が自分の回帰を落とした

A55-d の「生の `in UNIT_UNKNOWN` は0箇所」を書いたら、**自分で落ちました。**
直した箇所に「ここは `pu in UNIT_UNKNOWN` と書いてあった」という**註釈**を残していたためです。

**物差しは境界を取らなければならない。**註釈行を除いて数えるよう直しました。
型7（浅い一致）の13件目で、**5件目の「監査・回帰の道具そのもの」**です。

---

## 7. 畳み方の穴 ―― 第14版は型5を閉じきっていなかった

第14版で `N₆` 追補と `audit_symbols()` を入れ、「9件目が書けなくなった」と書きました。
**A55 は書けました。**閉じたのは**記号の名前**だけだったからです。

```
名前   s6_decide_date / LT_months / ν …    GLOSSARY が見張る        閉じた
局所   関数の中の dcd                       見張っていない            A55(a)
述語   is_bottom ／ in UNIT_UNKNOWN          見張っていない            A55(b)
```

> **担体の一意性は、記号の名前だけでなく〈述語〉にも要る。**
> 「⊥ かどうか」を決める述語が二つあれば、⊥ という担体が二つあるのと同じである。

同型の検分は済ませました ―― `is_expr`（呼び出し1・生引き0）、`SOURCE_KINDS`（同一ブロックで同じ向き）、
`iso_date`・`parse_amount`（述語ではなく変換）。**いま二重化しているのは `is_bottom` だけでした。**

---

## 8. VS Code へ返すこと

1. **あなたの回帰を更新してください。**
   `check_decidable` の生 `UNIT_UNKNOWN` を**4箇所で固定**した回帰は、**0箇所**へ。
   数えるときは**註釈行を除いて**ください（§6）。
2. **`check_gates`（1079行）にも同じ代入がありました。**§10 の対象は2箇所です。
3. `test_sales_logic.py` の3項目を書き換えています（§5）。同ファイルを触っているので、
   突き合わせのとき**この3つは私の側を取ってください**（欠陥を固定していた項目です）。
4. **`R12b` の誤停止の確定（あなたの #4「21件を v14 のコードで採点し直す」）は、
   この版で走らせてください。**手元では差が出ません（§4）。
   停止 9→6・うち買い手が進めた2件が消えるはずです。**外れたら、まず採点器を疑ってください。**

---

# 追補 ―― 合流後（第14.1版と第14.3版の突き合わせ）

VS Code の2 commit（`ba23bac` / `9941cff`）と合流したところ、**3項目が落ちました。**
**三つとも「欠陥そのものを固定していた回帰」で、その欠陥がこちらで直っていたためです。**

| 落ちた回帰 | 何を固定していたか | いま |
|---|---|---|
| `A54 表示文が無い理由コードは A19_ORG_COST_POSITIVE の1件だけ` | 表示文の欠落1件 | 第14.2版で表示文を追加 → **0件** |
| `A54 コードに無い表示文は R10b_UNIT_REVERTED の1件だけ` | 退役し忘れ1件 | 第14.2版で退役 → **0件** |
| `A53 生成スキーマに欠けている申告欄を名指しできる（A46・A47）` | スキーマの欠落2件 | 第14.2版で4欄を追加 → **0件** |

**私が VS Code に「4箇所 → 0箇所へ更新してください」と頼んだのと、同じ操作の裏返しです。**
債務を固定する回帰は、返済したら**「返済済み」を固定する形へ書き換える**必要があります。

## 返済の確定 ―― `audit_model.py` (2) が閉じた

```
理由コード 208 ／ 表示文 208 ／ 差 0・0
```

**(2) は第14版の指示書で「残・最大」と書いた二つのうちの一つでした。**閉じました。
残るのは (6) の未発火164（VS Code が `第14.1版-未発火の仕分け.md` で仕分け済み）だけです。

## §17 #4 も片づけました ―― `s6_kappa_by_seat` / `s6_quantity_sources` を `REQ_RETIRED` へ

答えは **(a) 吸収済み**でした。**欄は `Declared` に残します**（旧走行を読むのに現に使う）。
外したのは「生成器に要求する」という位置づけだけです。

残していたために、二つの監査が**吸収済みの欄へ誤報し続けていました**。

```
audit_requirements()          N6_VALUE_SCALAR ×2      →  8件 → 6件（残る6件は全部 FIELD_SCALAR＝本物）
reqs_not_in_gen_schema()      「スキーマに無い」×2     →  0件
```

**要求から外した欄は、スキーマに無くても配管の欠落ではない** ―― この一行が抜けていました。

## 回帰の側の対照を1本足しました

`A53` の検査が「空を返す」ようになったので、**空が〈直った〉のか〈壊れて何も出ない〉のか**を
区別する対照を置きました。一つ前の版のスキーマ（`wf_gen136.js`）に当てると 7件を名指しします。

> **これは §5 の「出ないこと ≠ 無いこと」と同じ問題です。**
> 検査が 0 を返したら、**0 を返さない入力を1件作って見せる**まで、閉じたと言ってはいけません。

## 合流後の期待値

```
python3 test_sales_logic.py        344項目すべて ok
python3 test_v14_2.py               13項目すべて ok
python3 test_v14_3.py               20項目すべて ok
audit_requirements / audit_symbols   6 0        ← 8 0 から変わりました
python3 audit_model.py              (2)(3)(4)(5)(7) すべて「なし」／(6) 44 発火・164 未発火
python3 feasible136.py              8セルとも解が在る
```

---

# 追補2 ―― 第14.4版：A57「退役が読み手を置き去りにする」

**見つけたのは VS Code。第14.3版で私が入れた欠陥です。**

## 何が起きていたか

`s6_kappa_by_seat` を `REQ_RETIRED` へ移すとき、**欄の意味が `s6_quantities` に吸収済みか**だけを見て、
**コードの読み手を数えませんでした。**読み手は3つあり、移っていたのは2つでした。

```
check_chain            quantities_by_seat(dec)   橋を通る
check_quantity_sources quantities_by_seat(dec)   橋を通る
check_seat_words       dec.s6_kappa_by_seat      生読み ★
```

`wf_gen137.js` は第13.7版でその欄を落としています（スキーマが安全分類器を通らず、
「吸収済み」の欄を削って縮めた）。したがって **`A23_SEAT_WORD_ABSENT` は
新しい形の申告で構造的に出なくなっていました** ―― 同じ紙で 旧スキーマ 1件／新スキーマ 0件。

**第14.2版に直した A49（A28 の橋が旧 `source` しか読んでいなかった）と同型です。**隣の関数の取りこぼし。

## 私の誤り ―― 正しい問いに、間違った根拠で答えた

VS Code は第14.1版でこう問うていました。

> `s6_quantity_sources` は 36件中 24件で届いていましたが、`wf_gen137.js` で欄を落としています。
> **吸収が本当なら (a)、吸収が漏れているなら (b) です。**判断を Cowork へ返します。

**問いは正確でした。**私が「(a) 吸収済み」と答えたのは**設計の側からで、コードを数えていません。**

> **「吸収済みか」は、欄の意味への問いではなく、読み手への問いである。**

さらに悪いことに、`reqs_not_in_gen_schema()` は `s6_kappa_by_seat` を名指ししていました。
**私はそれを「吸収済みの欄への誤報」と読んで黙らせました。**本物の警報でした。

## 二つの計器のどちらにも映らない

- `audit_model.py` (6) ―― 旧走行（gen136 期）で発火済みなので「**出たコード44**」の側に入る
- `triage_codes.py` ―― 関数は呼ばれているので **(c) にも (b) にも載らない**

第14.3版の追補に書いた **「その検査を通す入力を1件でも作れるか」** を当てて初めて出ました。
**作れるのに 0 件なので (a) ではなく (b) です。**自分で書いた規律に、自分で助けられた形です。

## 処置（第14.4版）

1. **`check_seat_words` を橋へ差し替え**（1行）。新旧どちらの形でも立ちます
2. **`audit_retired_reads()`** を追加 ―― `REQ_RETIRED` の欄を、橋を通さずに読む関数を名指しする
   （`A57_RETIRED_READ_UNBRIDGED`）。**旧欄も見るが新しい側も見ている形は通します**
   （`check_quantity_sources` の畳み方が正しいので）
3. **`test_v14_4.py`（13項目）**。0 を返す監査なので、**0 を返さない入力を1件見せる対照**を付けました
4. 規律を `CLAUDE.md` に一行：**読み手を全部 橋へ移す → 監査が 0 → `REQ_RETIRED` へ入れる。逆をやらない**

## 直した直後に、レンダラの欠陥が2件出ました

**A23 が死んでいた間、`render_slides.py` は「⑥に座席の様式語を出す」を一度も検められていません。**

```
E1-P2  入試広報課長／学部長会      6語すべて⑥の本文に無い
R1-P2  商品本部バイヤー            原価率・取引条件・粗利率 が無い（店長側は「作業時間」で通る）
```

**Π₂ 通関律の中核です** ―― その席の帳票にある語で書かれていなければ、その席では読めません。
レンダラが自分の検査に落とされるのは**三度目**（`R9_V0` 6/8、`A43` 5/8 に続く）。
**レンダラ側の欠陥なので、処置は VS Code へ返します。**回帰は 2セルで固定してあり、3セル目は許しません。

## 版

`sales_logic.py` sha256[:12] **`77de8ffa8f17`**／理由コード **209 = 209**
回帰 **345 / 13 / 20 / 13**（`test_sales_logic` / `v14_2` / `v14_3` / `v14_4`）
`audit_requirements` **6**／`audit_symbols` **0**／**`audit_retired_reads` 0**
