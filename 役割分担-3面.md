# 役割分担 ―― Cowork / VS Code / 通常スレッド

決め方の軸は「何ができるか」ではなく、**何が状態を持つか**と**誰が評価者か**の二つ。

| | 状態を持つもの | 評価者 | 向いている仕事 |
|---|---|---|---|
| **通常スレッド** | 会話 | **いない** | 問いを立てる／選択肢を並べる／短い判断 |
| **VS Code**（Claude Code） | **リポジトリ** | テスト（回帰） | 検証・実装・繰り返し回す作業 |
| **Cowork**（クラウド） | **走行** | **別の体**（盲検・多体） | 生成と評価を分けねばならない仕事 |

---

## 1. モックアップが壊滅したのは、面の選択を誤ったから

診断は当たっています。**通常スレッドには評価者がいません。**
一つの文脈の中で作って、同じ文脈の中で「よく書けている」と判定している。
このプロジェクトが最初に捨てた作り方そのものです。

本モデルは同じ問題を三役の分離で解いています ―― 生成器／独立予測器／盲検買い手。
そして三つの規律がある。

1. **走行前に予測を置く**（後から基準を動かせなくする）
2. **買い手はペルソナと資料しか見ない**（作り手の意図を知らない）
3. **突合は依頼側の索引で行う**（エージェントの自己申告を信じない）

**モックアップにも、そのまま同じ形が使えます。**しかも都合がよいことに、
**UI は資料と同じく「座席が読むもの」**です。誰が・どの基準で・どの様式の語で読むかは、
モデルが既に `seats` / `kappa` / `form` / `chi` として持っている。

### 「AI臭い」の正体と、機械の handle

これは語彙の問題で、既に道具があります。

- `V0` / `BAN` ―― 形式体系の語を本文に出さない（既に 0/36枚で守られている）
- `form_n` ―― **その座席の稟議に載る語**。載らない語で書いたものは会議に出せない
- `A23_SEAT_WORD_ABSENT` ―― 座席の様式語が本文に無ければ停止

**「AI臭い」＝ 誰の様式の語でもない語で書かれている、ということです。**
UI コピー用に同じ形の検査を一本足せば、機械で落とせます
（体言止めの連打／「〜という点において」／見出し語だけの箇条書き／「重要です」「最適化」
「シームレス」等の無担体語）。**作るのは VS Code、当てるのは Cowork。**

---

## 2. これからの作業の割り振り

**モックアップ用に、教育以外の2業界で 業界分析・顧客分析・サンプルデータ生成。**

| # | 作業 | 面 | 理由 |
|---|---|---|---|
| 1 | **2業界の選定** | **Cowork（計算）** | 勘で選ばない。`ind25.json` から、学校法人と**軸で最も遠い**組を機械で出す（拘束の所在／執行座席／暦／κ_n／θ・σp）。較正が効かない業界を選ぶと、導出と較正の分離も同時に測れる |
| 2 | **業界分析・顧客分析（ν の記入）** | **Cowork（多体）** | 前例がある。25業界の記入で**一度も走らせずに規定の穴が256件**出た。記入者と検分者を分ける |
| 3 | 記入規定の穴の検分・回帰化 | **VS Code** | 検証はリポジトリ側 |
| 4 | **サンプルデータ生成**（4〜8セル） | **Cowork** | 多体走行はここにしかない |
| 5 | 生成物の機械採点（`validate_*`） | **VS Code が作る／Cowork が当てる** | コードはリポジトリ、走らせるのは走行の側 |
| 6 | **モックアップの実装** | **VS Code** | ファイルが状態。差分が見え、テストが書ける |
| 7 | **モックアップの評価** | **Cowork（盲検・座席）** | **作成者と評価者を分ける。ここが今回の失敗点** |
| 8 | UI コピーの語の検査 | **VS Code が作る／Cowork が当てる** | 上記「AI臭い」の handle |
| 9 | MBA 的議論 | **通常スレッド可** | ただし**結論はリポジトリかプロジェクトに書き戻す**。会話に置いたままにしない |

### #7 の手順（今回の失敗を繰り返さないために）

```
1. 評価基準を**先に**書く。何をもって「見やすい」とするか、何をもって「通る」とするか。
   モックアップを見る前に書く。見てから書くと、目の前のものに合わせてしまう。
2. 実装は VS Code。**評価者には作り手の意図を渡さない。**
   渡すのは「画面」と「あなたは誰か（座席）」だけ。
3. 座席ごとに盲検で読ませる。判定は 通過／揺らぐ／棄却。理由は種類で申告させる。
4. 申告と散文の二重で数える。食い違ったら**食い違いを第一の結果**として扱う
   （第13.5版で 申告16／散文3 の食い違いが出て、判定を保留にした）。
```

---

## 3. Cowork が重くなる問題 ―― 運用で解く

原因ははっきりしています。**会話そのものが状態を持っているから**です。
生成物を会話に載せると、そのぶん全部が以後の文脈に乗り続けます。

- **1セッション1走行。** 走行が終わったらレポートをプロジェクトに書いて、**セッションを切る**。
  今日は 3走行＋実装を1セッションでやって利用上限に当たりました。
- **生成物を会話に載せない。** ファイルへ書き、会話には**数と結論だけ**。
  今日の走行結果が会話に流れてきたのは、ワークフローの返り値をそのまま受けたからです。
  `harvest.py` で journal から直接ファイルへ落とす経路を使えば、会話に乗りません。
- **コードは Cowork で書かない。** 書くと bundle の往復が発生します。今日それで3往復しました。
- **長い走行の前に必ず予測を置く。** 途中で切れても、置いた予測が残っていれば再開できます。
  今日 R2-P2 の買い手2体が上限で落ちましたが、予測があるので走行は継続可能です。

---

## 4. 通常スレッドの使いどころ ―― 成果物を作らせない

使ってよい：**問いを立てる／選択肢を並べる／短い事実確認／文章の一段落を直す。**
使ってはいけない：**成果物（モックアップ・資料・データ・コード）を作らせる。**

理由は一つ。**評価者がいないからです。**
出てきたものが良いかどうかを、同じ文脈が判定している。
このプロジェクトが第8版から一貫して避けてきた形です。

議論の結論は、必ず

- コードに関わるもの → リポジトリ（`SPEC8.md` か引き継ぎ書）
- 実験・モデルに関わるもの → プロジェクトの書庫（`claude/…`）

のどちらかへ書き戻してください。会話に置いたままの結論は、次のセッションには存在しません。

---

## 5. 面をまたぐときの約束

- **意味論を変えるのは Cowork 側だけ。** VS Code は検証と提案まで
  （走行の間にコアの意味が動くと、アノマリーの効果測定が切り分け不能になる）。
- **版はタグで指す。**短縮ハッシュで指さない（一度これで事故を起こしました）。
- **as-run は上書きしない。**`predict_*.md` は走行後に書き換えない。外れも記録です。
- **リポジトリが正。** Cowork のサンドボックスはセッションが終われば消えます。
