# 第12版 ── 提示仕様アーム3本の設計と、走る前の予測

第10版の走行（学校法人／小売 8セル）で出た **stop 25件** を、指示文との機械的な照合で3つに分けた。
分類は「その要求が指示文のどこに書いてあったか」だけで決まる。

| 群 | 件数 | 内訳 | 置き場所 | 処置 |
|---|---|---|---|---|
| **(A) 指示文に書いていなかった** | 8 | R12b 1／R10a 5／R13 2 | ― | 実験しない。全アーム共通に修正 |
| **(B) リストにはあったが枚仕様に無い** | 7 | A16 | 【書き方の制約】のみ | Arm 1 で枚仕様に書き下ろす |
| **(C) 枚仕様にあったのに守られなかった** | 10 | R10b_UNIT_ABSENT 5／UNIT_REPLACED 5 | ⑥の (1) に明記 | Arm 2 で字数を配分し直す |

## 前提となる観察 ── 同一プロンプト内に対照が成立していた

```
リスト単独        A16_CHAIN「⑥の量は、資料を読むすべての座席の基準で読める形にする」  → 0/7
リスト＋枚仕様     R11_FORM_MAPPING ＋ ③の一文「…すべての座席の様式語から一つずつ選ぶ」  → 6/6
```

**リストに1行あるだけでは守られない。** 第11版で自動的に増える3行も、増えるのはリストの側である。
ただし交絡が1つ：③の一文には「一つずつ」と**個数**が書いてあり、A16 のリスト行には無い。
効いているのが〈置き場所〉か〈個数の明示〉かは、この設計では分けていない（Arm 1 は両方入れる）。

## 共通修正（全アームのベースラインに入れる。実験対象ではない）

1. **着手期限を指示文に出す** ── `start_deadline` は決定表にあり `validate_copy` にも渡していたのに、
   生成器には一度も渡していなかった。R1-P2 は教えていない期限を3日超えて落ちた。
2. **R10a を rules に載せる** ── 「④で反復を宣言するなら⑥が同じ周期で問題を作り直すな」。
   `fire_rules` にも `messages.json` にも無かった。検査だけして伝えていなかった。
3. **A24：`s6_realize` を組の配列に**（モデル側の型の修正）──
   指示は「その人が減らせる費目**だけを挙げる**」と複数形なのに、申告欄は単数の文字列だった。
   生成器は「媒体費・広報外注費」と連結して申告し、そんな費目は無いと判定された。
   `act⟨w,d,o⟩` は集合である。診断コード `A24_REALIZE_MERGED` を新設（連結と誤費目を区別する）。
4. **A23 の紙側 `A23_SEAT_WORD_ABSENT`**（実装中に発見した穴）──
   `check_chain` は `s6_kappa_by_seat` の**申告**しか見ないので、申告するだけで A16 が消えてしまう。
   R10b が②の単位についてやっているのと同じ照合を、座席の様式語についても行う。
   これが無いと、アーム比較は「本文の遵守」ではなく「申告の遵守」を測ってしまう。

## アーム

| | 内容 | 差分行（8セル） |
|---|---|---|
| **Arm 0** | 共通修正のみ。⑥の枚仕様・字数・申告欄は第10版のまま | 走行時→arm0 で 12〜13行 |
| **Arm 1** | Arm 0 ＋ ⑥の枚仕様に**座席をその場に列挙**（③と同型、個数明示）＋ 申告欄 `s6_kappa_by_seat` | arm0→arm1 で 13行 |
| **Arm 2** | Arm 1 ＋ ⑥の字数 **700〜900字**、末尾に表を許可 | arm1→arm2 で 3行 |

Arm 1 の ⑥ 仕様（R2-P1 の場合）:

```
(1) 量は、資料を読む座席の数だけ並べる。この資料を読むのは 店長・社長 の2座席。
    ・店長 に向けて：作業時間／廃棄率 のいずれかの語で読める量を一つ
    ・社長 に向けて：仕入原価／手元資金／粗利 のいずれかの語で読める量を一つ
    座席名を明記して並べること。一つの量で全部の座席を賄おうとしない。
(2) ②で使った単位は、(1) の並びの中にそのまま残す。
```

②の単位保持を (1) の**内側**に置いたのが要点。第10版では「終端の量を併記」と「②の単位を残す」が
同じ (1) に並列で入っていて、字数が詰まると片方が落ちた。座席ごとの並びの中に位置づければ、
落とすと構造が壊れるので落ちにくい（という仮説）。

### 却下した案

- **⑥を2枚に割る** ── 枚の集合 Σ は Π₁ の階段から導出される。提示仕様が勝手に7枚目を作ると形式系と矛盾する。⑥の内部を2部構成にするのが正しい。
- **最後に自己チェックリストを通させる** ── 効果が測りにくく、他のアームと交絡する。

## 測定 ── 買い手を使わない

仕様の遵守を測るのに買い手は要らない。`validate_copy` が決定的に判定する。

- **第1段（今回）**：3アーム × 8セル ＝ **生成24体のみ**。予測者・盲検買い手なし。
- **第2段（勝ったアームだけ）**：+予測8 +盲検買い手8 ＝ 16体。「仕様を満たした資料が実際に通るか」を測る。

層が分かれたのだから、測定も分けられる。

## 走る前の予測

| | stop 合計 | A16 | R10b | A23紙側 | pass |
|---|---|---|---|---|---|
| 第10版（実測） | 25 | 7 | 10 | ― | 0/8 |
| **Arm 0** | **17** | 7 | 10 | 0 | 0/8 |
| **Arm 1** | **10** | 1 | 8 | 1 | 0〜2/8 |
| **Arm 2** | **2** | 0 | 1 | 0 | 4〜8/8 |

`validate8_v11.py` に `PREDICT` として埋め込んであり、採点時に自動で突合される。**外れたものが次のアノマリー。**

判定の読み方：

- Arm 0 で A16 が 7 のまま → **リストに1行では効かない**が追認される
- Arm 1 で A16 が落ちる → **置き場所（枚仕様）が効く**。Layer 5 が原因だったと確定
- Arm 1 で A16 が落ちない → 原因は提示仕様ではない。別の機構を探す
- Arm 2 でだけ R10b が落ちる → **字数が原因**だったと確定
- Arm 1 で `A23_SEAT_WORD_ABSENT` が多い → 申告はするが本文に書かない。指示の形をさらに変える必要

## ファイル

| | |
|---|---|
| `sales_logic.py` | 1,183行。A24（`s6_realize` の組）／`A23_SEAT_WORD_ABSENT`／`R10a_NO_REPRODUCE` を追加。87μs/セル |
| `test_sales_logic.py` | **93項目 全通過**（第10版時点の81 ＋ A24 5 ＋ 伝達漏れ 4 ＋ A23紙側 3） |
| `prompts8_v11.py` | アーム3本を生成。`prompts8_v11_arm{0,1,2}.json` |
| `wf_v11.js` | 24エージェント（生成のみ）。`s6_realize`／`s6_kappa_by_seat` は required に入れない（**入れたかどうか自体が測定対象**） |
| `validate8_v11.py` | アーム別に採点し、`PREDICT` と突合 |
| `prompts8_v10_ASRUN.json` | 走行時に実際に渡した指示文（第10版）。差分の基準として保存 |
