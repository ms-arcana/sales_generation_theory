# 引き継ぎ書（VS Code / Claude Code 側）―― **コード群の検証**

あなたの仕事は**検証**です。実験の設計・走行・レポートは Cowork 側が持ちます。
版：**tag `v13.6`** ／ `sales_logic.py sha256[:12] = a6d1c8416305` ／ 2,228行。

**最初に版を確かめてください。**（一度、`main` が21コミット遅れたまま `v13.6` タグが作業途中に
打たれていて、この引き継ぎ書の指すコミットがリモートに無い、という事故を起こしています。）

```bash
git describe --tags --exact-match          # → v13.6   （コミットの短縮ハッシュには依存しない）
python3 -c "import hashlib,pathlib;print(hashlib.sha256(pathlib.Path('sales_logic.py').read_bytes()).hexdigest()[:12])"
                                                             # → a6d1c8416305
python3 test_sales_logic.py | grep -c '^ok'                  # → 239
ls validate136.py validate_stage136.py harvest.py feasible136.py audit_td.py score_reasons.py
```

**この5つが揃わなければ、取り込みが古いままです。**先へ進まず、bundle を取り直してください。

---

## 0. 役割分担 ―― これを踏み外さないこと

| | VS Code（あなた） | Cowork |
|---|---|---|
| 決定的コアの検証・回帰・物差しの検分 | **担当** | しない |
| SPEC8.md の債務返済（A24〜A43・N₄′・R20 の記載） | **担当** | しない |
| 死んだ検査・未配線の検査の洗い出し | **担当** | しない |
| マルチエージェント走行（生成8＋盲検買い手16） | **しない** | 担当 |
| 予測を置く／アノマリーを起こす／版を上げる | **しない** | 担当 |

**あなたはモデルの意味論を変えてはいけません。**
検査の**閾値・定義域・語**を変える提案は歓迎しますが、**変更は Cowork 側の版付けを通します**。
理由：アノマリーの効果測定が走行単位で行われており、走行の間に意味論が動くと切り分け不能になるからです
（第13.5版で「形式は N₄′ と同時に変える」と決めたのと同じ理屈）。

**やってよい変更**：回帰テストの追加／検査の**欠陥**の修正（下記 §2 の型に当たるもの）／
ドキュメント／死んだコードの削除の**提案**（削除自体は提案まで）。

---

## 1. まず動かす

```bash
python3 test_sales_logic.py        # 239項目。すべて ok が出ること
python3 -c "from sales_logic import audit_requirements as a; print(len(a()))"   # → 8
python3 feasible136.py             # 8セルとも解が在ること
python3 audit_td.py                # 25業界の T-D 検分（A42）
python3 regen_v12.py               # 決定表と指示文を作り直す。**決定表が不変であること**
python3 validate136.py             # 第13.6版の機械採点
```

外部ネットワーク・乱数・時刻に依存しません。`TODAY` は `cells8_v9.py` で固定です。
`regen_v12.py` は `assert_fresh()` で「決定表がいまのコードで計算されたものか」を突き合わせます。
**ここが落ちたら、それより先へ進まないこと。**古い決定表が指示文に入る事故を二度踏んでいます。

---

## 2. 最優先の仕事 ―― 物差しの検分

**本モデルで最も多い欠陥は、モデルの誤りではなく検査の誤りです。今日だけで5件出ました。**

| # | どこ | 何が起きていたか |
|---|---|---|
| 1 | `own_retracted` の数え方 | 散文の否定（「取り消す必要は生じなかった」）を非 null と数えた。12→6 |
| 2 | 「タダ」の照合 | 接続詞の「ただ、」に当たった。16→2 |
| 3 | `SLOT_RE` | 括弧の中に空白しか無い前提。`【　　　ポイント】` を記入欄と読まず、9個置いた資料で0個と判定 |
| 4 | 散文で日程の苦情を数える物差し | **前の版の語彙で較正されていた**。同じ苦情が別の語で書かれると測れない（申告16 / 散文3） |
| 5 | `R20` の `is_bottom` | **記入欄【　　　】を「値が在る」と読んだ**。17行中13行が記入欄なのに停止0件。直したら停止21件 |

**五つとも「浅い一致」です。規則ではなく型です。**

### やること

1. `sales_logic.py` の**文字列に触っている全箇所**（粗い数えで229行）を機械的に列挙し、
   一つずつ**実データに当てて**、主張どおりに発火するか／しないかを確かめる。
   第12.1版に前例があります（178箇所を全数走査した）。**同じことを、今の版でもう一度やる。**
2. 特に見るべき型：
   - **⊥ の扱い**：空文字・`None`・記入欄・「なし」「未定」・散文での否定 ―― 全部 ⊥ か。
     `N₂` は「⊥ はいかなる値とも比較できない」と言う。**⊥ を値として扱っている箇所を探す。**
   - **部分一致**：`in` で照合しているところは、境界（語の切れ目）を取っているか。
   - **単位・桁**：値の文字列の中に単位が混ざっていないか（`unit_in_value` はその一例。他にもあるはず）。
   - **語彙への依存**：ある版の言い方でしか当たらない照合になっていないか。
3. 見つけたら**回帰テストを先に書いて落としてから**直す。テストの名前に「何が起きていたか」を書く。

---

## 3. 次の仕事 ―― 発火していない検査の棚卸し

```
辞書のコード 178 ／ 8セルで一度でも発火 26 ／ 一度も出ていない 152
回帰で名指しされているコード 103／178 ＝ 57%
```

**「出ていない」には三種類あります。区別してください。**

- (a) **出るはずがない**（そのセルに条件が無い）―― 正常。
- (b) **出るはずなのに出ていない** ―― 第12版の `R10a_NO_REPRODUCE` がこれでした
  （規則は足したのに、古い決定表を読む指示文には一度も載らなかった）。**最も高くつく型。**
- (c) **配線されていない** ―― 定義されているのに `compile_deal` にも `validate_copy` にも呼ばれていない。

現時点で (c) の候補：

```
check_applies      check_binder      check_binder_dim      check_tau_order
```

このうち `check_applies` と `check_binder` は `check_tau` の中から呼ばれているので (c) ではありません。
`check_binder_dim` と `check_tau_order` を**実際に追ってください**。`check_tau_order`（R12）は
`check_tau` の末尾で呼ばれていますが、その結果が使われているかを確かめること。

**やること**：178コードそれぞれについて (a)(b)(c) を判定した表を作る。
(b) が見つかったら、それは Cowork 側へ**即座に**返す（走行の前提が崩れているため）。
(c) は削除の提案。回帰で名指しされていない75コードには、最低限「発火させる回帰」を足す。

---

## 4. 三つ目 ―― 決定表の白名簿の穴

`compile_deal()` が返す辞書のうち、`cells8_v10.run()` の `rec.update({k: d.get(k) for k in (...)})`
に**書かれた鍵だけ**が決定表に載ります。ここに書き忘れると、**計算しているのに指示文にも検査にも渡りません。**

今日これで詰まりました ―― `lt_months` / `today` / `decide_deadline` / `decision_gates` / `omega` / `busy_months`
が白名簿に無く、A37 の検査が「渡されていない」判定になっていた。

**やること**：`compile_deal` が返す鍵の集合と、白名簿の集合を突き合わせる回帰テストを書く。
落ちるべきは「compile_deal が出しているのに白名簿に無い鍵」。
意図的に載せない鍵があるなら、**明示的な除外リスト**に書かせること（黙って落とさない）。

---

## 5. 四つ目 ―― SPEC8.md の債務

`SPEC8.md` は第11版までしか書かれていません。**未記載**：

```
A24 A25 A25b A25c A26 A27 A28 A29 A30 A31 A32 A33 A34 A35 A36
A37 A37b A38 A39 A40 A41 A42 A43        N₄′        R20
```

**記法（N）と原理（Π）の側から書くこと。**アノマリー番号の羅列にしない。
今日の三件は同じ形をしています ―― **一つの欄／規則が二つの担体を持っていた**。

```
A37   s6_start_date  ＝ 〈決定が締まる日〉 と 〈実際に動き出す日〉
A37b  LT_months      ＝ 〈決定→着手〉     と 〈着手→境界〉
A41   τ 項           ＝ 〈今やる理由の担体〉 と 〈決定が物理的に締まりうる窓〉
```

これは `N₆`（要求の四つ組）が捕まえるはずの型です。**`N₆` に「担体は一つ」を書けるか**を検討し、
書けるなら `audit_requirements()` に検査を足してください（**足すこと自体は提案まで**）。

---

## 6. 五つ目 ―― 導出と較正の棚卸し

規律：**導出表（原理から出るもの）と較正表（実測から出るもの）を分け、衝突したら較正が譲る。**
未較正の業界では較正由来の判定を降格する（`apply_calibration` / `CALIBRATED_CODES`）。

今日、較正値を一つ足しました ―― `Nu.busy_months`（買い手の繁忙期）。
出所は**第13.5版の買い手16体の言葉**であり、8セル分しかありません。
**未較正の業界でこれが効いてはいけません。**`apply_calibration` に載っているかを確かめてください。

**やること**：`CALIBRATED_CODES` に載っているコードと、実際に較正由来の表を引いている検査の集合が
一致しているかを機械で突き合わせる。ずれていたら（＝較正由来なのに降格されない判定があれば）報告。

---

## 7. リポジトリの地図

```
sales_logic.py          決定的コア。2,228行・標準ライブラリのみ・check_* 28本・audit_* 2本
messages.json           理由コードの表示文。findings 113 / judgments 65 / blocks 34 / rules 32
test_sales_logic.py     回帰 239項目。**各項目が買い手の逐語と対応づけられている**
cells8_v10.py           8セルの ν（buyer×商材）。TODAY はここで固定
stamp.py                走行物に版を刻む。dump_stamped / load / assert_fresh
regen_v12.py            決定表と指示文を作り直す（走らせない）。assert_fresh を通る
prompts8_v11.py         決定表 → 日本語の指示。**判定は書かない**（0行であること）
validate8_v12.py        生成物 → Declared への変換と score()。層3.5 の入口
validate136.py          第13.6版の機械採点（M1〜M11）
validate_stage136.py    第13.6版の買い手採点（B1〜B9）
score_reasons.py        買い手の理由を二重に数える（申告／散文）。**§2 の 4件目はここ**
feasible136.py          制約が重なったときの充足可能性を総当たりで確かめる
audit_td.py             25業界の T-D 記入の検分（A42）
harvest.py              走行の journal から返り値を取り出す
SPEC8.md                仕様書（第11版まで。§5 の債務）
引き継ぎ書-第13.6版.md    Cowork 側の状態と次の一手
predict_v13_*.md        **走行前**に置いた予測。走行後に書き換えない
verified*.json          採点済みの走行物（版が刻んである）
```

---

## 8. 触ってはいけないもの

- `predict_*.md` ―― **走行前に置いた予測**です。走行後に書き換えない。外れも記録です。
- `decisions8_v10.json` / `prompts8_v11_arm*.json` ―― **as-run**（実際に走らせたもの）。上書きしない。
- `stage2/` `stage135/` `stage136/` `gen135/` `gen136/` ―― 走行の生データ。
- `_stamp` を持つファイルは `stamp.load()` で読むこと。`json.load` で直に読むと `{"_stamp":..,"data":..}` が返ります。

---

## 9. 報告の形

見つけたものは、この形で返してください（Cowork 側がそのまま版に載せます）。

```
所在   入力ν / モデル / 決定表の受け渡し / 生成の指示 / 検査 / 配管   のどれか
型     浅い一致 / ⊥ の取り違え / 担体の二重 / 配線漏れ / 適用範囲のずれ / その他
証拠   実データに当てた結果。**「こう見える」ではなく「この入力でこう出た」**
回帰   落ちるテストを先に書く。名前に「何が起きていたか」を書く
影響   これまでの走行の数字のうち、どれが影響を受けるか
```

**最後に一つ。** この作業でいちばん価値があるのは、**直すこと**ではなく
**「今まで正しいと思って数えていた数のうち、どれが信用できないか」を確定させること**です。
今日、⑥の通過数・棄却理由の数・停止の数が、いずれも一度は誤った物差しで数えられていました。
