# 第14.1版 ―― 未発火コードの仕分け（§3）と、21件の採点し直し（§5 #4）

版：`sales_logic.py` sha256[:12] `5f188dfd73e3`（v14 から変えていない）
回帰 **327項目 全通過**（314＋13）／`N₆` 要求監査 8 ／ `N₆` 記号監査 0 ／ 整合性 (3)(4)(5) なし

引き継ぎ書 第14版 §3「いま一番やってほしいこと ―― 未発火 167コードの仕分け」の報告です。

---

## 0. 結論を三行で

1. **(b) が出ました。**`A46_DISCLAIMER_REPEATED` と `A47_S2_ASKS_POSSESSION` は、
   **申告欄が生成スキーマに無いので、原理的に出られません。**指示文は 3/3 arm で要求しています。
2. **(c) は 0 件でした。**全ての検査関数が、実行時に入口から呼ばれています。
3. **仕分けの前に、数えている物差しが壊れていました。**「167／257」は
   **正しくは 164／208**。旧計測は両側で誤っていました（**A54 の 9〜12件目**）。

指示どおり、**ここで止めます。**§5 の #2〜#5 には手を付けていません。

---

## 1. まず物差しが壊れていた ―― A54 の 9〜12件目

仕分けを始める前に「167」と「257」を再現しようとして、**再現しませんでした**（170／257）。
差の2件は `LT_months` と `N6_SYMBOL_AMBIGUOUS` ―― N₆追補で `sales_logic.py` に増えた文字列です。
`LT_months` は `Sym("LT_months", …)` の**記号名**であって、理由コードではありません。

追ったところ、`audit_model.py` の物差しは**四箇所**で浅い一致を踏んでいました。

| # | 何を間違えたか | 影響 |
|---|---|---|
| ⑨ | `"([A-Z][A-Za-z0-9]*(?:_[A-Za-z0-9]+)+)"` が**文字列の形しか見ない**ので、記号名（`LT_months`）・ブロック名（`B_deadline`）・規則発火名（`R1_OK`）まで理由コードと数えた | **＋59件の水増し** |
| ⑩ | 同じ正規表現が `_` を必須にしたので、下線の無い `UNCALIBRATED` を見落とした | −1件 |
| ⑪ | literal しか見ないので、`f"R7_{d}_OK"` で組む**9件**を取りこぼした | −9件 |
| ⑫ | 発火側で走行 JSON を**生テキスト**で舐めた。`blocks` 欄・`rules` 欄を「出たコード」と数えた | **＋44件の水増し** |

⑫ には見本のような一件があります。

```json
"KAPPA_MERGED": 0,     ← 自己申告の集計ブロックの鍵。値は 0。
```

**「0件だった」という集計を、「出た」と数えていました。**

### 直した後の数

```
                        旧          新
理由コード               257    →   208     （59件は理由コードではなかった／10件を取りこぼしていた）
表示文                   208        208
一度でも出た              90    →    44
一度も出ていない          167    →   164
(2) 表示文が無いコード     60    →     1     ← A19_ORG_COST_POSITIVE だけが本物
(2) コードに無い表示文     11    →     1     ← R10b_UNIT_REVERTED だけが本物
```

**`audit_model.py` の (2) は、ほぼ全部が物差しの誤りでした。**
「大半は内部定数で、出るコードかどうかで仕分ける必要がある」（第14版 §5）という見立ては正しく、
**その仕分けは正規表現を捨てれば機械で終わりました。**

理由コードの定義を、散文ではなく実装で置きました。

> **理由コードとは、`Finding`／`Judgment` の第1引数に渡る文字列である。**構文木で取る。
> **発火とは、判定欄（`findings`／`needs_judgment`／`post_findings`／`post_judgments`）に載ったことである。**

回帰テストを13件足してあります（`test_sales_logic.py` 末尾・第14.1版の節）。
展開できない動的コードが出たら `audit_model.py` が**必ず表示します**。黙って捨てません。

---

## 2. 仕分けの道具 ―― `triage_codes.py`

人の記憶ではなく**実行トレース**で分けます。入口は二つしかありません。

```
compile_deal(ν, seller, today)   8セルの決定表（決定的・LLM を呼ばない）
validate_copy(copy, Declared…)   手元の生成物 36件で再生する
                                 （run12 / run12b / gen135 / gen136 / gen137 / pair12 / pair12b）

  発火行が実行された          → ① 出た
  関数には入ったが行は未実行   → ② 条件が偽（(a) か (b)）
  関数に一度も入らなかった    → ③ (c) 配線されていない
```

```
python3 triage_codes.py

  ① 出た                       55
  ② 関数に入ったが条件が偽      143  （34関数）
  ③ 関数に一度も入らなかった      0  （0関数）  ＝ (c) 配線されていない
  ④ 監査入口（回帰テストが叩く）  11
```

### (c) は 0 件

**全ての検査関数が、実行時に入口から呼ばれています。**書いたのに呼ばれていない検査はありません。
`audit_*` の11件だけが別入口（回帰テストが叩く）で、これは設計どおりです。

### ② を割る ―― 読む申告欄が届いているか

「条件が偽」には二種類あります。**買い手が健全だから偽**（＝(a)）と、
**そもそも測れていないから偽**（＝(b)）。後者は `dec.<欄>` で読む申告欄が届いているかで割れます。

---

## 3. **(b)** ―― A46 と A47 は、申告側が一度も測られていない

```
(7) 生成スキーマ（wf_gen137.js）の declared 欄 30 ／ REQS 34
    ★要求しているのにスキーマに欄が無い:
      s6_quantity_sources      指示文は 3/3 arm で要求している
      s2_asks_possession       指示文は 3/3 arm で要求している
      s5_disclaimers           指示文は 3/3 arm で要求している
      s6_kappa_by_seat         指示文は 2/3 arm で要求している
```

`s2_asks_possession` と `s5_disclaimers` は、**36件の生成物すべてで鍵すら現れません（0/36）**。
`wf_gen136.js`・`wf_run12b.js` のスキーマにも在りません。**一度も答えられたことがない欄です。**

指示文は、日本語ではっきり頼んでいます。

> 申告は s2_asks_possession（保有を問うたか。**False であること**）
> 断った対象を s5_disclaimers に並べて申告する（文言ではなく**対象**）。同じ対象が二度出てはならない。

構造化出力のスキーマに欄が無いので、**モデルは答えられません**。結果こうなります。

```python
if dec.s2_asks_possession is None:                    # ← 8セルで永久に真
    j.append(Judgment("A47_S2_FORM_UNDECLARED"))
elif dec.s2_asks_possession is True:
    f.append(Finding("A47_S2_ASKS_POSSESSION", "stop", …))   # ← 永久に出ない
```

### 決め手 ―― 起きていたのに、報告できなかった

`gen136/out_E2-P1.json` を v14 のコードで採点し直すと、こうなります。

```
FINDING   A47_S2_POSSESSION_QUESTION  stop  「この二つの数字が、いま手元で出せる形になっているかどうか。」
JUDGMENT  A47_S2_FORM_UNDECLARED            （申告 s2_asks_possession = None）
JUDGMENT  A46_DISCLAIMER_MANY               ⑤ に断りの文 4件（上限 3）
JUDGMENT  A46_DISCLAIMER_UNDECLARED         （申告 s5_disclaimers = None）
```

**紙側の検査は、保有を問う文を現に捕まえています。**
つまり `s2_asks_possession` が `True` であるべき事例が**実在します**。
それを申告する欄が無いために、申告側の停止は一度も出ません。A46 も同じ形です
（断りの過剰は紙側が現に検出しているのに、`A46_DISCLAIMER_REPEATED` は出られない）。

**これは第12版の `R10a` と同型です。**あのときは「規則は足りたのに指示文へ渡らなかった」。
今回は「**指示文は頼んだのに、答える欄が無かった**」。向きが逆なだけで、同じ配管です。

### 要確認（人が決めること）

`s6_quantity_sources` は 36件中 **24件で届いていました**が、`wf_gen137.js` で欄を落としています。
`wf_gen137.js` のコメントは「`s6_quantities` に**吸収済み**」と言っています。
**吸収が本当なら `check_quantity_sources` の5件は (a)、吸収が漏れているなら (b) です。**
ここはモデルの意味論なので、判断を Cowork へ返します。`s6_kappa_by_seat` も同じです。

---

## 4. (a) ―― 8セルの入力では条件が立たない 138件

残りは、関数には入るが条件が偽です。上位だけ挙げます（全量は `triage_codes.py`）。

```
check_declared        25    check_price          6    check_gates          3
check_tau             17    check_R7             6    check_chain          3
check_realize         12    check_quantity_sources 5  check_D5_binder      3
check_decidable        8    check_binder_dim     5    …
check_dates_v7         8    check_applies        4
```

**ただし (a) は「この8セルでは立たない」であって「立ちえない」ではありません。**
引き継ぎ書 §8 が言うとおり、いまの実測は E1-P1 の1セル中心で n=1 です。
**8セル・粗検16体で測り直すまで、(a) と (b) の境目は動きます。**
この137件を「出るはずがない」と確定させないでください。

---

## 5. ついでに確かめたこと ―― 回帰は無い

as-run の採点（`verified8_v12_arm1` / `verified135` / `verified136` / `verified8_v13`、計32セル）を、
**同じ生成物・同じ決定表**で v14 のコードから再生しました。

```
消えたコード種  0   ← as-run で出ていたものが、いま出なくなったものは無い
増えたコード種 15   ← A37 / A41b / A43 / A45 / A46 / A47 / A50 / R20（走行より後に入れた検査）
```

**v14 のコードは、過去の走行で出ていた判定を一つも落としていません。**
増えた15件は、その走行の後に足した検査が遡って当たったものです。

決定表の側も確かめました。`compile_deal` を8セルで走らせて構築される `Finding`／`Judgment` は
**14種**、`decisions8_v13.json` に直列化されているのも**同じ14種**。**配管で落ちている分はありません。**

---

## 6. Cowork へ返すこと

| # | 返すこと | なぜあなた側か |
|---|---|---|
| **1** | **`wf_gen137.js` の `GEN_SCHEMA.declared` に `s2_asks_possession`（boolean）と `s5_disclaimers`（string の配列）を足す** | 走行側のスキーマ。**足すまで A46・A47 の申告側は測定になりません** |
| **2** | `s6_quantity_sources`・`s6_kappa_by_seat` は本当に `s6_quantities` に吸収済みか | モデルの意味論。吸収済みなら `REQS` から外すのが筋 |
| 3 | `A19_ORG_COST_POSITIVE` の表示文（`messages.json` に無い唯一のコード） | 買い手・営業に出る文言 |
| 4 | `R10b_UNIT_REVERTED` の表示文は残すか（コード側に無い唯一の文） | 退役かどうかの判断 |

**#1 を入れずに走らせないでください。**A46・A47 は 25業界で 7/21・8/21 の頻度で出た型です。
いま走らせても、その二つは `*_UNDECLARED` が8セル分並ぶだけで、**効果測定になりません。**

---

## 7. 触っていないもの

`predict_*.md`／as-run（`decisions8_v10`・`v12`、`prompts8_v11_arm*`・`v12_arm*`）／
`gen*`・`oracle137`・`stage*` ―― いずれも読むだけです。

`regen_v13.py` を確認のため走らせたところ、`decisions8_v13.json` と `prompts8_v13_arm*.json` の
**`_stamp` だけが書き換わりました**（中身は同一）。旧 `_stamp` は
「その表を作ったのは v14 より前の `sales_logic`（`8533c901c220`）である」という来歴なので、
**`git checkout --` で戻してあります。**CLAUDE.md にこの副作用を書き足しました。

---

# 第二部 ―― §5 #4「21件を v14 のコードで採点し直す」

§3 の「止めてください」は理由が**走行**に限定されています（「先に走らせても測定になりません」）。
#2〜#5 は走行を伴わないので、独立に進められます。安い #4 から片づけました。

## 9. `R12b` の誤停止は**確定**しました

25業界走行の21件（第12.8版・`sales_logic.py` `28181539e212`）を、v14 のコードで採点し直しました。
生成物と決定表は別枝にあり、`industry-run.bundle` から読みます（`rescore21.py`）。

```
git fetch industry-run.bundle refs/heads/industry23-it-consulting:refs/remotes/bundle/industry23
python3 rescore21.py
```

```
R12b 停止 3件 ['I01-IT', 'I05-CS', 'I08-IT']
  うち**買い手は進めた** 2件 ['I05-CS', 'I08-IT']   ← 誤停止
  ⑥決定日を申告している件数: 0 / 21
```

**21件すべてで ⑥決定日が ⊥ です。**第12.8版の申告欄は日付が一つしかありませんでした。

### 何が起きているか ―― ⊥ を値として扱っている

```python
dcd = dec.s6_decide_date or dec.s6_start_date   # 旧走行は欄が一つしかない
...
if deadline and dcd:
    if a > b:
        f.append(Finding("R12b_START_AFTER_DEADLINE", "stop", …))
```

`s6_decide_date` が ⊥ のとき、**着手日を決定日に代入しています。**
そして `deadline` は A37b で担体が確定した**決定期限**です。つまり

> **着手日（担体：動き出す日）を、決定期限（担体：決定が締まる日）と比べている。**

A37b が消したはずの**型5（一つの記号が二つの担体）そのもの**が、
`or` による代入という形で残っていました。同じ関数の docstring はこう書いています ――
**「着手日 start ≥ decide ＋ LT_months（上限は無い）」。上限が無い量を、上限で止めています。**

しかも v14 は同じセルで `A37_DECIDE_UNDECLARED`（決定日が未申告）を**申し送っています**。
**「決定日を知らない」と「その決定日は遅すぎる」を同時に言っています。**

### 反実仮想 ―― 代入をやめると

```
                              R12b停止   R12b申し送り   買い手が進めたのに停止
いまの v14（着手日で代用）          3件        0件            2件
⊥ を ⊥ のまま（N₂）              0件       19件            0件
```

**3件の停止はすべて代入が作っていました。**やめると停止は申し送りへ移ります。
`R12b_START_UNDECLARED` という受け皿は**既に実装されています**。

### いま走らせても再発します

旧走行だけの話ではありません。手元の生成物31件のうち、
**着手日はあるが決定日が ⊥ のものが 14件**。そこでは同じ代入が働いています。

## 10. 提案（`check_dates_v7` の定義域なので、実装はしません）

```diff
- dcd = dec.s6_decide_date or dec.s6_start_date   # 旧走行は欄が一つしかない
+ dcd = dec.s6_decide_date                        # ⊥ は ⊥ のまま（N₂）。代用しない
```

旧走行との突合が要るなら、代用ではなく**別の申し送りコード**で明示するのが筋です
（「この走行は日付欄が一つしかない」という事実は、停止ではなく註記です）。

判断は Cowork へ返します。**入れれば 25業界の誤停止2件が消え、φ = +0.055 が測り直しになります。**

---

---

# 第三部 ―― §5 #3「V1 物差しの全数検分」

## 11. 当て直した ―― `audit_matchers.py`

`sales_logic.py` の文字列に触っている全14の物差しを、**売り手の本文290件・業界資料**に当て直しました。
見たのは五つ ―― 死んでいる物差し／境界を取らない一致／⊥ の取りこぼし／表そのものの傷／
**⊥ を生の表で判定している箇所**。

### 出たもの

| | 所見 | 判定 |
|---|---|---|
| `EXPR_MARKS` に `×`（U+00D7）が**二度**入っていた | `any(m in s …)` なので振る舞いは不変 | **落とした**（語は一つも減らしていない） |
| `UNIT_TOKENS` の並びが「長い語が先」 | `unit_in_value` は最初の一致で判定するので**順序が意味を持つ** | 正しい。**回帰で固定した** |
| `EXPR_MARKS` の `*` が Markdown の `**強調**` に当たる | 本文に120件。ただし `is_expr` の当て先は申告欄で、実データでは 0件 | **未然**。申告欄は自由記述なので、いつでも踏める |
| `POSSESSION_WORDS` の `手元` が `手元資金` に当たる | 本文で104件。ただし当て先は②の疑問文で、実際の当たりは2件・**誤検出 0** | 実害なし。`お手元` は `手元` に含まれ、常に死んでいる |
| `V0`／`V0_RE` が一度も当たらない | R9（設計語の漏洩）。**0件は「漏れていない」という結果** | 正しい。壊れていない |

**当たらない＝壊れている、ではありません。**当て先と目的を見ないと、この判定を間違えます。

## 12. いちばん大きい一件 ―― $\bot$ の述語が二つある

`is_bottom` は $\bot$ を「`UNIT_UNKNOWN` に在る ∨ 記入欄」で判定します。
ところが **`check_decidable` の4箇所が、生の表 `UNIT_UNKNOWN` を直に引いています。**

```
sales_logic.py:2126  if pu in UNIT_UNKNOWN or ru in UNIT_UNKNOWN:
sales_logic.py:2138  if den in UNIT_UNKNOWN:
sales_logic.py:2148  if legacy in UNIT_UNKNOWN:
sales_logic.py:2155  if v in UNIT_UNKNOWN:
```

そこでは**記入欄 `【　　　】` が $\bot$ と読まれません**。実際に何が起きるか、実行して確かめました。

| 申告 | いま出るもの | 出るべきもの |
|---|---|---|
| 単位欄の片方が記入欄 | `R20_UNIT_MISMATCH`（**停止**） | `R20_UNIT_UNDECLARED`（申し送り） |
| **単位欄の両方が記入欄** | **何も出ない**（⊥ = ⊥ を「一致」と読む） | `R20_UNIT_UNDECLARED` |
| **分母が記入欄** | **何も出ない** | `R20_DENOMINATOR_MISSING` |
| 出所欄が記入欄 | `A49_SOURCE_MERGED`（連結して書いた形だと言う） | `R20_SOURCE_UNDECLARED` |

**下二つは黙って通ります。**これは `R20` に `is_bottom` を入れたとき（停止 0→21件）に
直したはずの誤りの、**直し残し**です。いまの生成物では 0件 ―― **未然**です。

規律にすると、型5（担体は一つ）の述語版になります。

> **$\bot$ かどうかを決める述語は、一つでなければならない。**
> 二つ書くと、片方だけが更新されて古びる。

**直すのは検査の定義域なので提案までにとどめ**、代わりに
「**この4箇所より増やさない**」を回帰で固定しました（5箇所目を書くと落ちます）。

---

# 第四部 ―― §5 #2「`SPEC8.md` の債務返済」

## 13. 型の側から書きました（`SPEC8.md` §14）

番号（A24〜A54）を並べずに、**8つの型**として書きました。
第8版が 7命題・16規則・15条件を 3原理・5記法へ畳んだのと同じ操作です。

```
§14.0  なぜ番号ではなく型で書くか
§14.1  N₄′ ―― 量は〈比較の相手〉を持つ（記法の追加）
§14.2  R20 ―― その座席はこの紙だけで決められるか（規則の追加。Π₂ の終点）
§14.3  N₆ 追補 ―― 担体は一つ（記法の追加）
§14.4  八つの型と、それぞれの受け皿
§14.5  型6 A53 配管 ―― **向きが三つある**（計算→指示／申告→検査／指示→申告）
§14.6  型7 A54 浅い一致 ―― 一覧と、機械で先に見つける四つの当て方
§14.7  型2 の再発 ―― ⊥ の述語を二つ持つと片方が古びる
§14.8  まだ返していない債務
```

書いていて分かったことが二つあります。

1. **型5・型6・型7 で28件 ―― 52件の半分を超えます。**どれもモデルの誤りではありません
   （記法の誤り・配管の誤り・**検査の誤り**）。CLAUDE.md の見出しは正しかったことになります。
2. **本書には `## 13.` が二つあります**（第11版・N₆）。番号の払い出しが二重になった痕で、
   **A36〜A39 の衝突とまったく同じ型**です。§14 の冒頭に註記しました。

---

# 第五部 ―― §5 #5「決定的レンダラ」

## 14. `render_slides.py` ―― 157塊のうち135（85%）が LLM 抜きで出ます

設計メモの見積りは「16のうち13＝81%」でした。**8セル合計で 135/157＝85%。**

```
python3 render_slides.py            # 8セル分の被覆と字数
python3 render_slides.py --cell E1-P1
python3 render_slides.py --check    # 組んだ⑥を validate_copy に通す
```

散文へ回るのは**4種だけ**です ―― 今のやり方との比較／自社案にも当てた結果／
買い手の決定を否定していない確認／下向き説明資料。
いずれも〈買い手の語との写像〉か〈論理の向きの反転〉で、差し込みでは書けません。

**数字を作りません。**決定表に無い量は記入欄 `【　　　】` で出します。
`is_bottom` はこれを $\bot$ と読むので、`R20` が正しく立ちます。**それが正しい振る舞いです。**

### 検査に通した結果 ―― 設計メモの予測が当たりました

```
E1-P1 〜 R2-P2   8セルすべて   停止 2件   ['R20_PAY_MISSING', 'R20_RETURN_MISSING']
```

設計メモ §4 は三つの切り分けを置いて、こう予測していました。

> **通らないが理由が数字の欠落** → 問題は生成ではなく**入力側**。
> いまの `R20_RETURN_MISSING` 8/8 を踏まえると、**三つ目になる可能性が高い**と見ています。
> そのときの結論は「**LLM を使うかどうかは問題ではなかった**」です。

**8/8 で三つ目でした。**決定的に組んだ⑥は、〈払う〉〈戻る〉以外のすべての検査を通ります。
残る2件は生成の失敗ではなく、**誰も聞き取っていない数字**です。

## 15. レンダラは、自分の検査に二度落とされました

最初の実装は、このモデル自身の検査に落とされました。**両方とも直しました。**

| 落ちた検査 | 何をしていたか |
|---|---|
| `R9_V0` 6/8 | 記入欄の註に「（**拘束の所在**・執行座席・暦が同型の先）」と書いた。**設計語がテンプレートから漏れた** |
| `A43_REALIZE_IN_BUSY` 5/8 | `add_months` を**写して**実現日を計算し、繁忙期を避けなかった |

二つ目が示唆的です。`sales_logic` には `earliest_realize`（繁忙期を避ける）が既にあり、
**私はその算術を書き直していました。**これは §14.3 の型5そのもの ――
**同じ計算の担体が二つになり、片方だけが古びる**。いまは `sales_logic` の関数を呼びます。
回帰で `render_slides.add_months is sales_logic.add_months` を固定しました。

---

## 16. 変えたファイル

```
audit_model.py            物差しを構文木・判定欄ベースに。(7) 生成スキーマの配管を追加
triage_codes.py           新規。実行トレースで (a)/(b)/(c) を分ける
rescore21.py              新規。25業界21件を今のコードで採点し直す＋反実仮想
audit_matchers.py         新規。文字列に触っている全箇所を実データに当て直す
render_slides.py          新規。決定表から⑥を組む（LLM を呼ばない）
sales_logic.py            EXPR_MARKS の重複した × を落とした（**振る舞いは不変**）
SPEC8.md                  §14 を追加。A24〜A54・N₄′・R20・N₆追補を8つの型として書いた
test_sales_logic.py       第14.1版の節（26件）。314 → 340
CLAUDE.md                 期待値 340／走らせるものを追加／A54 の一覧／regen_v13 の副作用
```

**`sales_logic.py` の意味論は動いていません。**変えたのは重複した表項目1件だけで、
`any(m in s for m in EXPR_MARKS)` はそれに依存しません。

## 17. Cowork へ返すもの（優先順）

| # | 返すこと | 効き |
|---|---|---|
| **1** | `wf_gen137.js` の `GEN_SCHEMA.declared` に `s2_asks_possession`・`s5_disclaimers` を足す | **足すまで A46・A47 の申告側は測定になりません**（25業界で 7/21・8/21） |
| **2** | `check_dates_v7` の `dcd = … or dec.s6_start_date` をやめる | 25業界の誤停止2件が消え、φ = +0.055 が測り直しに |
| **3** | `check_decidable` の4箇所を `is_bottom` へ | 単位欄・分母が記入欄のとき**黙って通る**のを止める |
| 4 | `s6_quantity_sources`・`s6_kappa_by_seat` は本当に吸収済みか | 吸収済みなら `REQS` から外すのが筋 |
| 5 | `A19_ORG_COST_POSITIVE` の表示文／`R10b_UNIT_REVERTED` を残すか | 各1件 |
| 6 | 決定的に組んだ⑥を盲検買い手に当てる | **散文が要るかどうかが決まります** |
