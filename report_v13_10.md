# 第13.10版 ―― A50／A48／A49／A51／A52 を実装した

版：`sales_logic.py` sha256[:12] `8533c901c220`／回帰 **299項目 全通過**／N₆監査 8／理由コード 206
充足可能性 8/8。as-run（`decisions8_v12.json`・`prompts8_v12_arm*.json`）は無変更。

---

## 0. いちばん大きいこと

**第13.9版で「機械が見ていない」と書いた2件のうち、A50 が見えるようになりました。**

手で組んだ oracle の⑥（⑥由来の停止0・要判断0 だったもの）を新コードで測り直すと、
**`A50_REALIZE_AFTER_BOUND` が 2件**立ちます ―― 実現 2028-03-31／2028-04-30 が、
④の境界 2027-06-30 より後。**買い手2人が decisive に挙げたのと同じ事実**です。

> 「④で急ぐ理由に据えた日に、提案自身が間に合っていない」

⑦が買い手の棄却理由を**初めて名指しした**一件になります。

---

## 1. A50 ―― ④の境界日に、提案は間に合っているか

```python
earliest_realize(today, LT, ω, busy, gate)
    決定は今日でもよい／着手 ≥ 決定＋LT かつ 決定の窓以降／実現 ≥ 着手＋ω かつ繁忙期を避ける
```

この最速値が④の境界日を越えていれば `A50_BOUND_UNREACHABLE`（**生成前・要判断**）。
紙側は `A50_REALIZE_AFTER_BOUND`（生成後・要判断）。

**停止にしませんでした。**総当たりで確かめたら **4/8 が充足不能**だったからです。

```
E1-P1  境界 2027-06-30  最速の実現 2027-08-28   ← 窓 2027-05-31 が着手を押し下げる
E1-P2  境界 2027-06-30  最速の実現 2028-03-06
R1-P2  境界 2027-09-30  最速の実現 2028-02-06
R2-P2  境界 2027-09-30  最速の実現 2028-02-06
```

**売り手のリードタイムと買い手の窓では、その期日に物理的に間に合わない。**
止めると走行が成立しませんし、機械は「間に合わなくてもその日を根拠に使ってよいか」を
決められません（A22 と同型 ―― 機械は照合しかできない）。**落とさず、申し送り、指示文に書かせる。**

根本原因も分かりました。`start_deadline` は τ の **A/C 形からしか逆算しない**。
E1 の境界日は `Ec`（解禁日）なので、**何も縛っていませんでした。**

---

## 2. A48 ―― 軸の型検査

`audit_axis_types()` を置きました（`audit_requirements()` と同じ位置づけ・**9本を名指し**）。

```
λ 所在（Π₃ が8値を列挙）      ε 様式（Π₃ が2値を列挙）
ν 検証時点  θ 分割試用  σp 切替コスト  ω 発現ラグ  α 帰属可能性  β₁ 会計分類  β₂ 予算内外
```

**型検査は依然として在りません。**記法を通らない群なので、`check_axis_values` の列挙照合だけです。
できたのは「人の承認が要る箇所として明示する」ところまで ―― それが A48 の内容でもあります。

---

## 3. A49 ―― 〈出所〉の欄を割った

`pay_source` / `ret_source`。旧 `source` は後方互換で残し、**連結して書かれた形**を
`A49_SOURCE_MERGED`（要判断）で拾います。

**検分：第13.7版の生成物に当てると 3/3 座席で立ちました。**
生成器は「払う＝自社の運用手順に基づく試算／戻る＝式（係数は…）」と書いていた ――
一つの欄に二つの担体。**A37／A37b／A41／A48／A48b に続く6件目。**

---

## 4. A51 ―― 入力に ⊥ を書く場所を作った

`Product` は全成分に既定値を持っていました。

```python
nu = "使えば分かる"   theta = "段階分割"   sigma_p = "中"   ...
```

**聞き取っていない欄が、黙って特定の値になります。**型2 そのもの（未定義を既定値と読む）。
全定義域に「不明」を足し、`A51_AXIS_BOTTOM`／`A51_AXIS_UNDECLARED`（要判断）で人へ回します。
**値としては使わせません。**

これは `busy_months=()`（空＝未聞取り＝⊥）と同じ扱いを、商材座標へ広げたものです。

---

## 5. A52 ―― ⑥の〈戻る〉の式が掛ける相手を、入力の量に縛った

第13.8版で A44 の出口として〈式〉を作りましたが、**式が掛ける相手が入力に在るとは限りませんでした。**

第13.7版の走行では、3座席のうち**2つ**が入力のどこにも無い量を掛けていました
（理事会「予算科目『広報外注費』の年間執行額」など）。**生成器が発明していた。**

在るのは τ の量です（`q`／`q_low`〜`q_high`／`q_source`）。
**決定表にも指示文にも出していなかったので、発明するしかなかった。**

```
E1-P1 の買い手の量（決定表に載せ、指示文に列挙した）
  ・募集要項の確定までに接触できていない高校の数       40〜70／出所 買い手データ
  ・認証評価の受審までに整えるべき根拠資料の点数       90〜140／出所 買い手データ
```

紙側は `A52_BASIS_NOT_IN_INPUT`（要判断）。機械は真偽を見られないので停止にしません。

---

## 6. 検分（実データ）

| 当てた先 | 停止 | 要判断 | 新しく立ったもの |
|---|---|---|---|
| 8セルの決定表（生成前） | 変化なし | +4 | `A50_BOUND_UNREACHABLE` 4/8（総当たりと一致） |
| 第13.7版の**生成された⑥** | 0 | 7 → **19** | A49 ×3・A50 ×3・A52 ×2 |
| 第13.9版の**手で組んだ⑥** | 0 | 1 → **3** | A50 ×2（**買い手が挙げた日程の問題**） |

**A49 と A52 は oracle では 0 でした** ―― 手で組んだときは出所を列挙値で書き、
式ではなく値を置いたからです。**新しい検査が、生成器と手組みの差を正しく分けています。**

---

## 7. 残り

- **A50b**（散文が表の数字を言い直すときの算術）は**未着手**。第13.9版で買い手2/2 が検算して指摘した件で、いま最も高い一件です。
- **A45d**（幅を持つ金額の三状態）も未着手。
- **V3 の回帰**（`compile_deal` の鍵と受け手の突き合わせ）―― この版でも `buyer_quantities` を
  白名簿と `to_declared` へ**手で足しました**。四度目です。
- **8セル・盲検16体での測り直し**は未了。いまの実測はすべて n=1 です。
