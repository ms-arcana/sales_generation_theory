# 上司に見せる MVP ―― 必要な実装の洗い出し

## 0. 先に、いちばん重要なこと

**いまのままデモすると、自分の検査で 8/8 のセルが停止します（停止21件）。**
画面が赤で埋まる。上司は「動いていない」と読みます。

止まっている理由は一つです ―― `R20_RETURN_MISSING` が 8/8。
**〈戻る〉の欄が記入欄（⊥）のまま**だからです。
これは生成器の出来ではなく、**売り手が買い手の数字を持っていない**という入力側の欠落。

**MVP の最大の阻害要因はここで、しかも直し方は分かっています**
（`設計メモ-ROIの機械化.md` §5 ―― ⊥ の代わりに〈買い手の量 × 売り手の係数〉の式で出す）。
**これを入れないと、他を何本作ってもデモは赤いままです。**

---

## 1. 上司は何を見るのか（この製品自身の言葉で書くと）

上司も座席です。**κ と様式の語を先に決めます。**

| | |
|---|---|
| **κ（何で判断するか）** | 「これは他社の生成AIと何が違うのか」＝**説明可能性**。次に「いくらかかるか」＝価格 |
| **様式の語** | 案件数／受注率／提案書の作成時間／差し戻し回数／内製か外注か |
| **通し方** | 3分見て、**一つの具体で腑に落ちるか** |
| **止め方** | 「で、これは AI が文章を書くやつでしょ？」で終わる |

**したがって、デモの主役は生成された文章ではありません。**
文章を主役にすると、上司の頭の中で既存の生成AIと同じ棚に入ります。

> **主役は「送る前に、どの座席がなぜ止めるかを名指しする」ところ。**

そしてこれには実測の裏づけがあります ―― 25業界21件で買い手が挙げた理由の上位3つ

```
量の出所が確かめられない   21/21
価格・費用の内訳が無い     21/21
営業へ回した空欄が資料を殺す 21/21
```

**三つとも機械で検査できます。**「送る前に必ず出る不備を、送る前に出す」。これが売りです。

**同時に、盛ってはいけない一点。**⑦の検査は買い手の判定を予測していません
（φ=+0.055、無条件通過 0/21）。**「これを通せば売れる」とは言えません。**
言えるのは「**全員が指摘する不備を、送る前に潰せる**」まで。ここを踏み外すと、
最初の実案件で信用を失います。

---

## 2. いま持っているもの

| | |
|---|---|
| 決定的コア `sales_logic.py` | **2,228行**・標準ライブラリのみ・`check_*` 28本 |
| 回帰テスト | **239項目 全通過**・各項目が買い手の逐語と対応 |
| 理由コード（表示文つき） | **178種** |
| 決定表 → 指示文 → 生成 → 検査 | 8セルで一周した実績あり |
| 盲検買い手の実測 | 第13版16体・第13.5版16体・第13.6版14体・25業界21件 |
| 入力3画面のモックアップ | あり（**盲検で通過0**。UI は作り直しが要る） |
| 2業界のデータ | あり（製造・観光。今日作った） |

**中身は揃っています。足りないのは〈一本の道〉と〈見せ方〉です。**

---

## 3. 必要な実装 ―― これだけ

### 必須（これが無いとデモが成立しない）

| # | 実装 | 中身 | 面 | 規模 |
|---|---|---|---|---|
| **M1** | **`R20` に〈式〉の状態** | 値／**式（買い手の量 × 売り手の係数・係数の出所つき）**／⊥ の三状態。式なら停止しない | CW | 半日 |
| **M2** | **層(i) の算術検査** | 幅の比（上限÷下限）・内訳の有無・内訳と総額の一致・単価の単調性 | CW＋VS | 半日 |
| **M3** | **決定的レンダラ `render_slides.py`** | 決定表 → ⑥の表3つ＋定型文＋記入欄。散文はプレースホルダ。**LLM 不要** | VS | 1〜2日 |
| **M4** | **一本の道（CLI 1本）** | `python3 demo.py --buyer MFG --product P1` で 入力→決定表→指示文→生成→検査→HTML まで通る | VS | 1日 |
| **M5** | **検査結果の見せ方** | 停止／要判断を**日本語で・座席ごとに**出す。`messages.json` は既にある。**赤の羅列にしない** | VS | 1日 |

### 強く推奨（無いと「で、何が違うの」で終わる）

| # | 実装 | 中身 | 面 | 規模 |
|---|---|---|---|---|
| **M6** | **同じ案件の〈直す前／直した後〉** | 同一入力で、不備あり版と是正版を並べる。**差分が価値そのもの** | CW | 半日 |
| **M7** | **買い手の逐語を1件だけ添える** | 「1,400万から3,200万は2.3倍の開き、これは見積ではなく相場表だ」――**実測が1件あるだけで話が変わる** | ―（済） | 0 |
| **M8** | **入力画面は3画面まるごと出さない** | 盲検で通過0。**デモでは「入力済みの状態」から始める**。入力の作り込みは MVP の後 | VS | 0（やらない判断） |

### やらない（MVP に要らない）

- 認証・権限・マルチテナント
- DB（**JSON ファイルで足りる**。8セル分しかない）
- 入力3画面の作り直し（盲検の指摘21件は本番前の課題）
- 25業界への横展開（**層(i) を潰すまで業界差は見えない**）
- 閲覧ログとの接続（帰納の場だが、MVP の外）
- SPEC8.md の債務返済（社内の資産だが、上司には見せない）

---

## 4. 3分のデモ台本（実装はこれに合わせて削る）

```
0:00  入力済みの案件を開く          ← 入力画面は作り込まない（M8）
      「相手は自動車部品メーカー。決めるのは工場長、止めるのは購買部長」

0:30  〔資料を作る〕を押す
      表が3つ出る（座席ごとの量／消す部分／誰がいつ何を減らすか）
      **表は機械が決めている。**文章はそのあと

1:00  検査の結果が座席ごとに出る
      「購買部長：**相見積の規程を満たさない**。3社比較表を提案側が用意している」
      「工場長：**戻る額が空欄**。設備投資稟議は起こせない」

1:30  営業が2箇所だけ埋める（記入欄）
      もう一度検査 → 止まっていたものが消える            ← M6 の差分

2:00  「なぜこれが分かるのか」
      買い手21件の実測。**全員が挙げた3つは、送る前に機械で潰せる**

2:30  正直に言う一点
      「**これを通せば売れる、とは言えません。**
        全員が指摘する不備を、送る前に潰せるところまでです」
```

**この台本に出てこない実装は、MVP には要りません。**

---

## 5. 順序と見積り

```
第1週   M1 R20 の〈式〉         ← これが無いと赤いまま。最優先
        M2 層(i) の算術検査      ← 20〜21/21 が挙げた。安い
第2週   M3 決定的レンダラ        ← 表と定型文。LLM を呼ばない
        M4 一本の道（CLI）
第3週   M5 検査結果の見せ方
        M6 直す前／直した後
        台本どおりに一度通す（Cowork で生成を1回だけ回す）
```

**約3週間。**新しい理論は要りません。**既にあるものを一本の道につなぐだけです。**

---

## 6. 上司に必ず訊かれること（先に答えを用意する）

| 訊かれること | 答え |
|---|---|
| 「他の生成AIと何が違うの」 | **文章を書く前に、通るか通らないかを機械が決めている。**通らないなら理由と座席が出る。文章はそのあと |
| 「精度はどれくらい」 | **精度という言い方をしていません。**⑦の検査は買い手の判定を予測していない（φ=0.05）。言えるのは「21件全員が挙げた不備を、送る前に潰せる」まで |
| 「どこまで自動なの」 | 表と定型文は**全自動**（LLM を呼ばない）。散文だけ LLM。**数字は営業が埋める** ―― 売り手が持っていない数字を機械が作らないのが設計 |
| 「業界はどれだけ対応してる」 | 25業界の入力を作り、21件を走らせた。ただし**業界差はまだ測れていない**（全業界で同じ3つが 100%）。まずそこを潰す |
| 「いくらで作れるの」 | コアは**標準ライブラリのみ・2,228行・外部API なし**。動くのは 109マイクロ秒/件。**費用がかかるのは散文の生成だけ** |
| 「本当に動くの」 | **回帰239項目。**各項目が買い手の実際の発言と対応づけてある |

---

## 7. 危ないところ（先に潰す）

1. **デモの数字が自分の検査に落ちる。**幅の比・内訳・単価の単調性は、**デモデータ自身が満たしていること**。
   今日作った2業界のデータは 1.81倍／1.79倍で通してあります（`mockup_presets.json`）。
2. **赤を全部見せない。**停止が21件出る画面は「壊れている」と読まれます。
   **座席ごとに畳んで、決定的な2〜3件だけ前に出す。**
3. **生成の時間。**LLM の散文生成は数十秒かかります。**デモでは生成済みを出す。**
   その場で回すなら「ここは実際には30秒かかります」と先に言う。
4. **入力画面を見せない。**盲検で通過0。訊かれたら「入力は次の課題です」と正直に言う。
5. **「AI が書きました」と言わない。**上司の頭の中で棚が決まります。
   **「機械が判定し、文章は最後の1工程だけ」**と言う。
