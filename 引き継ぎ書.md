# 引き継ぎ書 ── 営業資料生成モデル 第12版

新しいスレッドはこの1枚から始めてください。前のスレッドはコンテキストを一度圧縮しており、これ以上続けると
「確かめていないこと」が「確かめたこと」に化ける危険があるので切りました。

---

## 0. 最初の15分でやること

**ダウンロードは要りません。すべてプロジェクトの中にあります。**（前スレッドで、JSON 以外のファイルが
クライアント側で開けないことが分かったため、全部プロジェクト経由に切り替えました。）

```
# 1) コードを復元する ── プロジェクトの4枚を project_read して展開
claude/コード束1-中核.md        sales_logic.py
claude/コード束2-回帰と辞書.md   test_sales_logic.py / messages.json
claude/コード束3-入力と指示.md   cells8_v10.py / prompts8_v10.py / prompts8_v11.py
claude/コード束4-走行と採点.md   wf_v11.js / validate8_v11.py / wf_v10.js /
                                validate8_v2.py / report8_v10.py / stamp.py
```

各枚の冒頭に復元スクリプトが載っています（`===== FILE: <名前> =====` 区切り）。
同じ内容は JSON 形式でも渡せます（`{ファイル名: 中身}` の辞書）。

```
# 2) 走行データを復元する ── 再走行しないと作れないもの
claude/data-run8-v11.json           → run8_v11.json          第12版アーム実験の生成物24件
claude/data-run8-v10.json           → run8_v10.json          第10版の走行
claude/data-prompts8-v10-asrun.json → prompts8_v10_ASRUN.json 第10版で実際に渡した指示文
claude/data-decisions8-v10.json     → decisions8_v10.json    決定表（※第11版で再計算済み。§6の罠）

# 3) 動くことを確かめる（ここが通らなければ、以降の数字は信用しない）
python3 test_sales_logic.py | tail -3        # → 97項目 すべて通過
python3 validate8_v11.py run8_v11.json       # → arm0 stop10 / arm1 stop4 / arm2 stop7

# 4) git を立てる
git init -q && git add -A && git commit -m "第12版 基準点" && git tag v12
python3 stamp.py                              # 版が刻めることを確認
```

**§3 の2つが再現しなければ、まず環境を疑ってください。** 数字が合わないまま先へ進むのが、
このプロジェクトでいちばん高くついた失敗です（→ §3）。

**復元されないもの**：`cells8_v2/v6/v6b/v7/v8/v9.py`（第6〜9版のセル定義。回帰テストが import します）と、
`SPEC8.md`（＝プロジェクトの `claude/モデルv11-仕様.md` から復元可）、過去の走行レポート HTML。
回帰テストが `ModuleNotFoundError` を出したら、その版のセル定義が無いということです。
**97項目のうちどれが落ちたかを記録したうえで**、必要なら該当セクションを一時的に切り離して進めてください
（黙って消さないこと）。

## 1. これは何か

売り手が複数の商材を複数業界へ売り込むための、営業資料の生成システム（Django ＋ LLM を想定）。
中核は「文章を書く機械」ではなく、**「この買い手のこの状況では、何を書けば通り、何を書くと落ちるか」を
機械的に確定させる装置**です。日本語を書くのは最後の1工程だけ。

7層構造。`claude/モデル-流れの図解.html` に全体図があります（**まずこれを読むのが速い**）。

```
入力ν → ①公理 → ②軸 → ③形式論理 → ④Python(compile_deal) → ⑤プロンプト → ⑥LLM → ⑦検査(validate_copy)
        └────────── モデル ──────────┘   └─生成の指示─┘  └LLM┘  └─検査─┘
```

---

## 2. モデルの現在地（第12版）

**3原理**：Π₁ 蓄積律（合意は無矛盾に積み上がる資本）／Π₂ 通関律（資本は使う座席まで運ばれないと使えない。
可能 conv ∧ 執行 ∃act の二重連言）／Π₃ 所在律（拘束と証拠は同じ λ×ε に属する）。
**5記法**：N₁ 承認演算子／N₂ 単位元と未定義／N₃ 行為の三つ組 act⟨w,d,o⟩／N₄ 単位付き量 Qty／N₅ 座席の型。

| | |
|---|---|
| `sales_logic.py` | 1,226行・標準ライブラリのみ・検査関数22本・**87μs/セル** |
| 理由コード | 停止/降格等 81種・要判断 27種・必須ブロック 33種・書き方の制約 31種 |
| `test_sales_logic.py` | **97項目 全通過** |
| アノマリー | A1〜A25（A26 は候補） |
| 仕様書 | `SPEC8.md` 624行（第11版まで。**第12版の A24・A25 は未記載＝債務**） |

### 直近3版の中身

- **第11版**：A21（W は存在しても実行を拒む）／A22（適用対象の出所）／A23（⑥の量は座席ごとの組）
- **第12版で追加**：
  - **A24** `s6_realize` を〈誰が・いつ・どの費目〉の**組の配列**に。単数欄しか無く、生成器が
    「媒体費・広報外注費」と連結して落ちていた。診断コード `A24_REALIZE_MERGED`。
  - **A23 の紙側** `A23_SEAT_WORD_ABSENT`。`check_chain` は申告しか見ないので、申告するだけで
    A16 が消えてしまう。座席の様式語が⑥の本文に在るかを照合する。
  - **A25** 単位語の照合を `unit_tokens()` で正規化。→ §3
  - 伝達漏れの修正：着手期限を指示文へ／`R10a_NO_REPRODUCE` を rules へ

---

## 3. 確定していること と、疑わしいこと

**ここがこの引き継ぎ書でいちばん大事な節です。**

### 確定している（実測が複数回ある）

- **R9（形式系の語彙が本文へ漏れる）は 6走行連続で 0/36。** 生成器に設計語を渡さず、段ごとの提示形態だけを
  指定する設計は、業界・商材・仕様を替えても保たれている。
- **κ の階数（実務性0/価格1/財源2/説明可能性3/政治的可視性4）と隣接則で、手書き25マス中24マス再現。**
  外れるのは 説明可能性→価格 の1マスのみ。
- **λ×ε の2座標で、効かない証拠 12/12 を説明できる。**
- **⑥の枚仕様に書き下ろすと効く**（Arm 1：A16 3→0、stop 10→4）。
- **出力スキーマの欄そのものが指示として働く**（Arm 0：座席申告が予測 0/8 に対し実測 8/8）。
  ただし「申告はするが本文に書かない」が起きる。**欄＋枚仕様で初めて本文に出る。**
- **枠を広げても要求は満たされない**（Arm 2：450→900字で stop 4→7 と悪化。⑥は新しい上限にまた貼り付いた）。

### 疑わしい／まだ測っていない

- **⚠ 仕様を満たした資料が、実際に買い手を通るかは一度も測っていない。**
  第12版のアーム実験は買い手を使っていない。**次にやるべきはこれ。**
- **R10a はどのアームでも動かない**（4/4/5）。指示と検査の文言がずれている（指示「同じ周期で」／
  検査 `s6 ≤ s4`＝「同じ頻度以上」）。さらに商材が3〜12か月契約と SaaS なので `s6_period=0` 以外に
  満たす道がない可能性。**→ A26 候補：比べるべきは課金周期ではなく〈提案後に問題が残る周期〉ではないか。**
- **較正台帳の `START` は唯一残る業界依存表。** 未較正業界では判定を stop→demote へ降格する設計。
- **cost 単調性（Π₁ 第2式）は依然として検査対象を持たない。** 3原理で唯一、実装が空。
- SPEC8.md §6 の X1〜X6（原理から導けないと明示してあるもの）は据え置き。

### ⚠ 過去の報告のうち、訂正されたもの

**第10版の走行結果は、当時の報告が誤っていました。**

| | 当時の報告 | 修正後 |
|---|---|---|
| stop 合計 | 25 | **13** |
| `A16_NOT_CONV_AT_SEAT` | 7 | **3** |
| `R10b`（ABSENT＋REPLACED） | 10 | **2** |

原因は **A25**：`check_unit_presence` が宣言された単位を⑥本文と**完全一致**で照合していた。生成器は
`s2_unit` に「作業時間（時間）」のような説明句つき文字列を入れるので、本文に「作業時間」が在っても外す。
しかも `kept_unit` は `check_chain` の候補基底を左右するので、**A16 の件数まで押し上げていた。**

これにより、**失敗の所在が四つに増えました**。〈入力ν〉〈モデル〉〈生成の指示〉に加えて〈**検査**〉。
A16 も R10b も買い手には見えない内部検査なので、5走行のあいだ誰も気づけませんでした。

**プロジェクト内の古い文書には、まだ訂正前の数字が残っています。**
`claude/第10版-対照実験レポート.html` と `claude/第12版-アーム設計と予測.md`（設計時の予測）が該当。
`claude/モデル-流れの図解.html` と `claude/第12版-アーム実験レポート.md` は訂正済み。

---

## 4. 次の4手（この順に）

1. **A25 の目で既存の検査を洗い直す。** 文字列の完全一致で照合している箇所が他に無いか。
   `check_seat_words` は候補を回しているので同型の穴は無いが、`check_insult` の
   `d in v or v in d` などは要確認。**汚れた物差しで測る前に、物差しを検める。**
2. **R10a を作り直す（A26）。** 宣言に〈提案後に問題が残る周期〉を足し、比較対象を課金周期から切り離す。
   指示文と検査の文言も揃える。
3. **Arm 1 を確定させる** → `prompts8_v12.py`。
4. **第2段を走らせる。** Arm 1 の生成物に予測者と盲検買い手を付ける（16体）。**ここが本丸。**

その先（着手はまだ早い）：25業界への拡大、較正台帳の `START` を埋める、Django 統合
（コアモジュールの配置、軸入力フォーム、選択ログ／閲覧ログのテーブル）。

---

## 5. 方法についての約束事（破らないこと）

- **Python には、論理式から機械的に導かれることだけを置く。** 「この買い手に響く言い回し」の類は一行も
  入れない。だからこそ検査に使える。ユーザーからの明示的な制約。
- **形式系の語彙を生成器に渡さない。** 禁止語リスト（`BAN`）で担保。6走行連続 0/36。
- **予測を先に置く。** 走る前に数値の予測を書き、外れたものをアノマリーとして扱う。
  `validate8_v11.py` の `PREDICT` がその実装。
- **生成器・独立予測器・盲検買い手の3役を分ける。** 予測を先に置くと「モデルが外した」と
  「買い手が厳しかった」を分離できる。
- **突合は配列のインデックスで行う。** エージェントが返した文字列 id を鍵にしない（一度それで欠損した）。
- **アノマリーを立てる前に「この欄は誰かが既に測っていないか」を先に引く。**
  A（検証時点）は Nelson / Darby-Karni そのものだった。Acct の硬さは cost stickiness の再発見だった。
- **業界固有のバイアスを他業界へ伝播させない。** 導出表（業界非依存）と較正表（業界依存）を分け、
  未較正業界では較正由来の判定を stop→demote へ降格する。
- **枚の集合 Σ は Π₁ の階段から導出される。** 提示仕様が勝手に7枚目を作ってはいけない
  （「⑥を2枚に割る」案を却下した理由）。

---

## 6. ファイルの地図と、罠

### 現役

| | |
|---|---|
| `sales_logic.py` | 決定的コア。`compile_deal()`（生成前）と `validate_copy()`（生成後） |
| `test_sales_logic.py` | 97項目。各アノマリーが買い手の逐語と対応づけてある |
| `messages.json` | 表示用の日本語。コードはコードだけ返す設計 |
| `cells8_v10.py` | 8セルの入力ν（学校法人E1/E2・小売R1/R2 × 商材2種） |
| `prompts8_v11.py` | アーム3本を生成 → `prompts8_v11_arm{0,1,2}.json` |
| `wf_v11.js` | 24体（生成のみ）。取り違え・欠落を検出して `log()` する |
| `validate8_v11.py` | アーム別採点＋`PREDICT` との突合 |
| `SPEC8.md` | 第11版までの形式系。**A24・A25 未記載** |
| `flow.html` | 7層の図解（＝`claude/モデル-流れの図解.html`） |

### 罠（知らないと必ず引っかかる）

- **`decisions8_v10.json` は第11版で再計算されたもの**で、第10版の走行時の決定表ではありません。
  走行時に実際に渡した指示文は **`prompts8_v10_ASRUN.json`**。差分を取るときはこちらを基準に。
- **`verified8_v11_RAW.json` は A25 修正**前**の採点**、`verified8_v11.json` が修正**後**。混ぜないこと。
- テストの「W と受領記録を入れれば、第7版の検査はすべて通る」は **A20 系を除外して**判定しています
  （第9版で A20 を足したときの措置）。不審に見えますが意図的です。
- `_bak/` は手動コピー。git の代わりにはなりません。**最初に git init を。**
- `report8_v*.py` は HTML レポート生成器。第12版用は未作成。

---

## 7. 過去に踏んだ地雷（同じ穴に落ちないため）

| | |
|---|---|
| 数式の文字化け | 原因は丸数字ではなく**複数行 `$...$`**。単一事例にパッチせず、全数式を機械的に走査して分類する |
| Workflow の `args` | 文字列として届いて `cells.map is not a function`。**エージェントに `Read` させる**方式に変更 |
| 型の混同（複数回） | A24（費目の連結）・A25（単位の説明句）は同じ型。**指示が複数形を誘うのに欄が単数**、が共通パターン |
| 0 の二義性 | `period_months=0` を「反復しない」と「即座に反復」の両方に使っていた（誤検出6件）。N₂ が要る理由 |
| 規則の向きの反転 | R16 が `⊬` になっていて「自社が同じ試験を通ることを証明してはならない」と読めた。内部検査で発見 |
| 恒偽の前件 | R1 の前件が値域の変更に追従せず、一度も発火しない書き方になっていた |
| 降格が元を落とす | `check_applies` の戻り値を一律 `continue` していて τ の元が消え、`R6_NO_TAU` が連鎖 |
| 同じ規則の二度発見 | R7（第5版）と R16（第7版）は同じ規則だった。層も別に置いていた |

---

## 8. プロジェクト文書（読む順）

1. `claude/モデル-流れの図解.html` ── **全体像。まずこれ**
2. `claude/第12版-アーム実験レポート.md` ── 直近の結果と A25 の訂正
3. `claude/モデルv11-仕様.md` ── 形式系の本体（＝`SPEC8.md`）
4. `claude/第12版-アーム設計と予測.md` ── 実験の設計思想（**数字は訂正前**）
5. `claude/統合検討-既存理論と本モデル.md` ── 既存研究との対応
6. `claude/モデルv10-商材座標.md`／`claude/モデルv9-座席の由来.md` ── 軸の追加の経緯
7. `claude/営業ウェブアプリ要求.md`／`claude/上司説明カンペ.md` ── 事業側

過去の走行レポート（第6版・第8版・第10版の HTML）は経緯の参照用。**第10版のものは数字が訂正前**です。

---

## 9. 運用（複数スレッドを使う場合）

- Cowork のセッションは1本ごとに独立したコンテナ。**別スレッドとファイルは共有されません。**
  共有されるのは**プロジェクトだけ**（＝同一アカウント内のみ）。
- 並列スレッドは、仕事が並列になってから。§4 の4手は直列です。
- 走行の出力には `sales_logic.py` のハッシュを同梱すること。二拠点になると
  「決定表がどの版で計算されたか」が必ず混ざります（既に一度混ざりかけた）。
- プロジェクトへの書き込みはそのプロジェクトの全チャットのプロンプトキャッシュを壊します。
  走行の区切りでまとめて書くこと。
