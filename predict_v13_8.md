# 第13.8版 予測 ―― R20〈式〉＋ 層(i) の算術（A45／A45b／A45c）

**走らせる前に置く。**外れも記録する。書き換えない。

版：`sales_logic.py` sha256[:12] `0e16f89909d5`／回帰 **273項目**／N₆監査 8／充足可能性 8/8
処置：**A41b（第13.7版）＋ R20〈式〉＋ A45／A45b／A45c**。A42・A46・A47 は未処置（対照）。

---

## 0. 何を直したか

| | 中身 | 由来 |
|---|---|---|
| **R20〈式〉** | 〈戻る〉が ⊥ でも、`ret_basis`（買い手の量）× `ret_coef`（売り手の係数）＋ `coef_source`（係数の出所）が揃えば**停止しない**。成分が欠ければ停止 | A44 **21/21** |
| **A45** | 上限÷下限 > **2.0** で停止。閾値は**較正値**（実測は 2.3倍で拒否／1.8倍は未検証の**二点だけ**）。`CALIBRATED_CODES` 登録済みで未較正業界では降格 | A45 **20/21** |
| **A45b** | 内訳が無ければ停止／内訳の和が総額と合わなければ停止（Π₁ 無矛盾から**導出**。降格しない） | A45b **21/21** |
| **A45c** | 数量が増えるほど単位あたりが高くなる並びで停止 | 実測「月60万 → 月75万」 |

**R20〈式〉は規制を緩めた変更である。**これまで停止だったものが通る。
だから**停止が減るのは当たり前で、それ自体は成功の証拠にならない。**
成功の証拠は**買い手の側で〈空欄〉の苦情が減ること**だけである。

---

## 1. 機械側の予測

| # | 予測 | 根拠 |
|---|---|---|
| **M1** | `R20_RETURN_MISSING` は **8/8 → 0〜3セル** | 指示で式の書き方を明示した。生成器は既に近い形で書いている |
| **M2** | `R20_RETURN_AS_EXPR`（要判断）が **5セル以上**で立つ | 上と表裏。**立たずに停止も消えたら、検査が黙っている** |
| **M3** | `R20_EXPR_COEF_UNSOURCED` が **1〜4件**出る | 係数の出所は売り手が持っていないことがある。A28 と同型 |
| **M4** | `A45B_BREAKDOWN_MISSING` は **0〜2セル** | 内訳は売り手の数字だけで書ける。**多ければ指示が届いていない** |
| **M5** | `A45_RANGE_TOO_WIDE` は **1〜4セル** | 幅を狭めるには条件を切る必要があり、生成器には難しい |
| **M6** | `A45B_BREAKDOWN_MISMATCH` は **0〜1セル** | 和を合わせるのは算術。合わないなら生成器が桁を間違えている |
| **M7** | `A45C_UNIT_PRICE_NOT_MONOTONE` は **0〜1セル** | 階層を書くセル自体が少ないはず |
| **M8** | **停止の総数は 23 → 8〜14** | R20 の13件が主因。ここが減らなければ (a) 指示が届いていない (b) 検査の欠陥 |
| **M9** | `A41B_START_BEFORE_GATE`・`A41B_S4_FOREIGN_DATE` は **0/8** | 第13.7版で指示に明記した。**as-run では E1-P1 に 1件ずつ実在した** |

**先に置く規律。**M1 が減って M2 が立たなければ、**まず検査を疑う**。
`is_bottom` が式の欄を「値が在る」と読んでいる可能性がある ―― 第13.6版とまったく同じ形。

---

## 2. 買い手側の予測（盲検16体）

| # | 予測 | 第13.6版の実測 |
|---|---|---|
| **B1** | 「営業へ回した空欄が資料を殺す」は **16/16 → 6〜11** | decisive な〈空欄〉13/14 |
| **B2** | 代わりに「**係数の出所が確かめられない**」が **3件以上**出る | 苦情が消えるのではなく**移る**と見ている。移らなければ式は効いていない |
| **B3** | 「幅が広すぎる／内訳が無い」は **減る**（各 2件以下） | 幅 20/21・内訳 21/21（25業界） |
| **B4** | **⑥の通過は 0〜3人。**16人中0人という状態は変わるが、過半には届かない | ⑥ 0/16 |
| **B5** | ⑤の棄却は**動かない**（触っていない） | 対照 |

**B4 が今回の中心の予測。**第13.7版では「⑥の通過は増えない」と書いた。
今回は**増えると書く** ―― 買い手が挙げた上位3つのうち2つ（空欄・価格）を直したから。
**0 のままなら、上位3つは決定的ではなかったことになり、⑦の設計そのものを疑う番になる。**

---

## 3. 外れ方の解釈を先に決める

| 起きたこと | 読み方 | 次の一手 |
|---|---|---|
| M1 が減り、M2 が立たない | **検査が黙っている**（`is_bottom` が式欄を値と読んだ） | 第13.6版と同じ処置。実データで欄を全数目視 |
| M4・M5 が多い（5セル以上） | 指示が届いていない（伝達） | 指示文の位置と語を変える。R12b と同じ |
| B1 が減り **B2 が出ない** | 買い手は式を**読んでいない**。式は形式的な逃げ道になった | 式に〈買い手が入れる欄〉を明示させる |
| **B4 が 0 のまま** | 上位3つは decisive ではなかった。**⑦の設計そのものへの反証** | 逆方向 oracle を最優先へ繰り上げ |
| B4 が 8 以上 | 予測より強い。**価格と空欄が主因だった** | 多業界の再走行を前倒し（床が上がった） |

---

## 4. 走行前に済ませたこと

- [x] **回帰を先に書いて落とした**：19項目（R20〈式〉6・A45系13）。`ImportError` で落ちるのを確認
- [x] **N₆ 監査が新しい欄を捕まえた**：`s6_price_*` 5欄を要求として書いていなかったので 8 → 13 に増えた。
      **監査が仕事をした。**さらに `s6_price_tiers` と `s6_price_low` の**同順位の衝突**を検出し、
      「階層は選択肢の一覧であって本提案の金額ではない」と `yields_to` に明記して解消（8 に復帰）
- [x] **新しい検査を実データに当てて検分**：
      - 金額の表記 **26種すべて読めた**（買い手に渡した本文＋25業界データ）。`parse_amount` は ⊥ を 0 に落とさない
      - 第13.6版 as-run を新コードで採点し直し、**A45 系の誤停止 0**（価格が未申告なので `A45_PRICE_UNDECLARED` の申し送りだけ）
      - `R20` の既存の停止は**そのまま 13 件**（式の欄が無いので対照が保たれている）
- [x] **A41b が as-run で現に当たった**：E1-P1 に `A41B_START_BEFORE_GATE`（着手2027-03-01 < 窓2027-05-31）と
      `A41B_S4_FOREIGN_DATE`（④2026-12-30）が**1件ずつ実在**。買い手の二つの苦情を機械が再現した
- [x] **指示文の差分を読んだ**：価格4欄・式4欄の説明が 8/8 セルに入っている
- [x] **as-run を上書きしていない**：`decisions8_v12.json` / `prompts8_v12_arm*.json` は無変更

---

## 5. 走らせ方

```
python3 regen_v13.py                          # 決定表・指示文（作成済み）
Workflow({scriptPath: ".../wf_gen138.js"})    # ⑤⑥の生成（prompts8_v13_arm1）
python3 validate8_v12.py                      # 機械側 M1–M9
stage138_build.py → wf_stage138.js            # 盲検買い手16体 B1–B5
```

**R2-P2 の店長・社長は第13.6版で欠けたまま。**ここで 16体を揃えること。
