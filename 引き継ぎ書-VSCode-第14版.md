# VS Code への指示書 ―― 第14版

前版：`引き継ぎ書-VSCode-コード検証.md`（第13.6版）。**この版で置き換えます。**

版：タグ **`v14`**（短縮ハッシュで指さない）／`sales_logic.py` sha256[:12] `5f188dfd73e3`
回帰 **314項目 全通過**／N₆ 要求監査 **8**／**N₆ 記号監査 0**／理由コード 257／表示文 208

---

## 0. あなたの持ち場 ―― 変わっていません

**検証です。**実装の細部と回帰、物差しの検分、仕様書の債務返済。
走行（マルチエージェント）と**モデルの意味論の変更**は Cowork 側が持ちます。

> **検査の閾値・定義域・語の変更は「提案まで」。**
> 走行の途中で意味が動くと、アノマリーの効果測定が切り分け不能になります。

**第14版で一つ増えました。**

> **記号を足す前に `GLOSSARY` を見る。**同じ記号が既に在るなら、書き分け（`distinct`）を
> 書かないと `audit_symbols()` が落とします。これは提案ではなく**守る規律**です。

---

## 1. まず動かす

```bash
python3 test_sales_logic.py                              # 314項目すべて ok
python3 -c "from sales_logic import audit_requirements as a, audit_symbols as b; print(len(a()), len(b()))"
                                                          # 8 0
python3 audit_model.py                                    # (3)(4)(5) が「なし」
python3 feasible136.py                                    # 8セルとも解が在る
python3 regen_v13.py                                      # 決定表と指示文を作り直して差分を読む
```

外部ネットワーク・乱数・時刻に依存しません。標準ライブラリのみ。
どれか一つでも期待値を外したら、**先に進まずに Cowork へ返してください。**

---

## 2. この版で入った新しい道具 ―― `audit_model.py`

**あなたの持ち場の V3・V5 が、これで機械の検査になりました。**
六つを毎回突き合わせます。

| | 見るもの | 状態 |
|---|---|---|
| (1) | 台帳 ↔ 理由コード | ★コードに在って台帳に無い：なし |
| (2) | 理由コード ↔ 表示文 | **表示文が無い 62 ／ コードに無い表示文 11** ← 残 |
| (3) | **決定表の配管**（V3） | `compile_deal` 31鍵／白名簿 23／指示文 23／★漏れなし ← **閉** |
| (4) | 申告欄の配管 | `Declared` 37／`REQS` 34／`to_declared` 34／★漏れなし ← **閉** |
| (5) | **較正の棚卸し**（V5） | 9件。A46 の `DISCLAIM_MAX` の登録漏れを見つけて直した ← **閉** |
| (6) | 発火していない検査 | **一度でも出た 90 ／ 一度も出ていない 167** ← 残・**最大** |

**(3) は四度手で直した箇所です。**
`lt_months` → `decide_deadline_tau` → `s6_price_*` → `s2_asks_possession` → `buyer_quantities`。
五度目からはこのスクリプトが先に見つけます。**新しい欄を足したら必ず走らせてください。**

---

## 3. いま一番やってほしいこと ―― 未発火 167コードの仕分け

**これが最大の未整理です。**`audit_model.py` (6) が出す167件を、三つに分けてください。

```
(a) 出るはずがない        入力側の定義域が閉じている／8セルでは条件が立たない
(b) 出るはずなのに出ていない  ★最も高くつく
(c) 配線されていない       検査は書いたが、呼び出し側から呼ばれていない
```

**(b) は第12版の `R10a` と同型です。**規則を足したのに指示文へ一度も載らず、
「R10a は提示の問題ではない」という結論が**丸ごと無効**になりました。

**(b) を一件でも見つけたら、そこで止めて Cowork へ返してください。**
走行の前提が崩れているので、先に走らせても測定になりません。

**副産物：**(2) の表示文のずれ（62／11）も、この仕分けで同時に解けます。
「買い手や営業に出るコードか、内部定数か」が分かれば、どちらに文が要るかが決まります。

やり方の提案（決めるのはあなた側で構いません）

```
1. codes_in_source() の 257 を、Finding / Judgment に渡っているものだけに絞る
   （EXPR_OK・BINDER_SRC・B_authority のような内部定数を外す）
2. 残りを、それを出す check_* の関数ごとにまとめる
3. 関数ごとに「8セルで条件が立つか」を入力から判定する
4. 立つはずなのに出ていないものが (b)
```

---

## 4. 第14版で畳んだこと ―― 読んでおいてください

第12版以降のアノマリー52件を型で数えたら、**8つの型**に畳めました。
**型は第12版以降増えていません。**番号が増えているのは、同じ型の**別の座標**を見つけているからです。

| 型 | 件数 | 処置 |
|---|---|---|
| 型1 連結（複数を一つの欄に） | 8 | `N₆` の card="n" |
| 型2 ⊥ を値として扱う | 2 | `N₂`。A51 で**入力にも ⊥ を置けるようにした** |
| 型3 適用範囲のずれ | 5 | 規則の全称・存在・定義域を実装へそのまま写す |
| 型4 順序・範囲の欠落 | 2 | `N₆` の `yields_to` |
| **型5 一つの記号が二つの担体** | **8 → 1** | **`N₆` 追補「担体は一つ」＋ `GLOSSARY`／`audit_symbols()`** |
| 型6 配管 | 5 → **A53** | `audit_model.py` (3)(4) |
| 型7 浅い一致 | 8 → **A54** | `audit_model.py` ＋ V1 の全数検分 |
| 欠落（検査が無かった） | 12 | 個別に実装 |

### あなたに直接効く二つ

**A53（配管）**は (3)(4) が見ます。**A54（浅い一致）は、あなたの V1 そのものです。**

```
own_retracted の数え方   散文の否定を非 null と数えた            12→6
「タダ」の照合            接続詞の「ただ、」に当たった             16→2
SLOT_RE                 括弧の中に空白しか無い前提。9個置いて0個判定
散文で苦情を数える物差し    前の版の語彙で較正されていた（申告16／散文3）
R20 の is_bottom        記入欄【　　　】を「値が在る」と読んだ。停止0→21件
R20 の三状態             生成器が式を ret 欄に直接書き「値が在る」と読まれた
④の日付照合（未然）        本文全体に当てると 8/8 誤検出。④限定で 0/8
audit_model.py（未然）    正規表現が R6b の小文字を拾えず、コードを199と数えていた（正しくは254）
```

**最後の一件は監査の道具そのものです。型7 はモデルの外にも湧きます。**
**⊥ を値として扱っている箇所**と**境界を取らない部分一致**を、まず疑ってください。

---

## 5. 残っている VS Code 側の仕事

| # | やること | なぜ |
|---|---|---|
| **1** | **未発火 167コードの仕分け**（§3） | 最大の未整理。(b) が出たら走行の前提が崩れている |
| **2** | **`SPEC8.md` の債務返済 ―― 型の側から書く** | **畳んだので、いま書けば短くなる。**A24〜A54・N₄′・N₆追補・R20 を、番号の羅列ではなく**8つの型**として書く。第8版が 7命題＋16規則＋15条件 を 3原理＋5記法 に書き直したのと同じ操作 |
| **3** | **V1 物差しの検分（全数）** | A54 は8件で、うち2件は未然。`sales_logic.py` の文字列に触っている全箇所を実データに当て直す |
| **4** | **21件を v14 のコードで採点し直す** | 走行不要。`R12b` の誤停止の疑い（停止9件中3件・うち2件は買い手が進めた）を機械で確定できる |
| **5** | **`render_slides.py`（決定的レンダラ）** | ⑥の必須要素16のうち**13は決定的に出せる**。**`oracle137.py` の手組み⑥がそのまま原型**（1,981字。生成物は 2,886字）。**LLM を呼ばない** |
| 6 | M4 一本の道（CLI）／M5 検査結果の見せ方／M6 画面遷移 | 上司デモ。`messages.json` は208コード分ある。**赤の羅列にしない**（座席ごとに畳んで決定的な2〜3件だけ前に出す） |

**#5 は今回いちばん条件が整いました。**`oracle137/out_E1-P1.json` が
「⑥由来の停止0・要判断0」を満たす⑥の実例です。**これを決定表から機械的に再現できれば、
散文は 1,862字 → 600〜900字 に落ちます。**AI 臭さは散文に集中しているので、
**散文を減らすことがそのまま AI 臭さを減らします。**

---

## 6. 触ってはいけないもの

```
predict_*.md                       走行前に置いた予測。書き換えない（外れも記録）
decisions8_v10.json                第11版の as-run
decisions8_v12.json                第13.6版の as-run
prompts8_v11_arm*.json             as-run
prompts8_v12_arm*.json             as-run
gen136/ gen137/ oracle137/ stage*/ 走行の生成物。上書きしない
```

新しい版は必ず**別のファイル名**にしてください（`decisions8_v13.json` / `prompts8_v13_arm*.json`）。
`_stamp` を持つ JSON は **`stamp.load()` で読む**（`json.load` だと包みが返ります）。

---

## 7. 版の指し方

**タグで指してください。短縮ハッシュで指さないでください。**

```bash
git describe --tags --exact-match     # v14
```

この版までのタグ：`v12`〜`v12.9` / `v13`〜`v13.10` / `v13.6-predict` / **`v14`**

---

## 8. いま Cowork 側が持っている宿題（あなたは待たなくてよい）

- **A50b** 本文の算術を申告欄と突き合わせる（買い手 2/2 が独立に検算して指摘した）
- **A45d** 幅を持つ金額の三状態
- **8セル・盲検16体で測り直す** ―― **いまの実測はすべて n=1**
- A42 の処置／N₃′ 様相／R17 の紙側／A40 戻りの因果／入力側の欄割り

**#3 が効いてきます。**第13.7〜13.10版の実測は、すべて **E1-P1 の1セル**です。
E1-P1 は8セル中**唯一〈決定の窓〉を持つ系統**で、A41b が効くように選びました。
**ここで見えたことを一般化しないでください。**
