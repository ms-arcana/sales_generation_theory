# 第12.1版 ── §4-1「A25 の目で既存検査を洗い直す」

`sales_logic.py` の比較演算を AST で全数走査（178箇所）し、文字列で照合している箇所を
一つずつ実データ（`run8_v11.json` 24体・`decisions8_v10.json`）に当てて確かめた。
**机上の指摘と実測を分けてある。**

版：`f673da0` ／ `sales_logic.py` sha256[:12] = `194a90be8f9e` ／ 回帰 **110項目 全通過**（97 ＋ 新規13）

---

## 0. 先に、いちばん重い二つ

### 0.1 R10a の指示文は、24体すべてに一度も渡っていない

第12版レポート §2④ は「**R10a はどのアームでも動かない ── 提示の問題ではない**」「指示文に載せても減らない」
と書いている。**載っていなかった。**

```
messages.json rules["R10a_NO_REPRODUCE"]
  「④で反復を宣言するなら、⑥の提案が同じ周期で同じ問題を作り直さないようにする」

arm0 のプロンプト8本に この一行が入っている数 … 0/8
arm1                                    … 0/8
arm2                                    … 0/8
```

機序は罠そのもの。第12版は `fire_rules` に `R10a_NO_REPRODUCE` を足したが、
**`decisions8_v10.json` は第11版で再計算されたまま**で、そこには入っていない（0/8）。
いまのコードで計算し直すと 6/8 で立つ。`prompts8_v11.py` はその古い決定表を読んで指示文を組む。

- 同じ経路にあった**着手期限は 6/8 で渡っている**（`build()` が `rec["start_deadline"]` を直接読むため）。
  だから「共通修正のうち2つは効いた」の R12b の側は生きている。落ちたのは rules 経由の一本だけ。
- `stamp.py` はまさにこれを防ぐために作られたのに、`decisions8_v10.json` に `_stamp` が無い。

**帰結：§4-2（R10a を作り直す）の前提が崩れた。** 作り直す前に、まず指示文を実際に渡して測り直す必要がある。
「R10a は提示の問題ではない」はまだ言えない。

### 0.2 A16 の残り2件は、A24・A25 と同じ型の第3例だった

arm0 に残っていた `A16_NOT_CONV_AT_SEAT` 2件（E2-P1・R2-P2）の中身は、座席の通関の失敗ではなかった。

```
s6_kappa = "価格・財源"          ← 連結された基準名
s6_kappa_by_seat[理事長] = "価格・財源"
```

`κ_n` が2つある4セルでだけ起き、1つのセルでは 0件。原因は**指示文の側**で、
プロンプトは `'・'.join(kappa_n)` を使うので「価格・財源」という連結形を **arm0 で4回・arm1 で3回**書いている。
一方スキーマの欄は単数（「実務性／価格／財源／説明可能性／政治的可視性 のいずれか」）。

> **指示が複数形を誘うのに欄が単数** ── 引き継ぎ書 §7 が名づけたパターンの、A24（費目の連結）・A25（単位の説明句）に続く3例目。

しかも診断が割れていた。`check_declared` は `EXPR_TABLE_MISS`（**要判断**）を出すだけで停止させず、
`check_chain` は同じ値を「読めない基準」と扱って**停止**させる。
要判断は stop の集計に出ないので、5走行のあいだ**表に現れなかった**。

---

## 1. 直したもの（物差しの誤りが確定しているものだけ）

予測を先に置き（`predict_v12_1.md`）、そのうえで直した。**予測は完全に的中した。**

| | 修正 | 根拠 |
|---|---|---|
| **A25b** | `unit_tokens` に助詞「の」の区切りを追加 | E2-P2 arm0：宣言が文（「担任1人あたりが年間に募集へ充てる日数（担任工数の日数）」）で照合語が取れず、⑥に「担任工数」も「日数」も在るのに R10b が発火していた。**A25 の取りこぼし** |
| **A25b** | 候補が空集合（単位が1文字）なら、停止でも通過でもなく**要判断**へ（`R10b_UNIT_UNCHECKABLE`） | `unit_tokens("件") = ∅` → 現行は**必ず停止**。日本語の単位は1文字が普通（件・人・日・円・回・台・店）なので、25業界へ広げれば確実に踏む。照合できないことは「無い」ことではない（N₂） |
| **A25b** | ⑥が Σ に無ければ R10b を検査しない | 潜在。現8セルは全部⑥を持つ |
| **A25b** | `R10b_UNIT_ABSENT` と `R10b_UNIT_REPLACED` の二重発火をやめ、停止は ABSENT 一本に | E2-P2 arm0 で同じ一つの事実を2回数えていた。`validate8_v11.py` は両者を足して R10b 件数にしている |
| **A25c** | 連結された基準名を照合側で正規化（`kappa_tokens`）。診断 `A25_KAPPA_MERGED` を info で残す | 上記 0.2 |
| **A25c** | `check_v0` の `\bD[1-7]\b` を ASCII 境界へ | **`\b` は日本語の隣で境界にならない**（Python の `\w` は日本語を含む）。「御社のD5について」は一致しない。つまり設計語の記号は**一度も検査していなかった**。ただし24枚を緩い正規表現で走査しても漏洩は 0件＝実害は出ていない |

### 採点への影響

| | 修正前 | 修正後 |
|---|---|---|
| arm0 stop | 10 | **6**（R10a 4 ＋ A23紙側 2） |
| arm1 stop | 4 | **4**（R10a 4） |
| arm2 stop | 7 | **7**（R10a 5 ＋ A23紙側 2） |
| A16（全アーム） | 2 | **0** |
| R10b（全アーム） | 2 | **0** |
| `EXPR_TABLE_MISS` | 2 | **0** |
| 生成前の決定表 | ― | **不変**（`R10a_NO_REPRODUCE` の件を除く） |

**アーム間の差の実体が変わった。**「Arm 1 が A16 を消した」のではない。A16 は物差しの誤りだった。
アーム差に残るのは **A23 の紙側（申告はするが本文に書かない）ただ一つ**である。

引き継ぎ書 §3 の

```
欄だけ            → 申告する。本文には書かないことがある
欄 ＋ 枚ごとの仕様  → 本文にも書く
```

は**生き残った。むしろ唯一の根拠になった。** 一方で「stop 10→4」という**幅は誇張**だった（正しくは 6→4）。

---

## 2. 直していないもの（モデルの意味の決定なので人間へ回す）

### G1 `check_seat_words` が座席の由来を見ていない ── **採点が動く**

SPEC §10.1 は「**Form が閉じているのは制度由来の座席だけ**」と言っている。
`check_chain` の `R18_BYPASSED_SEAT` は正しく `origin == "制度"` に限定しているのに、
第12版で足した紙側 `check_seat_words` は**全座席に様式語の一致を要求している**。

発火した4件は**すべて非制度座席**（arm0 R2-P1 店長・社長＝個人／arm2 E1-P1 入試広報課長＝組織・E2-P2 教務主任＝個人）。
中身を見ると言い換えである：

- R2-P1 ⑥「店長には**人時**のまま作り直し480〜700人時の削減」← 様式語は「作業時間」「廃棄率」
- E1-P1 ⑥「**出願**…」← 様式語は「出願件数」「広報予算執行」

制度座席（学部長会）は 3語すべて本文に在り、通っている。

**これを直すと 4 / 4 / 5 になり、arm0 と arm1 の差が消える。**
＝ 第12版の結論「⑥の枚仕様に書き下ろすと効く」の**唯一残った根拠が無くなる**。
だから機械的に直さない。**Form の閉性を非制度座席にも課すのか**が問われている。

### G2 生成後検査が商材座標を使っていない ── 潜在（0/24）

`check_declared` と `check_chain` はグローバルの `EXPR_OK`（＝`EXPR_OK_CALIBRATED`）を引く。
第10版で入れた `expr_ok_of(商材座標)` は `check_tau`（生成前）でしか使われていない。
`validate_copy` は `prod` も `industry` も引数に持たないので、**生成後検査には較正の降格（`apply_calibration`）も一度も掛からない**。

現に表が食い違うセルがある：

| | α=(測定,統制) | 硬さ | 実務性から通る先（座標） | （較正表） |
|---|---|---|---|---|
| R1-P1 | (高,高) | 変動 | **価格・実務性** | 実務性 |
| R2-P1 | (高,高) | 変動 | **価格・実務性** | 実務性 |

24件の採点は動かない（0/24）。ただし「実務性↔価格が開く」は第10版の中心的な主張なので、
生成後検査がそれを見ていないのは設計の食い違いである。

### G3 `check_insult` が ∃χ∈Γ^own を検査していない ── 潜在

```python
hit = [k for k, v in gamma_own.items() if d in v or v in d] if gamma_own else []
f.append(Finding("R17_DENIES_OWN", "stop", ...))   # ← hit を使っていない
```

`hit` を計算して**捨てている**。`s5_denies_own` が空でなければ、**Γ^own に無い文字列でも、Γ^own が空でも停止する**。
SPEC §1.1.1 は `insult(φ) ⟺ ∃χ ∈ Γ^own. …` と書いているので、Γ^own = ∅ なら侮辱は成立しない。
E2-P1・R2-P1 は `gamma_own = {}` なので、この2セルは「何を申告しても停止」する状態にある。
24体はすべて `s5_denies_own = ""` を返したので未発火＝潜在。

引き継ぎ書 §4-1 が名指しした `d in v or v in d` は、**判定には効いていない**（ref の飾りだけ）。
本当の穴は部分一致ではなく、**∃ が実装されていないこと**だった。

### G4 `Nu.A` と `prod.nu` の二重の真実 ── **生成前に実害あり**

SPEC §12.2 は「ν は旧 A そのもの」と書いたが、`Nu.A` は残ったまま `Product.nu` が足された。照合はどこにも無い。

| | `Nu.A` | `prod.nu` | |
|---|---|---|---|
| R1-P1 | 使っても分からない | **使えば分かる** | ★食い違い |
| R2-P1 | 使っても分からない | **使えば分かる** | ★食い違い |

`blocks_on` は `Nu.A` を引くので、この2セルは
「仕組みの開示」「認証・お墨付き」が点灯し、「**数字入りの導入事例**」が落ちている。
`prod.nu` を正とするなら、点灯すべきブロックが逆である。

---

## 3. 潜在（実データでは未発生。25業界へ広げる前に閉じておくもの）

| 箇所 | 内容 |
|---|---|
| `check_realize` | 三つ組の**〈いつ〉が一度も検査されていない**。`"昨年度中"` `"来期"` `"2027/4/1"` どれも通る（空文字だけ落ちる）。N₃ の第2成分に検査対象が無い |
| `check_dates_v7` | `s6_start_date > deadline` が**文字列比較**。24体は全部 ISO なので現状は正しいが、`"2027-4-1"`（ゼロ詰めなし）で誤判定する。書式の検証も無い |
| `iso_cases` | 構造キーの照合が**自由文の完全一致**。句読点1つ・助詞1つ・空白1つで事例が落ち、`D7a` が立たなくなる。第10版の「16/16 → 0/8」は、`cells8_v10.py` が買い手側と事例側を**同じファイルで同じ文字列に書いた**から成立している。営業が入力すれば一致しない |
| `check_realize` | A24 の連結診断は `・、,／/＋+` しか割らない。「媒体費**と**広報外注費」「媒体費**および**…」は `R13_ACCOUNT_NOT_HELD`（＝存在しない費目）と誤診断される |
| `START` / `ACQUIRE` / `cost_vector` | 表引きが**黙って既定値へ落ちる**。`START.get("比較検討中 ")` → 0（＝Σ が①〜⑥に変わる）、`ACQUIRE.get("買い手のデータ")` → 0（＝実効LTが縮み R6b が緩む）、`sigma_p="高い"` → 既定1。N₂ の「未定義は値ではない」が表引きに適用されていない |
| `check_realize` / `check_seat_words` | 実行者名・費目名・座席名が**完全一致**。24体では不一致 0件だが、生成器は現に `s2_unit` に説明句を付けてきた（A25）。同じ生成器が名前に説明句を付けない保証は無い |
| `check_v0` | `V0` にあって生成器の `BAN` に渡していない語が4つ（`消去対象` `warrant` `承認を積` `資本として`）。R10a と同じ「指示と検査の文言のずれ」 |

---

## 4. 型としての整理

見つかった穴は3つの型に落ちる。

**型1：指示が複数形を誘うのに欄が単数**（A24 費目 → A25 単位 → **A25c 基準**）
　処置は毎回同じ。宣言の型が緩いなら照合の側が正規化し、同時に**欄を割る**。

**型2：照合できないことを「無い」と読む**（1文字の単位 → 必ず停止／自由文の完全一致 → 事例が消える／
　表引きの既定値 → 黙って別の値になる）
　N₂ が言っているのはこれである。`⊥` は `X` のいかなる値とも比較できない。**未定義は要判断へ回す。**

**型3：規則と実装の適用範囲がずれる**（Form の閉性が制度座席だけなのに全座席へ／
　`∃χ∈Γ^own` を検査していない／商材座標が生成後に届かない／`Nu.A` と `prod.nu` の二重の真実）

そして 0.1 は**第4の所在（検査）ですらなく、配管**である ── 決定表がコードより古い。
`stamp.py` を実際に使うところまでは行っていない。
