# 引き継ぎ書 ── 営業資料生成モデル 第12.5版

第12.2版の引き継ぎ書を差し替えます。**A26・A27・A28 を実装し、モデル層に残っていた残余4件と
配管の刻み漏れを閉じた時点**の版です。数字が第12版から動いているので、**古いほうの数字を引かないでください**（→ §3）。

版：git tag `v12.5`／`sales_logic.py` sha256[:12] = `f46e6fb20d8f`／回帰 **173項目 全通過**

> **A26・A27・A28 はコードに入っただけで、効果は一度も測っていません。**
> 次の一手は Arm 1 の8体を走らせることです（→ §4）。予測は先に置いてあります。

---

## 0. 最初の15分でやること

**ダウンロードは要りません。すべてプロジェクトの中にあります。**

```
# 1) コードを復元する ── プロジェクトの4枚を project_read して展開
claude/コード束1-中核.md        sales_logic.py
claude/コード束2-回帰と辞書.md   test_sales_logic.py / messages.json
claude/コード束3-入力と指示.md   cells8_v10.py / prompts8_v10.py / prompts8_v11.py
claude/コード束4-走行と採点.md   wf_v11.js / validate8_v11.py / validate8_v12.py / regen_v12.py /
                                wf_v10.js / validate8_v2.py / report8_v10.py / stamp.py
```

各枚の冒頭に復元スクリプトが載っています（`===== FILE: <名前> =====` 区切り）。

```
# 2) 走行データを復元する ── 再走行しないと作れないもの
claude/data-run8-v11.json           → run8_v11.json          第12版アーム実験の生成物24件
claude/data-run8-v10.json           → run8_v10.json          第10版の走行
claude/data-prompts8-v10-asrun.json → prompts8_v10_ASRUN.json 第10版で実際に渡した指示文
claude/data-decisions8-v10.json     → decisions8_v10.json    決定表（※ as-run。§6の罠）
claude/data-run8-v12-arm1.json      → run8_v12_arm1.json     第12.2版 Arm 1 再走行の生成物8件
claude/data-decisions8-v12.json     → decisions8_v12.json    いまのコードで再計算した決定表

# 3) 動くことを確かめる（ここが通らなければ、以降の数字は信用しない）
python3 test_sales_logic.py | tail -3        # → 173項目 すべて通過
python3 validate8_v11.py run8_v11.json       # → arm0 stop6 / arm1 stop4 / arm2 stop7
python3 validate8_v12.py                     # → Arm 1 再走行：stop2 / pass 5-8 / R10a 2

# 4) git を立てる
git init -q && git add -A && git commit -m "第12.5版 基準点" && git tag v12.5
```

**この3つが再現しなければ、まず環境を疑ってください。**

**復元されないもの**：`cells8_v2/v6/v6b/v7/v8/v9.py`（回帰テストが import します）、`SPEC8.md`
（＝`claude/モデルv11-仕様.md` から復元可）、過去の走行レポート HTML。
`ModuleNotFoundError` が出たら、その版のセル定義が無いということです。**どれが落ちたかを記録したうえで**進めてください。

---

## 1. これは何か

売り手が複数の商材を複数業界へ売り込むための、営業資料の生成システム（Django ＋ LLM を想定）。
中核は「文章を書く機械」ではなく、**「この買い手のこの状況では、何を書けば通り、何を書くと落ちるか」を
機械的に確定させる装置**です。日本語を書くのは最後の1工程だけ。

7層構造。`claude/モデル-流れの図解.html` に全体図があります（**まずこれを読むのが速い**）。

```
入力ν → ①公理 → ②軸 → ③形式論理 → ④Python(compile_deal) → ⑤プロンプト → ⑥LLM → ⑦検査(validate_copy)
        └────────── モデル ──────────┘   └─生成の指示─┘  └LLM┘  └─検査─┘
```

**方法の出典。** 生成モデル（①〜⑥）も、マルチエージェント（買い手）による検証・修正のプロセスも、
**Timmermans & Tavory の abductive analysis を援用しています**（→ §5）。
①〜⑥はパースの探究過程に対応し、「予測を先に置く」「3役を分ける」「アノマリー」という語は
すべてそこから来ています。**第12版までの引き継ぎ書はこれを書いていませんでした。**

---

## 2. モデルの現在地（第12.5版）

**3原理**：Π₁ 蓄積律／Π₂ 通関律（可能 conv ∧ 執行 ∃act）／Π₃ 所在律。
**5記法**：N₁ 承認演算子／N₂ 単位元と未定義／N₃ 行為の三つ組／N₄ 単位付き量／N₅ 座席の型。

| | |
|---|---|
| `sales_logic.py` | 検査関数26本・標準ライブラリのみ |
| `test_sales_logic.py` | **173項目 全通過**（97 → 132 → 140 → 147 → 158 → 173） |
| アノマリー | A1〜A25（＋A25b・A25c）／**A26・A27・A28 は実装済み・効果は未測定** |
| 仕様書 | `SPEC8.md`（第11版まで。**A24・A25・A25b・A25c は未記載＝債務**） |

### 第12.1版で入れたもの（すべて**検査側**の修正。モデルは変えていない）

※ **第12.2版はコードを一行も変えていません。**走行と測定だけです（→ §3・§4）。


- **A25b** `unit_tokens` に「の」区切り／候補が空なら要判断へ（1文字の単位は必ず停止していた）／
  ⑥が Σ に無ければ検査対象外／`R10b` の ABSENT と REPLACED の二重発火をやめた
- **A25c** 連結された基準名（`s6_kappa="価格・財源"`）を照合側で正規化。`check_v0` の `\b` を ASCII 境界へ
- **G4** `nu_of()`：`Nu.A` と `prod.nu` の二重の真実。商材座標を正とする（SPEC §12.2）
- **G2** `validate_copy` が `prod` / `industry` を受け取る。第10版の生成子が生成前にしか届いていなかった
- **G3** `check_insult` に `∃χ∈Γ^own` を実装
- **潜在** N₃ の〈いつ〉を検査／日付を暦順で比較／`iso_cases` を正規化し全滅を申し送る／
  表引きの定義域を N₂ で検める（`check_axis_values`）

詳細と根拠は `claude/第12.1版-物差しの検分.md`。

### 第12.3〜12.4版で入れたもの（**仕様の側の矛盾**を三つ解いた）

A26・A27・A28 は形が同じです。**両立しない二つの要求が仕様に入っていて、優先順位が書かれておらず、
生成器が毎回それを自分で決めていた。**

- **A26** R10a の比較対象を〈課金周期〉→〈提案後に問題が残る周期〉へ差し替え（`s6_residual_period_months`）。
  **指示文は変えていない** ── ずれていた二つのうち**正しかったのは指示のほう**でした。
  `R10a_CHARGE_PERIODIC`（註記）で、旧 R10a なら止まっていた形＝偽陽性の正体を数えられるようにしてあります。
- **A27** ⑥の必須要素（**導出**）と字数上限（**較正**）の優先順位を規定 ── **要素は必須・字数は目安**。
  これは選択ではなく、較正台帳の規律（〈導出 > 較正〉）の適用です。落ちを隠させない `s6_omitted_blocks` を新設。
  あわせて**⑥字数を採点の根拠から外しました**（一度も検査対象でなく、同じ指示で 450〜1506 に振れたため）。
- **A28** ⑥に置く量の出所を要求（`s6_quantity_sources`）。**試算は禁じません**
  （禁じると A23「座席の数だけ量を置け」と衝突し、実績を持たない座席で生成不能になる）。
  代わりに、試算なら試算と分かるように書かせ、確定できないものは **`s6_to_sales` で営業へ回させます**。
  **機械は決められないものを `needs_judgment` へ回せるのに、生成器にはその出口が無かった。**

### 第12.5版で入れたもの（**モデル層**の残余4件＋配管）

- **`C_move` → ⟨ν, θ, σp⟩** 合成規則は要らなかった。`C_move` が駆動するブロックは `B_trial` と
  `B_migration` の2つだけで、**その2つがちょうど半分ずつに対応している**。合成された値を使う場所が無い。
  8セルの点灯は不変。食い違いは `C_MOVE_AXIS_CONFLICT` で申し送る（`NU_AXIS_CONFLICT` と同じ形）。
- **`S1`/`S2`/`S3`** SPEC §12.2 の「削除した」は誤り。`sigma_prod` が現に引いていた（参照1箇所・8セルで
  発火しないので消えたように見えていただけ）。**根拠の無い表は捨てずに較正台帳へ載せる**のが規律なので、
  較正表として登録し、未較正の業界では縮退させない。
- **cost 単調性（Π₁ 第2式）** **探していた対象が存在しなかった。**第2式は第1式の系です（Γ が単調に積むなら
  上限は自動的に非減少）。第10版が対象を与えたのは `cost(Mᵢ)`＝対抗手段の費用であって、Π₁ の `cost(φ)`＝
  承認の値段ではありません ── **同名の別関数を二版にわたって二度取り違えていました**。
  実質は〈**Σ は段の接尾辞である**〉（高い段を承認済みにして安い段を残すことはできない）。これは検査でき、
  **唯一これに引っかかるのが `sigma_prod` の {①,④,⑥}** でした。
- **`START`** 較正台帳が Σ に掛かっていなかった。`apply_calibration` は findings しか触らず、
  Σ の縮退は素通りで申し送りが出るだけでした。縮退は「その段は資料の外で承認済み」という**断定**なので、
  未較正の業界ではしません。**安全な向きに倒れます**（段が増えるだけ・A10 の停止も出なくなる）。
- **配管** `stamp()` は呼んだときだけ刻むので `cells8_v10.run()` を直接叩くと刻まれませんでした。
  **書き出す側で刻み**（`stamp.dump_stamped`）、読む側は `stamp.load()` で受け、
  指示文を組む前に **`assert_fresh()`** で決定表とコードの版を突き合わせます。
  **罠の本体は「刻まれていない」ことではなく「古い表が指示文へ入ったのに誰も気づかない」ことでした。**

詳細と根拠は `claude/第12.3版-A26A27実装.md`・`claude/第12.4版-A28設計.md`・`claude/第12.5版-モデル残余.md`。

---

## 3. 確定していること と、疑わしいこと

**ここがこの引き継ぎ書でいちばん大事な節です。**

### ⚠ 第12版の報告のうち、訂正されたもの（重いものが3件）

**(1) R10a の指示文は、24体すべてに一度も渡っていなかった。→ §4-2 で測り直し済み。**

第12版レポート §2④ の「R10a はどのアームでも動かない ── **提示の問題ではない**」は**誤りでした**。
`fire_rules` に足した `R10a_NO_REPRODUCE` が `decisions8_v10.json`（第11版で再計算されたまま）に入っておらず、
`prompts8_v11.py` はその古い決定表を読むので、指示文に一度も載りませんでした（0/8・全アーム）。
着手期限（R12b）は `build()` が直接読むので 6/8 で渡っています。落ちたのは rules 経由の一本だけ。

**その一行を入れて Arm 1 の8体を再走行した結果（第12.2版）：`R10a` 4件 → 2件。提示の問題でした。**
詳細は `claude/第12.2版-R10a再測定.md`。要点だけ：

- 消えた4件は**回避ではない**。申告が商材の課金構造に揃っただけ
  （広告・販促支援＝3〜12か月の役務 → `s6_period=0` 4/4／IT ＝ SaaS → `s6_period=12` 4/4。
  前回は同じ広告商材で `0 / 1 / 12 / 1` とばらばらだった）
- **8体中6体が、課金周期ではなく〈提案後に問題が残るか〉で答えている。機械だけが課金周期を見ている**
- E2-P1 の自己申告：「判定基準が規定されていないため、**納品物が校内に残ることをもって満たしたとみなした**」
  ── **生成器が自分で A26 の判定基準を発明している**
- **「この商材では充足不能かもしれない」も否定された。** 道は2本あった
  （前回 R1-P1 は `s4=6 / s6=12` で満たし、今回は4件が `s6=0` で対象外になった）
- **指示と検査のずれについては、正しかったのは指示のほうだった。** 直すのは検査

**(1b) ⑥の字数は、同じ指示でも走行間に 450〜1506 で振れる。**

第12版の「Arm 1 は必要な3セルだけが自分で上限を破った」という読みは**再現しませんでした**（再走行では6セル、
平均 560 → 853）。指示文の差が R10a の一行だけのセルでも `449 → 895`・`448 → 1506` になっています。
**方向は再現**（Arm 0 は8セルすべてが 447〜450 に貼り付き、Arm 1 は散らばって超える）。
**この振れ幅では、アーム間の字数の差はアームの差と区別できません。**

**(2) アームの採点が動いた（§4-1 の物差しの検分による）。**

| | 第12版の報告 | 第12.1版 |
|---|---|---|
| arm0 stop | 10 | **6** |
| arm1 stop | 4 | **4** |
| arm2 stop | 7 | **7** |
| `A16_NOT_CONV_AT_SEAT` | 2 | **0** |
| `R10b` | 2 | **0** |

arm0 の A16 2件は「⑥の量が中間座席で読めない」ではなく、**基準名の連結**（`s6_kappa="価格・財源"`）でした。
A24（費目の連結）・A25（単位の説明句）と同じ型の第3例です。
R10b 2件は A25 の取りこぼし1件を、ABSENT と REPLACED で2回数えていたものでした。

### 確定している（実測が複数回ある）

- **R9（形式系の語彙が本文へ漏れる）は 6走行連続で 0/36。** ただし `\bD[1-7]\b` は日本語隣接で効いておらず、
  **D記号については検査が走っていなかった**（第12.1版で修正済み。実本文への漏洩は緩い正規表現でも 0件）。
- **κ の階数と隣接則で、手書き25マス中24マス再現。** 外れは 説明可能性→価格 の1マスのみ。
- **λ×ε の2座標で、効かない証拠 12/12 を説明できる。**
- **出力スキーマの欄そのものが指示として働く**（Arm 0：座席申告が予測 0/8 に対し実測 8/8）。
- **枠を広げても要求は満たされない**（Arm 2：450→900字で stop 4→7 と悪化）。

### 疑わしい／まだ測っていない

- **⚠ 仕様を満たした資料が、実際に買い手を通るかは一度も測っていない。**（変わらず最重要）
- **⚠ アーム差 6/4/7 は、いま `A23_SEAT_WORD_ABSENT` **1本だけ**に乗っている。**
  しかもその検査は `check_seat_words` が**座席の由来を見ていない**ために立っている。
  SPEC §10.1 は「Form が閉じているのは制度由来だけ」と言っており、発火4件はすべて非制度座席。
  **制度座席に畳むと 4/4/5 になり、アーム差が消える。**
  ユーザー判断で**全座席のまま据え置き**、較正表と同じ「業界依存の疑いが残る」扱いにしてある。
  → 第2段（盲検買い手）は、Arm の選定と同時に**この検査の妥当性**も測ることになる。
- **較正台帳の `START` は唯一残る業界依存表**（＋上記の `check_seat_words`）。
- SPEC8.md §6 の X1〜X6 は据え置き。

### 第12.2版で立った3つ（**いずれも第12.3〜12.4版で実装済み。効果は未測定**）

- **A26（確定・実装待ち）** ── R10a は**測っている対象が違う**。課金周期ではなく〈提案後に④の問題が残る周期〉。
  現行は**両方向に誤る**：単発と申告すれば `R10a_NOT_PERIODIC`（info）で比較対象外になり問題の再発を一切見ない（偽陰性）、
  逆に本文で「翌年以降に同じ手数を積み直しません」と明示していても課金が年額なら停止する（偽陽性）。

  ```
  s6_residual_period_months : ④で問題化した事象が、本提案の実施後も再発する周期（月）。
                              仕組み・様式・型が買い手側に残って再発しなくなるなら 0。

  R10a: s4_declares_repetition ∧ s6_residual_period_months ≠ 0
        ∧ s6_residual_period_months ≤ s4_period_months  →  停止
  ```

  `s6_period_months`（課金周期）は R14 で使うので**残す**。R10a から外すだけ。**指示文は変えない。**
  ⑥の枚仕様に一行足す：〈本提案の実施後、④の事象が再発するか。しないなら、何が買い手側に残るからしないのかを書く〉。
- **A27（候補）** ── 提示仕様に**両立しない二つの要求**（⑥の必須要素の集合と字数上限）が入っていて、
  優先順位が規定されていない。**8体中5体が訴え、3体が名指し**。生成器は毎回同じ解き方（要素を優先し字数を破る）を
  するが、仕様はそれを書いていない。Σ を増やす（⑥を2枚に割る）のは Π₁ の階段から導出されないので採れない。
  取れる手は〈優先順位を明記する〉か〈必須要素の集合を減らす〉。
- **A28（候補）** ── ⑥は「読む座席の数だけ量を置け」と要求するが、**その量の出所は要求していない**。
  「根拠のない量を置いてよいのか、置かずに座席要件を欠くべきか」（R2-P1）。**8体中5体が同じ訴え**。
  **A22（適用対象に出所を要求する）とまったく同型の穴**が、τ ではなく⑥の量の側に開いている。

---

## 4. 次の手（この順に）

0. ~~**Arm 1 の8体を再走行して R10a を測り直す**~~ **済（第12.2版）。** 4件→2件。`wf_89236a3f-655`／36分37秒／50.6万トークン。
   → `claude/第12.2版-R10a再測定.md`
1. ~~**A26 を実装する**~~ **済（第12.3版）。** R10a の比較対象を課金周期→残存周期へ。指示文は変えていない。
2. ~~**A27 を決める**~~ **済（第12.3版）。** 〈導出 > 較正〉で決着 ── **要素は必須・字数は目安**。⑥字数は採点根拠から外した。
   → `claude/第12.3版-A26A27実装.md`
3. ~~**A28 を検討する**~~ **済（第12.4版）。** 試算は禁じず、確定できない数字は **`s6_to_sales` で営業へ回す**。
   → `claude/第12.4版-A28設計.md`
4. ~~**モデル層の残余4件**~~ **済（第12.5版）。** `C_move`／`S1,S2,S3`／cost 単調性／`START`。
   **4件のうち2件は問いのほうが間違っていた。** → `claude/第12.5版-モデル残余.md`

### いまここ

5. **Arm 1 の8体を走らせて A26・A27・A28 を測る。** ← **次の一手**
   予測は `predict_v12_3.md`（A26・A27）と `claude/第12.4版-A28設計.md` §6（A28）に**先に置いてあります**。
   指示文に届いていることは走行前に確認済み（arm0/1/2 とも 8/8）。約37分・50万トークン。
   入力は `prompts8_v12_arm1.json`、1体1ファイル（`run12/in_*.json` → `out_*.json`）。
6. **結果を見て A27 の次の手を決める。** `A27_BLOCK_OMITTED` が複数出たら、⑥を〈本文〉と〈運搬の道具〉に
   分ける根拠になる（第12.3版で採らずに残した案）。出なければ優先順位の規定だけで足りた。
7. **Arm 1 を確定させる** → `prompts8_v12.py`。あわせて**⑥の申告欄を割る**（型1 の処置。A24 と同じ）。
8. **第2段を走らせる。** Arm 1 の生成物に予測者と盲検買い手（16体）。**ここが本丸。**
   ただし**これは帰納ではない**（→ §5 の留保）。
9. **閲覧ログとモデルを接続する。** カスタム資料に軸の値をメタデータで持たせる。
   **これがないと演繹→帰納が閉じない。** `claude/営業ウェブアプリ要求.md` 第9章。

その先：25業界への拡大、較正台帳の `START` を実際に埋める（第12.5版で「未較正では縮退させない」と決めたので、
**縮退させたい業界は較正する必要がある**）、Django 統合。

**保留中：** モデル（公理・命題・軸・論理式）をより抽象的に整理し直す件（界面被覆グリッド／Carlile／
Fama & Jensen／Perelman／記帳の理論）。ユーザ判断で一旦保留。アノマリー数の増加が動機でした。

---

## 5. 方法についての約束事（破らないこと）

**この節は思いつきの作法ではありません。第12版までの引き継ぎ書に書いていなかったのは欠落です。**

生成モデル（①〜⑥）と、マルチエージェント（買い手）による検証・修正のプロセスは、
**Timmermans & Tavory の abductive analysis を援用しています。**

> Timmermans, S. & Tavory, I. (2012)「Theory Construction in Qualitative Research: From Grounded Theory to
> Abductive Analysis」*Sociological Theory* 30(3), 167–186
> Tavory, I. & Timmermans, S. (2014)『Abductive Analysis: Theorizing Qualitative Research』University of Chicago Press
> Timmermans, S. & Tavory, I. (2022)『Data Analysis in Qualitative Research: Theorizing with Abductive Analysis』同（2014年版の実務編）

全文の検討は **`claude/abductive-analysis組み込み.md`**（338行）にあります。要点だけ：

- **①〜⑥はパースの探究過程である。**① habit の措定／② breach＝surprise の生成（defamiliarization）／
  ③ casing／④ index による時間的係留／⑤ 仮説空間の絞り込み／⑥ 買い手が引く abductive conclusion。
  独立に組み立てた段の並びが、パースの探究過程と一致していました。だから T&T の技法がそのまま段の手続きになります。
- **「否定性は注入されてはならない」＝〈novelty 型を禁じ、anomaly 型のみを許す〉。**②で許されるのは
  ①の事実の再記述だけ（別の単位で数え直す／別の形式で書き出す）。これが「内在的否定」の工学的定義です。
- **驚きは関係的である。**「明示された理論的期待に対する不一致としてのみ成立する」。
  ここから下の「予測を先に置く」と「3役を分ける」が導かれます。

以下の約束事は、それぞれ T&T のどこから来ているかを併記します。

- **Python には、論理式から機械的に導かれることだけを置く。**
- **形式系の語彙を生成器に渡さない。**（`BAN`。ただし `V0` に在って `BAN` に無い語が4つある＝未整理）
- **予測を先に置く。** 第12.1版は `predict_v12_1.md` に置いてから直し、**完全に的中した**。
  ← T&T：驚きはデータに内在しない。**期待を書かなければ「破綻」は〈真のアノマリー／未踏／誤用〉の混ざった袋になる。**
- **生成器・独立予測器・盲検買い手の3役を分ける。**
  ← T&T：突合してアノマリーだけを次へ送るための分離。第1版で140件中76件を却下したのは、これをしていなかったため。
- **突合は配列のインデックスで行う。**
- **アノマリーを立てる前に「この欄は誰かが既に測っていないか」を先に引く。**
  ← T&T：**informed theoretical agnosticism**。理論なしでも単一理論でもなく、複数の伝統に通じつつどれにも与しない。
  文献との対話は研究の最後ではなく**最初から全段階で**行う。
- **業界固有のバイアスを他業界へ伝播させない。**
  ← T&T：**alternative casing**。一つの事例化に固着しないための規律。導出表と較正表を分けるのはその実装。
- **枚の集合 Σ は Π₁ の階段から導出される。**
- **【追加】決定表を作ったら必ず版を刻む。** `stamp.py` はあるのに使っておらず、それが §3(1) を生んだ。
  `regen_v12.py` は刻むようにしてある。
- **【追加】照合できないことを「無い」と読まない。** 空集合・未知の鍵・読めない日付は、
  停止でも通過でもなく**要判断**へ回す（N₂ を表引きと文字列照合へ適用する）。
- **停止規則はまだ採っていない。** T&T はグラウンデッド・セオリーの saturation を拒否します。
  アノマリーが出なくなることは「理論が正しくなった」ではなく
  **「この驚きは汲み尽くした。次は驚きを探す場所を変えろ」**を意味します。採るかどうかは未決。

### ★ 方法論上いちばん重い留保 ―― 我々のデータは、データではない

**これを落とすと「25業種で検証した」が「25業種で確認した」に化けます。**

| | T&T | 本プロジェクト |
|---|---|---|
| データの出所 | 実在の人間のフィールド観察 | LLM エージェントによる買い手のシミュレーション |
| 「驚き」の対象 | 世界についての驚き | **LLM の事前分布についての驚きでありうる** |
| T&T の用語では | data | **theoretically-informed conjecture**（理論に基づく推測） |
| 使える用途 | アブダクションの材料 ＋ 帰納の場 | **材料としては使える。帰納の場にはならない** |

走行レポートの「本レポートは思考実験であり実顧客の反応の実測ではない」という注記は、
礼儀ではなく**方法論上の必須条件**です。

**そしてこれは §4 の4手にそのまま効きます。**「第2段（盲検買い手16体）が本丸」と書いてきましたが、
**それも帰納ではありません。**盲検買い手は依然として LLM です。第2段が測れるのは
「仕様を満たした資料が、**モデルの想定する買い手像**を通るか」であって、実在の買い手を通るかではない。

**帰納の場は、このプロジェクトのもう半分にあります**——`claude/営業ウェブアプリ要求.md` の
**ページ単位の閲覧ログ**です。モデルは「どのページがどれだけ読まれるか」を予測しており、
アプリはそれを実測します。**この二つはまだ接続されていません。**
演繹（モデル → 検証可能な帰結）と帰納（閲覧ログ）を閉じて初めて、
パース後期の三段構え（アブダクション → 演繹 → 帰納）が一周します。

---

## 6. ファイルの地図と、罠

### 現役

| | |
|---|---|
| `sales_logic.py` | 決定的コア。`compile_deal()`（生成前）と `validate_copy()`（生成後） |
| `test_sales_logic.py` | **132項目**。各アノマリーが買い手の逐語と対応づけてある |
| `messages.json` | 表示用の日本語 |
| `cells8_v10.py` | 8セルの入力ν |
| `prompts8_v11.py` | アーム3本を生成 |
| `regen_v12.py` | **【新】決定表と指示文を作り直して差分だけ見る**（走らせない・版を刻む） |
| `validate8_v12.py` | **【新】Arm 1 再走行の採点。**採点入力の同一性を先に確認してから比べる |
| `run12/in_*.json` ／ `out_*.json` | **【新】1体1ファイル。**取り違えを構造的に潰した（8体とも取り違え0） |
| `validate8_v11.py` | アーム別採点＋`PREDICT` との突合。**商材座標を渡すようにした** |

### 罠（知らないと必ず引っかかる）

- **版の刻みは書き出し側で掛かる（第12.5版）。** `cells8_v10.run()` は自分で刻みます。
  読むときは **`stamp.load(path)`** を使ってください（刻みの有無を吸収します）。
  素の `json.load` で読むと `{"_stamp":…, "data":[…]}` の外側が来ます。
  指示文を組む前には **`stamp.assert_fresh(決定表)`** を掛けること ── 刻んでも照合しなければ気づけません。
- **`decisions8_v10.json` は as-run（アーム実験に実際に使われた決定表）。上書きしないこと。**
  いまのコードで計算し直したものは **`decisions8_v12.json`**、指示文は **`prompts8_v12_arm*.json`**。
  差分は2種類だけ（R10a の一行／R1-P1・R2-P1 のブロック）。
- **`validate8_v11.py` の `PREDICT` は第12版の走行時の予測**です。第12.1版で採点が動いたので、
  いま走らせると「外れ」が並びます。**予測の記録として残してあるので書き換えないこと。**
  第12.1版の予測は `predict_v12_1.md`。
- **`verified8_v11_RAW.json` は A25 修正前の採点**、`verified8_v11.json` が修正後（いまは第12.1版の物差し）。
- **第12.2版の再走行は `run8_v12_arm1.json`（版を刻んである）／採点は `verified8_v12_arm1.json`。**
  `run8_v11.json` の arm==1 と混ぜないこと。前者は R10a の一行**あり**、後者は**なし**の指示文で生成されている。
- **`validate8_v11.py` の `PREDICT` も `validate8_v12.py` の `PREDICT` も、走行時の予測の記録**です。書き換えないこと。
- テストの「W と受領記録を入れれば第7版の検査はすべて通る」は **A20 系を除外して**判定しています（意図的）。
- `report8_v*.py` は HTML レポート生成器。第12版用は未作成。

---

## 7. 過去に踏んだ地雷（同じ穴に落ちないため）

| | |
|---|---|
| 数式の文字化け | 原因は丸数字ではなく**複数行 `$...$`** |
| Workflow の `args` | 文字列として届く。**エージェントに `Read` させる**方式へ |
| **型の混同（3回）** | A24 費目の連結／A25 単位の説明句／**A25c 基準の連結**。共通は「**指示が複数形を誘うのに欄が単数**」。処置も毎回同じ＝照合側で正規化し、欄を割る |
| **二重の真実（2回）** | `Nu.A` と `prod.nu`（G4）／`C_move` と ⟨θ,σp⟩。座標を足すときに**旧軸を消し忘れ、古い側が黙って引かれる**。処置＝新しい側を正とし、食い違いを申し送る |
| **同名の別関数（2版）** | Π₁ の `cost(φ)`＝承認の値段と、A19 の `cost(Mᵢ)`＝対抗手段の費用。第10版 §10.4 と §12.3 で二度取り違え、「閉じた」と誤記した |
| **「削除した」と書いて削除していない** | `S1`/`S2`/`S3`。参照が1箇所で8セルでは発火しないので**消えたように見えていた**。SPEC の記述と実装が離れるときは、まず**発火しているか**を数える |
| **規律を作った層にしか掛けない** | 較正台帳を作ったとき、降格の対象を findings だけだと思っていた。**表が値を決めている場所すべて**に掛ける（`START` は「何枚書くか」を決めていた） |
| 0 の二義性 | `period_months=0` を「反復しない」と「即座に反復」の両方に使っていた |
| 規則の向きの反転 | R16 が `⊬` になっていた |
| 恒偽の前件 | R1 の前件が値域の変更に追従していなかった |
| 降格が元を落とす | `check_applies` の戻り値を一律 `continue` していた |
| 同じ規則の二度発見 | R7（第5版）と R16（第7版）は同じ規則だった |
| **決定表がコードより古い** | 第12版で足した規則が指示文に一度も載らず、「効かない」と誤って結論した。**stamp.py を使う** |
| **計算して捨てる** | `check_insult` は `hit` を計算して使っていなかった（∃ が未実装だった） |
| **`\b` は日本語で境界にならない** | Python の `\w` は日本語を含む。ASCII 境界で書く |

---

## 8. プロジェクト文書（読む順）

1. `claude/モデル-流れの図解.html` ── **全体像。まずこれ**（METHOD／NOW／OPEN の3節が第12.1版で新設）
2. `claude/abductive-analysis組み込み.md` ── **方法の出典。§5 の約束事はここから出ている**
3. `claude/第12.5版-モデル残余.md` ── **直近。モデル層の残余4件＋配管。SPEC の訂正4件**
4. `claude/第12.4版-A28設計.md` ── ⑥の量の出所と、営業への申し送り口
5. `claude/第12.3版-A26A27実装.md` ── R10a の差し替えと、要素 vs 字数の優先順位
6. `claude/第12.2版-R10a再測定.md` ── §4-2 の走行。A26 の確定、A27・A28 の発見
7. `claude/第12.1版-物差しの検分.md` ── §4-1 の結果と、訂正2件
4. `claude/第12版-アーム実験レポート.md` ── アーム実験の結果（**stop の数字は訂正前**）
5. `claude/モデルv11-仕様.md` ── 形式系の本体（＝`SPEC8.md`）
6. `claude/第12版-アーム設計と予測.md` ── 実験の設計思想（**数字は訂正前**）
7. `claude/統合検討-既存理論と本モデル.md` ／ `claude/モデルv10-商材座標.md` ／ `claude/モデルv9-座席の由来.md`
8. `claude/営業ウェブアプリ要求.md` ／ `claude/上司説明カンペ.md` ── 事業側
   （**閲覧ログ＝帰納の場。§5 の★留保を読んでから**）

**数字が訂正前の文書**：`claude/第10版-対照実験レポート.html`、`claude/第12版-アーム設計と予測.md`、
`claude/第12版-アーム実験レポート.md`（§1 の表の stop / A16 / R10b）。

---

## 9. 運用（複数スレッドを使う場合）

- Cowork のセッションは1本ごとに独立したコンテナ。**共有されるのはプロジェクトだけ。**
- 並列スレッドは、仕事が並列になってから。§4 の残り（5〜9）は直列です。
- 走行の出力には `sales_logic.py` のハッシュを同梱すること（`stamp.py`）。**決定表にも刻むこと。**
  第12.5版から**書き出す側で自動的に刻まれます**。読むときは `stamp.load`、組む前に `stamp.assert_fresh`。
- プロジェクトへの書き込みは全チャットのプロンプトキャッシュを壊します。走行の区切りでまとめて書くこと。
