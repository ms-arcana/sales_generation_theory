# 既存理論と本モデルの統合検討

添付の生成子探索報告を、**本モデルの走行データで検算したうえで**評価する。
結論から書くと、報告の中心的な主張——**較正表は業界の関数ではなく商材座標の関数である**——は、
私自身のコードと4走行分のログが独立に支持している。ただし報告が「移植的主張」として弱く扱った1件は、
本モデルの $\Pi_3$ から**導出できる**ので、移植は要らない。

---

## 1. 自分のデータで確かめた4点

### (a) 商材軸 $I$（装置/役務）は死んでいる

```
n.I の参照回数        0
n.A の参照回数        5
n.C_move の参照回数   2
```

判定に一度も使われていない。走行結果でも分かれない（食品・建設で上申 装置1/4・役務2/4、学校法人・小売で 1/4・1/4）。
S-D ロジックの A1（財はサービス提供の流通機構である）と報告の $\pi$ 再編は、どちらも同じ結論を出す。**削除。**

### (b) $S_1, S_2, S_3$ は、モデルが自認したヒューリスティックにしか繋がっていない

```
n.S1 参照 1 ／ n.S2 参照 2 ／ n.S3 参照 1   ―― すべて σ_prod の中だけ
```

そして SPEC §6 X1 は $\sigma_{\mathrm{prod}}$ の閾値について「理論的根拠はない」と自認している。
つまり**現行の商材軸6本のうち4本（$I, S_1, S_2, S_3$）は、根拠のない縮退規則を養うためだけに存在している。**
報告の $\beta_2$（予算内外）への置換提案は、この4本を1本の観測可能量に畳む。

### (c) `ISO_KEYS` は16セル中16セルで発火した ―― 一度も判定できていない

```
食品・建設      ISO_CONTEXT_MISSING  8/8 セル   欠けた鍵 所轄庁4・系列6
学校法人・小売  ISO_CONTEXT_MISSING  8/8 セル   欠けた鍵 系列8・所轄庁4
```

**事例の同型性検査は、全走行を通じて一度も実行されていない。** 常に「属性が足りないので判定できない」で終わっている。
報告が `ISO_KEYS` を最重要標的としたのは正しく、しかも被害は推測ではなく100%の実測である。

### (d) $\beta_1$（会計分類）と $\beta_2$（予算内外）は、買い手が自分から持ち出している

第7・8版の走行から、買い手の逐語：

> 1,200万〜2,800万円は**実行予算の労務費に載せるのか、本社の機械装置として資産計上して社内賃料で配賦するのか**。どちらを想定しているのか一言も無い。経理部が最初に聞く（建設・工務部）
> 利用料は毎年、**販管費に足し算で乗る**。設備投資枠にしても**償却が販管費に乗る**（小売・バイヤー）
> **パート人件費は販管費で、粗利の下だ。**減っているのは営業利益であって粗利じゃない（小売・社長）
> 幅と空欄のまま**予算科目には立てられない**（学校法人・学部長会）

第7版で U7 として*未踏*に積んだ「$\kappa$ の下位区分（稟議の形が違う）」は、$\beta_1$ のことだった。
**未踏ではなく、会計基準（ASU 2018-15 / IFRIC / 日本基準）が明文で決めている値を聞いていなかっただけである。**

---

## 2. 対応表 ―― 何が同じで、何が新しいか

| 報告の座標 | 本モデルの既存要素 | 判定 |
|---|---|---|
| $\nu$ 検証時点（Nelson / Darby-Karni / Dulleck-Kerschbamer） | $A$（買う前に分かる／使えば分かる／使っても分からない） | **同一。**私の $A$ は Nelson-Darby-Karni の三分類そのもの。出典を書いていなかっただけ |
| $\theta$ 分割試用可能性（Rogers） | $C_{\mathrm{move}}$（すぐ試せる／大仕事） | **部分一致。**$C_{\mathrm{move}}$ は $\theta$ と $\sigma_p$（手続切替コスト）が混ざっている。分割すべき |
| $\sigma$ 切替コスト（Burnham-Frels-Mahajan） | **なし** | **新規。**A19 で `cost_to_buyer` をスカラで入れたが、Burnham は3ベクトル。$M_0$ を消す難度は $\sigma_p$ が担う |
| $\beta_1$ 会計分類 / $\beta_2$ 予算内外 | **なし**（U7 として未踏に積んでいた） | **新規かつ明文。**基準書から商談前に決まる |
| $\alpha$ 帰属可能性（Selviaridis） | **なし** | **新規。**ただし尺度未確立（報告も明記） |
| $\omega$ 発現遅れ | $S_2$（反復性）と混同していた | **要分離。**購買の反復と効果の発現は別 |
| $\pi$ PSS類型（Tukker） | $I$（装置/役務） | **$I$ を捨てて $\pi$ へ。**ただし $\beta_1$ と強従属なので1自由度に縮約 |
| cost stickiness（ABJ 2003） | $\mathrm{Acct}$ の〈硬さ〉座標（第8版 §4.4） | **既に持っていた。**ABJ は**機序と大N実測**を与える。私はデータから座標を立て、文献が理由を与えた |

$\Pi_2$ の二つの連言が、報告の二つの生成子にきれいに対応する。

$$\underbrace{\mathrm{conv}(x, \kappa_j)}_{\text{可能}} \;\leftrightarrow\; \alpha\ (\text{帰属可能性}), \qquad \underbrace{\exists\,\mathrm{act}\langle w,d,o\rangle}_{\text{執行}} \;\leftrightarrow\; \text{cost stickiness}$$

第6版で8セル全部が⑥で落ちたとき、**落ち方は二つあった**——「金額に直せない」（$\alpha$ 側）と「浮いても支払いは減らない」（stickiness 側）。
私は前者を A5、後者を A12 として二版に分けて発見した。文献はこれを最初から二つの独立変数として持っている。

---

## 3. モデルから文献へ返せること

### (a) `ISO_KEYS` の構造キーは、移植ではなく $\Pi_3$ から導出できる

報告は「構造類似がB2B説得に効く」を認知科学（Gentner 1993）とCBRからの**移植的主張**とし、B2B直接実証がないことを確定的発見としている。正しい指摘である。

**しかし本モデルでは移植が要らない。** $\Pi_3$ 所在律は既にこう言っている。

$$\mathrm{effective}(e,c) \iff \lambda(e) = \lambda(c) \wedge \varepsilon(e) = \varepsilon(c)$$

事例 $e$ が拘束 $c$ に効くのは、**同じ所在に属するときに限る**。所轄庁も系列も $\lambda$ ではない。
したがって $D_{7a}$（同型かつ実名の事例）の同型性は $\lambda$ で測られなければならない——これは $\Pi_3$ の系であって、認知科学から借りる必要がない。

さらに Cartwright & Hardie の「support factors（there で効いたものが here で効くには支援要因が要る）」は、
本モデルの $W$（執行座席）と $\mathrm{Acct}$ **そのもの**である。事例が示しているのは $\mathrm{realizable}$ の実例であり、
$\mathrm{realizable}$ は $W$ に依存するのだから、事例が移るには買い手側に同型の $W$ が要る。

$$\boxed{\;\mathrm{ISO\_KEYS} = \{\lambda(\text{拘束の所在})\text{の一致},\ W\text{の同型性},\ \tau\text{の同型性}\}\;}$$

**規制産業の語彙（所轄庁・系列）は、$\lambda$ の粗い代理変数だった。** 医療・学校・建設では $\lambda = \text{制度}$ が支配的なので所轄庁が代理として機能し、非規制B2Bでは代理が空になって検査ごと死ぬ。16/16 の発火はこれである。

### (b) 帰属可能性の尺度未確立問題に、$\Pi_2$ が形を与える

報告は $\alpha$ に検証済み尺度がないことを難点とする。本モデルの側から見ると、$\alpha$ が低いとは
**$\mathrm{conv}(q, \kappa_n)$ が成り立たない**ことであり、それは既に機械判定の形を持っている（EXPR_OK の階数隣接則）。
尺度が要るのは $\alpha$ そのものではなく、**階数距離が2以上になる条件**である。問いが一段小さくなる。

---

## 4. 統合しないもの

| 項目 | 理由 |
|---|---|
| Burnham の $\sigma$ を決定的変数として扱うこと | 報告自身がメタ分析の不一致を挙げている。B2C標本（158名・144名）由来。**$\sigma_p$ のみを $\mathrm{cost}$ の一成分として採り、$\sigma_f, \sigma_r$ は採らない** |
| pilot purgatory の数値 | すべてコンサル調査。査読実証なし。$\theta$ の**存在**は採るが、**閾値**は採らない |
| Tukker の PSS 採用障壁 | 標本がB2C（若年層・モビリティ）。$\pi$ の類型だけ採り、障壁の知見は採らない |
| $\alpha$ の多項目尺度 | 未確立。**降格の対象**として較正台帳に載せる（§11 の機構をそのまま使う） |
| 政治的可視性の非公共での点火条件 | 理論的示唆のみ。現状どおり較正表に残す |

**そして最も重要な留保。** 報告が挙げた文献はすべて**説明**の理論であって、**判定**の理論ではない。
ABJ の 0.55/0.35 は費用が下方硬直することを示すが、この案件でこの費目が硬直するかは言わない。
$\Pi_1$〜$\Pi_3$ に置き換えてはならない。**生成子は較正表の中身を決めるが、原理を置き換えない。**

---

## 5. 第10版の設計（着手順）

現行の三層——原理 / 較正表 / 入力 $\nu$——の**中間層を、業界の関数から商材座標の関数へ移す**。

```
        第9版                              第10版
原理    Π1 Π2 Π3 ＋ N1〜N5                 変更なし
中間層  較正表（6業界で較正・伝播する）      商材座標の関数（商談前に観測可能）
入力    ν（案件ごと）                       ν ＋ 商材座標7本
```

**段階1（実証が固く、自分のデータも支持する）**
- $I, S_1, S_2, S_3$ を削除。$\beta_1$（会計分類）$\beta_2$（予算内外）を商材軸に追加する。
- `EXPR_OK` の 実務性↔価格 の辺を、$\alpha$ と $\beta_1$（費目の硬さ）の関数にする。ABJ が機序を与える。
- U7 を未踏から外す。$\beta_1$ で埋まる。

**段階2（$\Pi_3$ から導出できるので実証を待たなくてよい）**
- `ISO_KEYS` を $\{\lambda \text{一致}, W \text{同型}, \tau \text{同型}\}$ へ置換。16/16 の発火が消えるはずで、これは**次の走行で検定できる**。

**段階3（観測可能量への置換）**
- `ELIMINABLE_IF_FREE` の判定を `cost_to_buyer > 0` から $\theta$（分割試用可能性）$\times\ \sigma_p$（手続切替コスト）へ。
- $\mathrm{cost}$ をスカラからベクトル $\langle \sigma_p, \tau_{\text{並行運用}}, a_{\text{注意}} \rangle$ へ型付けし、$\Pi_1$ 第2式に初めて検査対象を与える。

**段階4（文献で決まらない残余）**
- $\alpha$ の閾値、`START` の商材→進捗予測関数、政治的可視性の非公共点火。自社商談ログからの回帰。

段階1と2を入れると、**較正表5本のうち4本が商材座標の関数になり、業界に依存するのは `START` だけになる**。
§11 の較正台帳の対象が5本から1本に縮む。これが今回の統合の実質である。

---

## 6. リサーチが足りていなかったことについて

先の指摘は当たっている。$A$（効果が分かる時点）は Nelson-Darby-Karni の search/experience/credence そのものだったし、
$\mathrm{Acct}$ の〈硬さ〉は cost stickiness の再発見だった。$U7$ は会計基準を読めば商談前に埋まる欄だった。
**9版のあいだ、アノマリーから発見してきたものの一部は、文献を先に読めば最初から入っていた。**

ただし逆向きも成り立つ。$\Pi_2$ の二連言（可能と執行の分離）は、$\alpha$ と stickiness を**一つの構造の二面**として扱えるが、
文献側にこの統合はない。$V$（決裁権なき拒否権）と $W$（執行座席）の対、$\Gamma^{\mathrm{own}}$ と侮辱の関係、
自己適用（落とすのに使った刃は自分にも当たる）は、既存文献に対応物を見つけていない。

**文献は較正表を埋め、走行は原理を削る。** 役割が違う。次からは、アノマリーを立てる前に「この欄は誰かが測っていないか」を先に引くのが正しい順序である。
