# 第12.4版 ── A28 の設計

**走らせていません。**コードと提示仕様の差分だけです。
版 `2d65deb`＋A28／回帰 **158項目 全通過**（132 → 140 → 147 → 158）。

---

## 1. 何が壊れていたか

④の量と⑥の量で、要求が完全に非対称だった。

| | 出所の要求 |
|---|---|
| **④の量**（今やる理由の根拠） | `q_source`（買い手データ／公開統計／売り手データ）＋ `q_receipt`（誰から・いつ・どの抽出条件）。買い手データを名乗って記録が無ければ **棄却**（R15・A15） |
| **⑥の量**（提案の価値そのもの） | **欄が一つも無い**。`Declared` の `s6_*` は18欄あるが、出所・受領記録に当たるものはゼロ |

**金額が出てくるのは⑥のほうである。**

そのうえで、A23 が「読む座席の数だけ量を置け」と要求する。売り手が実績を持つ座席は普通ひとつ。
**つまり仕様が構造的に量の捏造を誘っていた。**

> 社長向けに置いた「仕入原価120万〜300万円の圧縮見込み幅」は当社側の見込みであり、
> 買い手データにも当社の実績値にも裏付けがない。**根拠のない量を置いてよいのか、
> 置かずに座席要件を欠くべきか**（R2-P1。8体中5体が同じ訴え）

そして第12.2版の⑥本文を走査すると、**7/8 が自発的に「実測」「見込み」「概算」を入れていた**
（入れていないのは E1-P1 だけ）。**生成器は既にこの規範を持っていて、機械は一つも読んでいなかった。**

---

## 2. 設計の原則 ── 試算を禁じない

試算を禁じると A23 と正面から衝突し、**売り手が実績を持たない座席で生成不能になる**。
それは現実にありふれた状態なので、禁じない。

**機械は決められないものを `needs_judgment` に積んで営業へ回せる。生成器にはその出口が無かった。**
出口が無いから、**作るか落とすかの二択**になっていた。A28 は三つ目の出口を作る。

```
量の出所 ∈ { 買い手データ, 公開統計, 売り手の実績, 試算, 営業記入 }
                └──── 裏づけあり ────┘   └── 裏づけなし ──┘
```

裏づけの無い二つには、**安い要求を一つずつ**だけ付ける。

| 出所 | 要求 | 満たさなければ |
|---|---|---|
| **試算** | 本文に**試算と分かる語**があること（試算・見込み・概算・推定・想定…のいずれか） | `A28_ESTIMATE_UNMARKED`（停止） |
| **営業記入** | 本文に**記入欄**があり、`s6_to_sales` に何を仰ぐかが書かれていること | `A28_SLOT_ABSENT`（停止） |

どちらも生成器には安い。安くないのは、**根拠のない数字が実測の顔をして⑥に載ること**である
（買い手はそれを検算する）。

照合は A25 の教訓に従い、**候補を並べてどれか一つでも在れば通す**（完全一致では照合にならない）。

---

## 3. 入れたもの

### 宣言欄（2つ）

```python
s6_quantity_sources : Dict[座席名, 出所]   # 座席ごとに、置いた量がどこから来たか
s6_to_sales         : Tuple[str, ...]      # 営業に算出・判断を仰ぐ項目
```

### 検査 `check_quantity_sources`

| 場合 | 出るもの |
|---|---|
| 出所が未申告 | `A28_SOURCE_UNDECLARED`（要判断）。黙って通さない |
| 座席のどれかが未記入 | `A28_SOURCE_MISSING`（要判断） |
| 定義域の外の出所（「勘」など） | `A28_SOURCE_UNKNOWN`（要判断）。N₂ を出所にも適用する |
| 試算なのに本文に試算と分かる語がない | `A28_ESTIMATE_UNMARKED`（**停止**） |
| 営業記入なのに本文に記入欄がない | `A28_SLOT_ABSENT`（**停止**） |
| 裏づけあり | `A28_GROUNDED`（註記）。どの座席が裏づけを持つかが残る |
| 申し送りが未申告 | `A28_TO_SALES_UNDECLARED`（要判断） |
| 裏づけの無い量を置きながら申し送りが空 | `A28_TO_SALES_EMPTY`（要判断） |
| 申し送りあり | `A28_TO_SALES`（註記）。**内容が採点結果に残り、営業が読める** |

### 提示仕様（⑥の枚仕様に一項目）

> **上で並べた量それぞれについて、その数字がどこから来たのかを本文に添える。**
> 買い手から受け取ったデータ／公開統計／自社の実績、のいずれかなら、そう書く。
> **裏づけが無い見込みなら「試算」「概算」と分かる形で書く**
> （買い手はこれを実測だと思って検算する。取り違えさせないこと）。
>
> **数字を自分で確定できないなら、無理に作らず、営業が埋める記入欄（【　　　】のような空欄）に
> してよい。**その場合は `s6_to_sales` に『何を営業に算出・判断してほしいのか』を言葉で書くこと。
> **分からないものを分からないまま渡すのは、間違った数字を置くより良い。**

出力の申告欄にも同じ趣旨を置いた ──「**裏づけが無いのに『売り手の実績』と書かないこと**」
「**推測で埋めるより、ここに出すほうがよい**」。

---

## 4. これが直しているのは、実は仕様の側の矛盾である

A27 とまったく同じ形だった。

| | 両立しない二つの要求 | 規定されていた優先順位 |
|---|---|---|
| **A27** | ⑥の必須要素（10〜11個）と 字数上限（450字） | 無かった → 生成器が毎回決めていた |
| **A28** | A23「座席の数だけ量を置け」と 「根拠のない量を置くな」（生成器が④から推し量った規範） | 無かった → 生成器が数字を作っていた |

A27 では〈導出 > 較正〉で決めた。A28 では**どちらも譲らせず、三つ目の道を作った** ──
量は置く（A23 を守る）、ただし裏づけが無いことを隠さない（規範も守る）。

---

## 5. 走行前の確認（第12.2版で踏んだ配管の罠の再発防止）

```
arm0/1/2 とも  A26 枚仕様 8/8 ・申告欄 8/8
               A27 優先順位 8/8 ・申告欄 8/8
               A28 枚仕様 8/8 ・出所欄 8/8 ・申し送り欄 8/8
```

第12.2版の走行を新しい物差しで測ると、停止は 0 になり、
`R10a_RESIDUAL_UNDECLARED` 6 ／ `A27_OMISSION_UNDECLARED` 8 ／ `A28_SOURCE_UNDECLARED` 8 の
**要判断に落ちる**。旧走行が新しい欄を持たないためで、設計どおり。
**A26・A27・A28 の効果は、次の走行でしか測れない。**

---

## 6. 走らせるなら

3つとも別々の診断コードを持つので、採点上は分離できる。
⑥字数は A27 で採点の根拠から外したので、A27×A28 の主な交絡（⑥が長くなる）は測定を汚さない。

予測は `predict_v12_3.md`（A26・A27）に置いてある。**A28 の予測は走行を決めてから足す** ──
先に置かなければ意味がないので、決める前に書いておく：

| | 予測 |
|---|---|
| `A28_SOURCE_UNDECLARED`（要判断） | **0**（欄そのものが指示として働く。Arm 0 の実測 8/8 に依る） |
| 出所が「試算」または「営業記入」の座席 | **6〜10**（16座席中。売り手が実績を持つのは1商材あたり1座席程度） |
| `A28_ESTIMATE_UNMARKED`（停止） | **0〜2**（7/8 が既に自発的にヘッジ語を入れていた） |
| `A28_SLOT_ABSENT`（停止） | **0〜1** |
| `s6_to_sales` が空でないセル | **6〜8** |
| `A28_TO_SALES_EMPTY`（要判断） | **0〜2** |

**外れたら何が言えるか。** 出所が「売り手の実績」に偏ったら、
**生成器は裏づけの無さを認めない**ことになる（自己申告では認めていたのに）。
そのときは申告ではなく本文の側から測る必要がある。
