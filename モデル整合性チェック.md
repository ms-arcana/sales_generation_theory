# モデルの整理と整合性チェック ―― アノマリーは多すぎるのか

版：`sales_logic.py` sha256[:12] `8533c901c220`／回帰 299項目／理由コード 254／台帳の番号 61
道具：`audit_model.py`（機械で回す。人の記憶で数えない）

---

## 0. 結論を先に

**多すぎるのは番号であって、種類ではありません。**

```
A番号  52（枝番込み・台帳の行 61）
型     8   ← 第12版以降、増えていない
```

そして、**いちばん大きい型は「一つの語／欄が二つの担体を持つ」8件**で、
これは **`N₆` に一行書けば書けなくなります**（「担体は一つ」）。
第8版が 7中心命題＋16規則＋15条件 を 3原理＋5記法へ畳んだのと、同じ操作です。

---

## 1. 52件は、いくつの型に畳めるか

| 型 | 件数 | 中身 |
|---|---|---|
| **型1 連結**（複数を一つの欄に） | 8 | A24／A25／A25b／A25c／A30〜A33 |
| **型2 ⊥ を値として扱う** | 2 | A45d／A51 |
| **型3 適用範囲のずれ** | 5 | A26／A28／A39／A42／A50 |
| **型4 順序・範囲の欠落** | 2 | A27／A29 |
| **型5 一つの語／欄が二つの担体** | **8** | A37／A37b／A41／A41b／A47／A48／A48b／A49 |
| **型6 配管**（計算したのに届かない） | 5 | **すべて番号なし** |
| **型7 浅い一致**（検査の側） | 8 | A34 ＋ **7件が番号なし** |
| **欠落**（検査そのものが無かった） | 12 | A35／A36／A38／A40／A43／A44／A45系／A46／A50b／A52 |

合計 **50件**。うち **A番号が付いているのは 38件、番号が無いのが 12件**。

### いちばん重い事実

**型6（配管）と型7（浅い一致）の 13件のうち 12件に番号が付いていません。**
「モデルのアノマリー」ではなく「実装の欠陥」として扱ってきたからですが、
**合わせると型5と並ぶ最大勢力**です。

そして番号空間が二つある状態は、**A36〜A39 が衝突したときとまったく同じ構図**です。
（あのときも「走行レポート側」と「コード側」で二箇所から払い出していました。）

> **提案：型6・型7 にも番号を出す。**台帳の規律3「走行レポートも同じ番号空間を使う」を、
> 実装の欠陥へも広げる。**数えていないものは、減ったかどうかも言えません。**

---

## 2. 機械で見た整合性 ―― 六つ

### (1) 台帳 ↔ 理由コード

```
台帳の番号 61 ／ 理由コードに現れる番号 33
★コードに在って台帳に無い：なし        ← 一元管理は効いている
台帳に在ってコードに無い：28件          ← 吸収済み・設計のみ・未着手の合計
```

**28件の内訳を人が説明できることが、台帳の値打ちです。**
A29 は N₄′ が吸収、A30〜A33 は N₆ が吸収、A38/A39/A40/A45d/A50b は未着手、
A44/A48b は別のコードで扱っている ―― ここまでは追えます。

### (2) 理由コード ↔ 表示文

```
表示文が無いコード 59 ／ コードに無い表示文 11
```

**59 は多すぎます。**ただし大半は内部用の定数（`EXPR_OK`・`BINDER_SRC`・`B_authority` など）で、
買い手や営業に出る `Finding`／`Judgment` ではありません。
**「出るコードかどうか」で仕分ける必要があります**（下の (6) と同じ仕事）。

### (3) 決定表の配管（V3）―― **入りました**

```
compile_deal の鍵 31 ／ 白名簿 23 ／ 指示文が読む鍵 23
★計算しているのに白名簿に無く、指示文が読もうとする鍵：なし
```

**四度手で直した箇所が、これで機械の検査になりました。**
`lt_months`／`decide_deadline_tau`／`s6_price_*`／`s2_asks_possession`／`buyer_quantities` ――
五度目は、このスクリプトが先に見つけます。

### (4) 申告欄の配管

```
Declared の欄 37 ／ REQS 34（＋廃止3）／ to_declared 34
★REQS に無い欄：なし   ★to_declared が渡していない欄：なし
```

### (5) 較正の棚卸し（V5）

較正由来の判定は **8件**。未較正の業界では降格します。

```
A1_NOT_EXPRESSIBLE / A5_NOT_EXPRESSIBLE / A16_NOT_CONV_AT_SEAT  ← EXPR_OK
A2_C_NOT_SINGLETON                                              ← ISO_KEYS
A8_BINDER_EMPTY / A8_BINDER_NOT_ABOVE                           ← BINDER_SRC
R6b_LT_SHORT                                                    ← ACQUIRE
A45_RANGE_TOO_WIDE                                              ← PRICE_RATIO_MAX
```

**A46 の `DISCLAIM_MAX=3` が、この表に入っていません。**較正値なのに登録漏れです。
（本文で「較正」と書いたのに、コードの較正表には無い ―― **書いたことと実装が食い違っている**。）

### (6) 発火していない検査

```
一度でも出たコード 87 ／ 一度も出ていない 167
```

**これが最大の未整理です。**167 のうち、
(a) 出るはずがない／(b) 出るはずなのに出ていない／(c) 配線されていない
の仕分けができていません。**(b) が第12版の `R10a` と同型で、いちばん高くつきます。**

---

## 3. 監査そのものが、8件目の浅い一致を踏みかけた

`audit_model.py` の最初の正規表現は

```python
CODE_RE = re.compile(r'"([A-Z][A-Z0-9]*(?:_[A-Z0-9]+)+)"')
```

で、**`R6b` の小文字 b を拾えませんでした。**結果として `R6b_LT_SHORT` を
「較正表が実在しないコードを指している」と**誤って報告しかけました**。

直したら理由コードの数が **199 → 254** に増えました。
**55件が、それまでの数え方から漏れていた。**

> **監査の道具にも、同じ型の誤りが出ます。**
> 第13.7版の「本文全体に日付照合を当てると 8/8 誤検出」と同じ形です。
> 型7 は、モデルの外にも湧きます。

---

## 4. 提案 ―― 第14版で畳む

第8版と同じ操作を、もう一度かけられる状態にあります。

| # | やること | 何件が畳めるか |
|---|---|---|
| **1** | **`N₆` に「担体は一つ」を書く** | **型5 の 8件が 1つの型になる。**そして9件目が**書けなくなる** |
| **2** | **型6・型7 に番号を出す** | 12件が番号空間に入る。**減ったかどうかを言えるようになる** |
| **3** | 167 の未発火コードを (a)(b)(c) に仕分ける | 表示文の 59／11 のずれも同時に解ける |
| **4** | `DISCLAIM_MAX` を `CALIBRATED_CODES` へ | 較正の棚卸しが閉じる |
| **5** | `SPEC8.md` を A24〜A52 で更新（**型の側から書く**） | 番号の羅列ではなく **8つの型**として書けば、仕様が短くなる |

**1 が最も安く、最も効きます。**型5 は8件あり、しかも直近5版で連続して出ています
（A37・A37b・A41・A41b・A47・A48・A48b・A49）。
**「担体は一つ」を記法に書けば、9件目は書けなくなる** ―― これが第8版で
「同じ誤りが4回出たものは規則ではなく型である」と言ったことの、二度目の適用です。

---

## 5. 「多すぎる」への直接の答え

- **番号は多い。**52件。しかも**実装済みで効果を実測したのは 12件**、**未着手が 10件**。
- **型は多くない。**8つで、第12版以降増えていません。
- **増え方が異常なのではなく、畳む時期が来ています。**
  第7版→第8版のときも、15のアノマリーが溜まってから 3原理＋5記法へ畳みました。
  **いま溜まっているのは 52件で、畳む先は「担体は一つ」の一行です。**
