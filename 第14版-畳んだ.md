# 第14版 ―― 畳んだ

版：`sales_logic.py` sha256[:12] `5f188dfd73e3`／回帰 **314項目 全通過**
N₆ 要求監査 8 ／ **N₆ 記号監査 0** ／ 座標の棚卸し 9 ／ 理由コード 257 ／ 表示文 208

---

## 1. 何をしたか ―― 一行

**`N₆` の四つ組に、一行足しました。**

> **一つの記号（欄名・語・記法）は、一つの担体しか指してはならない。**

第8版が「同じ誤りが4回出たものは規則ではなく型である」と言って `N₁`〜`N₅` を置いたのと、
**同じ操作の二度目**です。今回は 4回ではなく **8回**出ていました。

```
A37    s6_start_date  ＝ 決定が締まる日 と 実際に動き出す日
A37b   LT_months      ＝ 決定→着手 と 着手→境界
A41    τ 項           ＝ 今やる理由の担体 と 決定が締まりうる窓
A41b   決定期限        ＝ ④からの逆算 と 決定の窓
A47    ②の問い        ＝ 単位の置き換え と 買い手の保有
A48    「軸」          ＝ 記法が型を与えるもの と 原理が値域を与えるもの
A48b   ν              ＝ 入力の記録 と 検証時点
A49    source         ＝ 払うの出所 と 戻るの出所
```

---

## 2. どう書けなくしたか

`GLOSSARY`（記号表）と `audit_symbols()` を置きました。
形は `Req.yields_to`（同順位の衝突に順序を書かせる）と**まったく同じ**です。

```python
class Sym(NamedTuple):
    sym: str        # 記号・欄名・語
    means: str      # 何を指すか
    carrier: str    # 担体
    where: str      # どこで使うか
    distinct: str = ""   # 同じ記号が他にもあるときの書き分け。**空なら監査が落とす**
```

**回帰でこれを確かめてあります。**書き分けを書かずに同じ記号を足すと `N6_SYMBOL_AMBIGUOUS` が出て、
書き分けを書くと通ります。**9件目は書けません。**

### 二つの重なりを、この機会に解きました

| 記号 | 決めたこと |
|---|---|
| **ν** | **入力の記録を ν、検証時点を ν_v と書き分ける。**コードは後方互換のため `Nu` / `Product.nu` のままで、**読み口は `nu_of()` 一箇所に限る**（直接 `n.A` を読まない） |
| **軸／座標** | **記法が型を与えるものだけを〈軸〉と呼ぶ**（入力ν・τ・J・Σ）。**原理／文献が値域を直接与えるものは〈座標〉**（λ・ε・商材座標の7成分）。後者は型検査が効かない |

10件の解決済みの記号は、記号表に**履歴として残してあります**。消しません。

---

## 3. 52件 → 8つの型

| 型 | 件数 | 処置 |
|---|---|---|
| 型1 連結（複数を一つの欄に） | 8 | `N₆` の card="n"（欄を割る） |
| 型2 ⊥ を値として扱う | 2 | `N₂`。A51 で入力にも ⊥ を置けるようにした |
| 型3 適用範囲のずれ | 5 | 規則の全称・存在・定義域を実装へそのまま写す |
| 型4 順序・範囲の欠落 | 2 | `N₆` の `yields_to` |
| **型5 一つの記号が二つの担体** | **8 → 1** | **N₆ 追補（この版）。9件目が書けなくなった** |
| 型6 配管 | 5 → **A53** | `audit_model.py` (3)(4) |
| 型7 浅い一致 | 8 → **A54** | `audit_model.py` ＋ V1 の全数検分 |
| 欠落（検査が無かった） | 12 | 個別に実装 |

**型は第12版以降、増えていません。**番号が増えているのは、同じ型の別の座標を見つけているからです。
これは第8版の被覆グリッドが言ったこと（「8/8 という再現率は新しい原理の発見ではなく、
グリッドの穴の指標である」）と同じ現象です。

---

## 4. 番号を出していなかった 13件に、番号を出した

**型6（配管）5件と型7（浅い一致）8件のうち、番号が付いていたのは A34 の1件だけ**でした。
「モデルのアノマリー」ではなく「実装の欠陥」として扱ってきたためですが、
**合計13件は型5と並ぶ最大勢力**です。そして番号空間が二つある状態は、
**A36〜A39 が衝突したときとまったく同じ構図**でした。

```
A53  配管 ―― 計算しているのに届かない（5件をまとめて1件に）
       lt_months/today／decide_deadline_tau／s6_price_*／s2_asks_possession・s5_disclaimers／buyer_quantities
A54  浅い一致 ―― 検査の側の文字列照合（8件をまとめて1件に）
       own_retracted／「タダ」／SLOT_RE／散文の物差し／R20 の記入欄／R20 の三状態／
       ④の日付照合（未然）／audit_model.py の正規表現（未然）
```

**個票を並べずに、型の水準で1件にしました。**数えられるようになったので、
**次からは「減ったか」を言えます。**

---

## 5. 整合性チェックの残り

`audit_model.py` で機械化した六つのうち、(3)(4)(5) は閉じました。残るのは二つです。

| | 状態 |
|---|---|
| (2) 表示文が無いコード **62** ／ コードに無い表示文 **11** | 大半は内部定数（`EXPR_OK`・`BINDER_SRC`）で、買い手や営業に出る `Finding`/`Judgment` ではない。**「出るコードかどうか」で仕分ける必要がある** |
| (6) 一度も出ていないコード **167 / 257** | **最大の未整理。**(a)出るはずがない (b)出るはずなのに出ていない (c)配線されていない の仕分けが要る。**(b) が第12版の `R10a` と同型でいちばん高くつく** |

(2) と (6) は同じ仕分けで同時に解けます。**次はここです。**

---

## 6. この版で確定したこと

1. **多すぎるのは番号であって、種類ではなかった。**52件 → 8型。
2. **最大の型は記法の一行で消えた。**型5 の8件が1つになり、9件目が書けなくなった。
3. **数えていなかった13件に番号が出た。**減ったかどうかを言えるようになった。
4. **監査の道具にも同じ型の誤りが出る。**`audit_model.py` の正規表現が `R6b` を拾えず、
   理由コードを 199 と数えていた（正しくは 254）。**型7 はモデルの外にも湧く。**
