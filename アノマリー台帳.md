# アノマリー台帳 ―― 番号の払い出しは、ここ一箇所で行う

第12.8版の25業界走行と第13.x のコード群で、**A36〜A39 が別の意味で衝突しました。**
番号が二箇所で払い出されていたためです。**以後、新しい番号はこの台帳でだけ払い出します。**

- **払い出し済みの最大**：`A57`（枝番は `A45d`・`A50b`・`A53b` まで）
- 次に使う番号：**`A58`**
- 番号を使う前に、この表に**行を足してからコードに書く**こと。

| 番号 | 名前 | 出所 | 状態 |
|---|---|---|---|
| A1〜A23 | （第5〜11版。`SPEC8.md` に記載済み） | 各版 | 実装済み |
| A24 | 行為は集合である（`s6_realize` の三つ組の配列） | 第12版 | 実装済み |
| A25 / A25b / A25c | 単位・基準の連結（型1） | 第12版 | 実装済み |
| A26 | 残余の周期（`s6_residual_period_months`） | 第12.3版 | 実装済み |
| A27 | 必須要素と字数の衝突順序（`s6_omitted_blocks`） | 第12.3版 | 実装済み |
| A28 | ⑥の量の出所（`s6_quantity_sources` / `s6_to_sales`） | 第12.4版 | 実装済み。**A44 で反証** |
| A29 | 担体＝量の同順位対 | 第12.7版 | **N₄′ が解いた**（第13.5b版） |
| A30〜A33 | 型1・型4（N₆ に吸収） | 第12.7版 | `audit_requirements()` |
| A34 | 記入欄の照合が浅い（`SLOT_RE`） | 第12.8版 | 実装済み |
| A35 | （第13版・第2段） | 第13版 | ― |
| A36 | （第13版・第2段） | 第13版 | ― |
| **A37**〔型5〕 | **LT を⑥の日付へ（決定／着手／実現の三段）** | 第13.5版 | **実装・走行済み。7/16 → 0/16** |
| **A37b**〔型5〕 | **`LT_months` の担体が二つ／逆算日は決定期限** | 第13.5版 | 実装済み |
| A38 | N₃′ 様相（`s6_realize` の第4成分） | 第13版 | **未着手** |
| A39 | R17 の紙側 | 第13版 | **未着手** |
| A40 | 戻りの因果 | 第13.3版 | **未着手** |
| **A41**〔型5〕 | **決定の窓（④から落とした τ 項が⑥の決定日を縛る）** | 第13.5b版 | 実装・走行済み |
| **A41b**〔型5〕 | **窓は着手日も縛る／決定期限は資料に一つだけ（実効値 min(逆算, 窓)）／④に出してよい日付の列挙** | 第13.6版 | **実装済み・未走行**（第13.7版）。`A41B_START_BEFORE_GATE` / `A41B_S4_FOREIGN_DATE`（停止）・`A41B_S4_DATES_UNCHECKED`（要判断）。真因は**規制ではなく欠落** ―― E1 は逆算が ⊥ で決定期限が一つも渡っておらず、生成器が自分で作っていた |
| **A42** | **D（逓増）の型ずれ（25業界で 23/25）** | 第13.5b版 | 検出のみ。処置は未着手 |
| **A43** | **実現日の季節（ω＝導出／繁忙期＝較正）** | 第13.5b版 | 実装・走行済み |
| **A44** | **営業へ回した空欄が、資料を殺す**（旧 A37・25業界レポート） | 25業界走行 | 21/21。`R20` が機械側で確認（8/8）。**出口の〈式〉を第13.8版で実装** ―― `ret_expr`／`ret_basis`／`ret_coef`／`coef_source`。式が揃えば停止しない（`R20_RETURN_AS_EXPR` で申し送り）。成分が欠ければ停止（`R20_EXPR_NO_BASIS`／`_NO_COEF`／`_COEF_UNSOURCED`）。**未走行** |
| **A45** | **幅のある金額は、稟議に載らない**（旧 A38・25業界レポート） | 25業界走行 | 20/21。**第13.8版で実装。**`A45_RANGE_TOO_WIDE`（停止）。閾値 `PRICE_RATIO_MAX = 2.0` は**較正値**（実測は2点のみ）で `CALIBRATED_CODES` に登録済み ―― 未較正業界では降格。**未走行** |
| **A45b** | **価格の内訳が無い** | 25業界走行 | 21/21。**第13.8版で実装。**`A45B_BREAKDOWN_MISSING`（停止）／`A45B_BREAKDOWN_MISMATCH`（停止・Π₁ の無矛盾から導出）。**算術なので降格しない。未走行** |
| **A45c** | **長く頼むほど単位あたりが高くなる** | 25業界走行 | 「180万÷3か月＝月60万、900万÷12か月＝月75万」。**第13.8版で実装。**`A45C_UNIT_PRICE_NOT_MONOTONE`（停止）。第13.7版の1ケース走行では 60万→50万で単調・停止0 |
| **A45d** | **内訳の金額が〈幅〉を持つとき、和の突合が下限だけになる** | 第13.7版・1ケース走行 | `parse_amount` は最初の数だけ取る（「240万〜360万円」→ 240万）。今回はたまたま下限の和＝総額の下限で一致した。**幅は値ではない**（三状態と同じ形）。下限の和・上限の和の両方で突き合わせるべき。**未処置・7件目の浅い一致（未然）** |
| **A46** | **断り書きが、逆に侮辱として読まれる**（旧 A36・25業界レポート） | 25業界走行 | 7/21。R17 への過剰適応。**第13.9版で実装。**`A46_DISCLAIMER_REPEATED`（停止・対象の重複）／`A46_DISCLAIMER_MANY`（要判断・段あたり3件超＝**較正**）。**回数で数えるので版をまたいで比べられる。**実測は段あたり 0〜7（第13.7版 E1-P1 の⑥が7件で最多）。**未走行** |
| **A47**〔型5〕 | **②の問い返しが、決めつけとして着弾する**（旧 A39・25業界レポート） | 25業界走行 | 8/21。**②の設計そのものへの反証。**担体を〈保有〉から〈単位〉へ。**第13.9版で実装。**`A47_S2_ASKS_POSSESSION`（申告）／`A47_S2_POSSESSION_QUESTION`（紙側・A23紙側と同型）。検分：実データ17件で停止**1件だけ**（E2-P1 の実在の一文）。**未走行** |
| **A48**〔型5〕 | **「軸」が二つの担体を持っていた**（型経由 入力ν・τ・J・Σ ／ 直列挙 λ・ε ＋ **商材座標の全成分**） | 第13.6b版・資料間の不一致 | **第13.10版で `audit_axis_types()` を置いた**（9本を名指し・`audit_requirements` と同じ位置づけ）。型検査は依然として無いので、**人の承認が要る箇所として明示するところまで**。直列挙の群は記法を通らないので型検査が効かず、`check_axis_values` の列挙照合だけ。A37／A37b／A41 と同じ型（一つの語が二つの担体）だが、**モデルではなく資料の側**で起きた |
| **A49**〔型5〕 | **〈出所〉の欄が二つの担体を持つ**（払うの出所／戻るの出所） | 第13.7版・1ケース走行 | `A28_SOURCE_UNKNOWN` が **3/3 座席**で立った。**A37／A37b／A41／A48／A48b に続く6件目の〈一つの語／欄が二つの担体〉。第13.10版で欄を割った**（`pay_source` / `ret_source`）。旧欄に連結して書かれた形は `A49_SOURCE_MERGED` で申し送る。**未走行** |
| **A50** | **④で「なぜ今か」に据えた境界日を、⑥の日程が越えている** | 第13.9版・逆方向 oracle | **A/B 両版・2/2 座席が decisive に挙げた。**`start_deadline` は A/C 形からしか逆算せず、E1 の境界日（`Ec`）は**何も縛っていなかった**。**第13.10版で実装。**`earliest_realize()` で最速の実現日を出し、④の境界日に間に合わなければ `A50_BOUND_UNREACHABLE`（**要判断**）。紙側は `A50_REALIZE_AFTER_BOUND`。**停止にしない** ―― 総当たりで **4/8 が充足不能**（売り手のLTと買い手の窓では物理的に間に合わない）。機械は「間に合わなくても根拠に使ってよいか」を決められない（A22 と同型）。**未走行** |
| **A50b** | **散文が表の数字を言い直すときの算術が検査されていない** | 第13.9版・逆方向 oracle | 手で組んだ⑥で「回収年数＝600万÷780万で1年未満」と書いたが、事例で実際に減ったのは差し引き180万で**3年以上**。**買い手 2/2 が独立に検算して指摘。**R20・A45 は申告欄の整合しか見ておらず、本文の言い直しを見ていない。**第13.3版の4欠陥と同じ形が、狭い範囲で再発した。未着手** |
| **A51** | **入力に ⊥ を書く場所が無かった** | 第13.10版 | `Product` は全成分に既定値を持つので、**聞き取っていない欄が黙って特定の値になる**（`nu="使えば分かる"`・`theta="段階分割"`…）。**型2 そのもの ―― 未定義を既定値と読む。**処置：全定義域に「不明」を足し、`A51_AXIS_BOTTOM`／`A51_AXIS_UNDECLARED`（要判断）で人へ回す。値としては使わせない |
| **A52** | **⑥の〈戻る〉の式が掛ける相手が、入力に無い** | 第13.10版 | A44 の出口として〈式〉を作ったが、**式が掛ける相手が入力に在るとは限らなかった** ―― 第13.7版では3座席のうち1つ（理事会「予算科目『広報外注費』の年間執行額」）が**入力のどこにも無い量**で、生成器が発明していた。**在るのは τ の量**（`q`／`q_low`〜`q_high`／`q_source`）で、**決定表にも指示文にも出していなかった**。処置：`buyer_quantities()` を決定表に載せ、指示文に列挙し、紙側で `A52_BASIS_NOT_IN_INPUT`（要判断）。**未走行** |
| **A53** | **配管 ―― 計算しているのに届かない**（型6・**5件をまとめて1件に畳んだ**） | 第12版〜第13.10版 | `lt_months`/`today`（第13版）／`decide_deadline_tau`（13.7）／`s6_price_*`（13.8・**A45 が一度も走らなかった**）／`s2_asks_possession`・`s5_disclaimers`（13.9）／`buyer_quantities`（13.10）。**番号を付けていなかったので、減ったかどうかを言えなかった。第14版で `audit_model.py` (3)(4) が機械の検査にした** |
| **A53b** | **配管の逆向き ―― 指示文は頼んだのに、答える欄が無かった** | 第14.1版・VS Code | `s2_asks_possession`／`s5_disclaimers` は指示文が **3/3 arm** で要求しているのに、生成スキーマ（`wf_gen*.js` の `GEN_SCHEMA.declared`）に欄が無く、**36件の生成物すべてで鍵すら現れなかった（0/36）**。A46・A47 の申告側は**原理的に出られない**。紙側は現に捕まえている（`gen136/out_E2-P1` に `A47_S2_POSSESSION_QUESTION`）。**第12版の `R10a` と同型で向きが逆**（規則→指示 ではなく 指示→答える欄）。**第14.2版でスキーマに4欄を追加**（＋A49 の `pay_source`／`ret_source`） |
| **A54** | **浅い一致 ―― 検査の側の文字列照合**（型7・**8件 → 13件**） | 第12.1版〜第14.3版 | `own_retracted`（12→6）／「タダ」（16→2）／`SLOT_RE`（9個置いて0判定）／散文の物差し（申告16・散文3）／`R20` の記入欄（停止0→21）／`R20` の三状態（式を値と読んだ）／④の日付照合を本文全体に当てると 8/8 誤検出（未然）／**`audit_model.py` の正規表現が `R6b` を拾えなかった（未然・コード199→254）**／**13件目（第14.3版・未然）：A55 の回帰で「生の `in UNIT_UNKNOWN` は0箇所」を数えたら、直した箇所に残した註釈（「ここは `pu in UNIT_UNKNOWN` と書いてあった」）を数えて落ちた。註釈行を除いて数える**。A34 のみ番号が付いていた。**⊥ を値として扱う箇所と、境界を取らない部分一致を疑う** |
| **A55**〔型5〕 | **⊥ の扱いが二つに割れていた ―― (a) ⊥ を別の欄で代用する／(b) ⊥ を決める述語が二つある** | 第14.1版・VS Code | **(a)** `dcd = dec.s6_decide_date or dec.s6_start_date`。決定日が ⊥ のとき**着手日を決定日の代理**にしていた。VS Code は `check_dates_v7` の1箇所を報告したが、**`check_gates`（A41）にも同じ代入があった（2箇所目）**。25業界の採点で `R12b` 停止 9 件のうち **3 件がこの形**で、**うち 2 件は買い手が「進める」と答えた紙**（＝誤停止）。受け皿は `R12b_START_UNDECLARED`／`A41_GATE_UNCHECKED`。**(b)** `check_decidable` の4箇所が `in UNIT_UNKNOWN` を生で引いており、`is_bottom` が見る**記入欄【　　　】を落としていた** ―― 単位欄の片方が記入欄なら `R20_UNIT_MISMATCH`（停止）が誤って立ち、両方・分母が記入欄なら**何も出ずに素通り**、出所欄が記入欄なら `A49_SOURCE_MERGED`／`A28_SOURCE_UNKNOWN` と誤診していた。**A44 の実装で値側を一度直した型を、単位・分母・出所側で再発させた形。第14.3版で6箇所とも処置**（`UNIT_UNKNOWN` は `is_bottom` の中だけで引く）。**手元の8セル走行では 18/18 が決定日を申告し、記入欄も 0/23 行なので、この欠陥は原理的に見えなかった** ―― 25業界側の紙でしか出ない |
| **A48b**〔型5〕 | **`ν` が二つの担体を持つ**（入力の記録 `class Nu` ／ 検証時点 `Product.nu`） | 第13.6b版 | **既に実害あり。**第10版が `Product.nu` を足した際に旧 `Nu.A` を残し照合を置かず、R1-P1・R2-P1 で食い違って `blocks_on` が古い側を引き「数字入りの導入事例」が落ちた（`nu_of()` docstring に記録済み・座標側を正とする処置は入済）。**語の重なりは未処置。**A48 の仕分けを誤らせた原因でもある |
| **A56** | **未取得がダミー値になる ―― 取込層**（`株式会社〇〇`／`山田 太郎`／`銀座1丁目1-1`／`01-2345-6789`） | 外部データ取込層の設計・実物 `back_office.pptx` | **A51 の裏返し。**A51 は「未申告が既定値になる」、A56 は「未取得がダミー値になる」。`R20` の空欄検査は**空欄しか見ない**ので、ダミーで埋まった欄は素通りする ―― 事例DBに実在しない企業が入る経路。**匿名事例が空欄より悪いのと同じ理由で、ダミー値は空欄より悪い。**処置案：インポータに辞書検査1本（〇〇／△△／□□／◇◇／ABC／山田太郎／1丁目1-1／03-0000／sample／example／Lorem）、当たったら値ではなく ⊥ として格納。**取込層で起きるので A48 と同じ扱い**（モデルではなく資料・入力の側）。**設計のみ・未実装** |
| **A57**〔型6〕 | **退役が読み手を置き去りにする ―― 畳んだ欄に生読みが残る** | 第14.3版・VS Code | 第14.3版で `s6_kappa_by_seat` を `REQ_RETIRED` へ移したとき、**欄の意味が吸収済みかだけを見て、読み手を数えなかった。**3つの読み手のうち `check_chain`・`check_quantity_sources` は橋 `quantities_by_seat()` を通っていたが、**`check_seat_words` だけが旧欄を生で読んでいた**。`wf_gen137.js` は第13.7版でその欄を落としている（スキーマが大きすぎて通らなかったため）ので、**`A23_SEAT_WORD_ABSENT` は新しい形の申告で構造的に出なくなっていた** ―― 同じ紙で 旧スキーマ 1件／新スキーマ 0件。**二つの計器のどちらにも映らない位置にあった**（`audit_model.py` (6) は旧走行の発火があるので「出たコード」側、`triage_codes.py` は関数が呼ばれているので (c) でも (b) でもない）。**第14.3版に書いた「その検査を通す入力を1件でも作れるか」を当てて初めて出た。**第14.2版の A49（A28 の橋が旧 `source` しか読んでいなかった）と同型で、隣の関数の取りこぼし。**第14.4版で処置** ―― 橋へ差し替え＋ `audit_retired_reads()`（`A57_RETIRED_READ_UNBRIDGED`）。**直した直後にレンダラの欠陥を2件検出**（E1-P2・R1-P2 で座席の様式語が⑥に無い）|

## 型 ―― 52件は8つに畳める（第14版）

| 型 | 件数 | 処置 |
|---|---|---|
| 型1 連結（複数を一つの欄に） | 8 | `N₆` の card="n"（欄を割る） |
| 型2 ⊥ を値として扱う | 2 | `N₂`。A51 で入力にも ⊥ を置けるようにした |
| ― | ― | **A55(b) は型2 と型5 の交点**：⊥ の**述語**が二つあったので、片方が ⊥ を値と読んだ |
| 型3 適用範囲のずれ | 5 | 規則の全称・存在・定義域を実装へそのまま写す |
| 型4 順序・範囲の欠落 | 2 | `N₆` の `yields_to` |
| **型5 一つの記号が二つの担体** | **8 → 9**（**A55**） | **`N₆` 追補「担体は一つ」＋ `GLOSSARY`／`audit_symbols()`（第14版）。9件目が書けなくなった** ―― と思ったが、**A55 が書けた。**`GLOSSARY` は**記号の名前**を見張るもので、`dcd` のような**関数の中の局所変数**と、`is_bottom`／`in UNIT_UNKNOWN` のような**述語**は見張っていない。担体の一意性は〈名前〉だけでなく〈述語〉にも要る |
| 型6 配管 | 5 → **A53**／逆向き **A53b**／**退役 A57** | `audit_model.py` (3)(4)(7)＋**`audit_retired_reads()`（第14.4版）**。**(7) 生成スキーマの配管**は第14.1版に VS Code が足した |
| 型7 浅い一致 | **13** → **A54** | `audit_model.py` ＋ V1 の全数検分。**うち5件は監査・回帰の道具そのもの**（第14.1版・第14.3版） |
| 欠落（検査が無かった） | 12 | 個別に実装 |

**型は第12版以降増えていない。**番号が増えているのは、同じ型の別の座標を見つけているから。
第14.1版で型6に**逆向き**（指示→答える欄）が、型7に**監査の道具そのもの**が加わったが、どちらも新しい型ではない。

**第14.3版（A55）で分かったこと ―― 畳んだ後に、畳み方の穴が出た。**
第14版は型5を `N₆` 追補と `audit_symbols()` で閉じたつもりだったが、閉じたのは**記号の名前**だけだった。

```
名前   s6_decide_date / LT_months / ν …   → GLOSSARY が見張る（閉じた）
局所   関数の中の dcd                      → 見張っていない  ← A55(a)
述語   is_bottom ／ in UNIT_UNKNOWN         → 見張っていない  ← A55(b)
```

**担体の一意性は、記号の名前だけでなく〈述語〉にも要る。**
「⊥ かどうか」を決める述語が二つあれば、それは ⊥ という担体が二つあるのと同じである。
処置は `is_bottom` 一本化と、`test_v14_3.py` の「生で `in UNIT_UNKNOWN` を引く行は0」。

**同型の検分（第14.3版で実施）**：`is_expr`（`EXPR_MARKS`）は呼び出し1箇所・生引き0、
`SOURCE_KINDS` は同一ブロック内で2箇所とも同じ向き、`iso_date`・`parse_amount` は
述語ではなく変換なので二重化していない。**いま二重化しているのは `is_bottom` だけだった。**

## 依存規則

`R1`〜`R20`。**払い出し済みの最大は `R20`**（決定可能性・第13.5b版）。次は `R21`。

## 記法

`N₁`〜`N₆` ＋ `N₄′`（量は〈単位〉と〈比較の相手〉を持つ・第13.5b版）
＋ **`N₆` 追補「一つの記号は一つの担体しか指さない」（第14版）** ―― `GLOSSARY` と `audit_symbols()`。
`N₃′`（行為は四つ組 ⟨w,d,o,m⟩）は**設計のみ・未実装**（A38）。

## 規律

1. **番号はこの台帳で払い出す。**コードに書く前に行を足す。
2. **番号を再利用しない。**取り下げたものも行を残し、状態に「取り下げ」と書く。
3. **走行レポートも同じ番号空間を使う。**別立てにしない（今回の衝突の原因）。


## 番号の衝突（第14.3版で1件・未然）

外部データ取込層の設計書が、ダミー値の件を **`A53（仮）`** として起草していました。
**`A53` は配管（型6）で使用済み**で、枝番 `A53b` まで出ています。**`A56` へ振り直しました。**

これは台帳を作った原因（第12.8版の `A36`〜`A39` 衝突）と同じ形です。
**別の面で起草した文書が、台帳を見ずに番号を仮置きすると必ず起きます。**

> 仮番号を置かないこと。**番号が要る段階なら、先にこの表へ行を足す。**
> 決まっていないのは番号ではなく処置なので、行の〈状態〉に「設計のみ・未実装」と書けば足ります。


## 退役の前提条件（A57・第14.4版）

**欄の意味が吸収済みであることと、コードの読み手が全部移っていることは、別の事実です。**

```
順序   読み手を全部 橋へ移す  →  audit_retired_reads() が 0  →  REQ_RETIRED へ入れる
誤り   REQ_RETIRED へ入れる   →  読み手が1つ残る            →  検査が静かに死ぬ
```

第14.3版で私は逆をやりました。VS Code が第14.1版で
「吸収が本当なら (a)、吸収が漏れているなら (b)。判断を Cowork へ返します」と**正しく問うた**のに、
**設計の側から「(a) 吸収済み」と答えて、コードの読み手を数えなかった**のが原因です。

> **「吸収済みか」は、欄の意味への問いではなく、読み手への問いである。**
