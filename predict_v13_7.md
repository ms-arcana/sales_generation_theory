# 第13.7版 予測 ―― A41b（窓は着手も縛る／決定期限は一つ）

**走らせる前に置く。**外れも記録する。書き換えない。

版：`sales_logic.py` sha256[:12] は走行時に刻む／`decisions8_v13.json`／`prompts8_v13_arm*.json`
処置：**A41b のみ**。R20・A43・A41・N₄′ は第13.6版のまま（対照）。

---

## 0. 何を直したか（三つ）

| | 中身 | 効くセル |
|---|---|---|
| **(a)** 窓は**着手日**も縛る（`A41B_START_BEFORE_GATE`・停止） | 委員会が開かれる前に動き出せない | **E1 の2セル** |
| **(b)** 決定期限を**実効値**に一本化（`decide_deadline = min(逆算, 窓)`） | E1 は逆算が ⊥ で**期限が一つも渡っていなかった**。生成器が自分で 2026-12-30 を作った | **E1 の2セル** |
| **(c)** ④に書いてよい日付を**列挙で渡す**（`A41B_S4_FOREIGN_DATE`・停止） | 「④の逆算 2026-12-30 と⑥の決定 2026-09-30 が三か月ずれている」への直接の処置 | 8セル全部（指示）／違反時のみ停止 |

**(b) は欠落の修復であって規制ではない。**買い手の苦情は生成器の勝手ではなく、
**期限を一つも渡していなかったこと**から出ていた。ここを読み違えると次も外す。

---

## 1. 機械側の予測（走行後 `validate8_v12.py` で数える）

| # | 予測 | 根拠 |
|---|---|---|
| **M1** | `A41B_START_BEFORE_GATE` の停止は **0/8** | 指示に明記した。出たら**指示が届いていない**（R12b と同型） |
| **M2** | `A41B_S4_FOREIGN_DATE` の停止は **0/8** | 同上。ただし④が長い E2-P1・R1-P1・R2-P1 は危ない |
| **M3** | E1 の2セルで、⑥の**着手日 ≥ 2027-05-31** が **2/2** | 実効期限と窓の両方を渡した |
| **M4** | E1 の2セルの本文に **2026-12-30 は出ない**（第13.6版は E1-P1 に出ていた） | 期限を渡したので作る必要が無い |
| **M5** | `R12b_START_AFTER_DEADLINE` は **0/8** | E1 は期限が付いたので**新たに落ちうる**。ここが増えたら (b) が裏目 |
| **M6** | `R20_RETURN_MISSING` は **8/8 のまま** | 触っていない（対照） |
| **M7** | 停止の総数は第13.6版（21件）から**大きく減らない**（±3以内） | R20 が主因で、そこは触っていない |

**先に置く規律。**M6 が減っていたら、まず**検査を疑う**。第13.6版で 0→21 になった前科がある。

---

## 2. 買い手側の予測（盲検16体・`validate_stage136.py` 系）

| # | 予測 | 第13.6版の実測 |
|---|---|---|
| **B1** | 「着手が窓より前で順序が逆」は **0/4**（E1 の4体） | **出ていた**（これが A41b の的） |
| **B2** | 「決定期限が二つある／三か月ずれている」は **0/16** | E1-P1 で出ていた |
| **B3** | 日付そのものを理由にした棄却は **減る**が **0 にはならない** | 実現日・繁忙期の指摘は別系統 |
| **B4** | **⑥の通過は増えない。** ⑥棄却は 16/16 のまま | 主因は〈戻る額が空欄〉であって日付ではない |
| **B5** | 「営業へ回した空欄」は **16/16 のまま** | R20 の〈式〉を入れていない（次版） |

**B4 が今回いちばん重要な予測。**日付を全部直しても⑥は通らない、と先に書いておく。
**通ってしまったら、日付が主因だったことになり、`R20` の優先度を下げねばならない。**

---

## 3. 外れ方の解釈を先に決める

| 起きたこと | 読み方 | 次の一手 |
|---|---|---|
| M1・M2 が 0 でない | **指示が届いていない**（モデルではなく伝達） | 指示文の位置と語を変える。R12b と同じ処置 |
| M5 が増えた | (b) が裏目。窓を期限にしたせいで⑥が落ちた | 窓由来の期限は**停止ではなく要判断**に落とす |
| B1 が 0 でない | 窓の担体がまだ足りない（四例目） | `decision_gates` の条件（拘束者・上位・decision）を疑う |
| **B4 が外れて⑥が通った** | 日付が主因だった。**R20 の見立てが誤り** | 多業界の再走行を前倒しし、`R20`〈式〉を後ろへ |
| 停止が**減った** | まず**検査を疑う**。`check_s4_dates` の `tau_ok_dates` が空で黙っていないか | `A41B_S4_DATES_UNCHECKED` の件数を先に見る |

---

## 4. 走行前に済ませたこと（第13.5版 A37b の教訓）

- [x] **制約を足したら総当たり**：`feasible136.py` に `着手 ≥ 窓` を足し、**8セルとも解が在る**
- [x] **走らせる前に指示文を読む**：`regen_v13.py` の差分を全行読んだ。**二つ嘘を見つけて直した**
      ―― E1 は逆算が無いのに「この月数は**すでに引いてある**」と書いていた（2箇所）。A37b と同じ型
- [x] **新しい検査を実データに当てて検分**：`check_s4_dates` を第13版 as-run の8セルに当てて**誤検出 0/8**
      （本文全体に当てると **8/8 誤検出**だった。④に限ってはじめて 0。**浅い一致の6件目を踏む寸前だった**）
- [x] **回帰を先に書いて落とした**：15項目。実装前に `ImportError` で落ちることを確認済み
- [x] **as-run を上書きしていない**：`decisions8_v12.json` / `prompts8_v12_arm*.json` は無変更

---

## 5. 走らせ方

```
python3 regen_v13.py                 # 決定表と指示文（作成済み・走らせない）
Workflow({scriptPath: ".../wf_gen137.js"})   # ⑤⑥の生成（prompts8_v13_arm1 を読む）
python3 validate8_v12.py             # 機械側 M1–M7
stage137_build.py → wf_stage137.js   # 盲検買い手16体 B1–B5
```

**R2-P2 の店長・社長は第13.6版で欠けたまま。**16体を揃えること。
