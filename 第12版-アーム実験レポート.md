# 第12版 ── アーム3本の走行結果

走行 `wf_f298e86e-068`／24体（3アーム × 8セル、生成のみ）／エラー0・取り違え0・欠落0／1時間47分／158万トークン。

---

## 0. 先に、いちばん重い訂正

採点中に **検査そのもののバグ** が見つかった。`check_unit_presence` は宣言された単位を
⑥の本文と**完全一致**で照合していたが、生成器は `s2_unit` に「作業時間（時間）」のような
**説明句つきの文字列**を入れてくる。本文には「作業時間」と書いてあるのに、外す。

| | `R10b_UNIT_ABSENT` | うち誤検出 |
|---|---|---|
| 第12版の走行（3アーム計） | 15 | **14** |
| 第10版の走行 | 5 | **4** |

さらに `kept_unit` は `check_chain` の候補基底に「実務性」を足すかどうかを決めているので、
**この誤りは A16 の件数も押し上げていた。**

**第10版を、修正後の同じ物差しで測り直すと：**

| | 当時の報告 | 修正後 |
|---|---|---|
| stop 合計 | 25 | **13** |
| `A16_NOT_CONV_AT_SEAT` | 7 | **3** |
| `R10b`（ABSENT＋REPLACED） | 10 | **2** |

ここ数回の議論の中心だった「A16 が 7/8」「⑥の字数が足りない」は、**相当部分が測定の産物**だった。
「リスト単独 0/7 ／ リスト＋枚仕様 6/6」という対照も、正しくは **0/3 ／ 6/6** である。向きは変わらないが、根拠はずっと弱い。

これを **A25** として記録する。`N₄` は Qty = ⟨数, 単位, 基準⟩ と型を定めているのに、
宣言欄は文字列なので生成器が単位に説明句を混ぜてくる。**A24（費目の連結）とまったく同じ型の混同である。**
処置：`unit_tokens()` で正規化してから照合する（1文字の候補は落とす）。

### 失敗の所在に、4つ目がある

これまで 〈入力 ν〉〈モデル〉〈生成の指示〉の3つで切ってきた。**4つ目は〈検査〉である。**
機械が「落ちた」と言うとき、落ちていないのに落ちたと言っている場合がある。
買い手が同じ箇所で落としていれば気づけるが、A16 も R10b も**買い手には見えない内部検査**なので、
5走行のあいだ誰も気づかなかった。

---

## 1. 結果（すべて修正後の物差し）

| | stop | A16 | R10b | R10a | A23紙側 | A24 | R12b | stop無し | pass | ⑥字数 |
|---|---|---|---|---|---|---|---|---|---|---|
| 第10版 | 13 | 3 | 2 | 5 | ― | 2 | 1 | ― | 0/8 | 474 |
| **Arm 0** | 10 | 2 | 2 | 4 | 2 | 0 | 0 | 3/8 | 2/8 | 449 |
| **Arm 1** | **4** | **0** | **0** | 4 | **0** | 0 | 0 | **4/8** | **3/8** | 560 |
| **Arm 2** | 7 | 0 | 0 | 5 | 2 | 0 | 0 | 2/8 | 1/8 | 898 |

**Arm 1 の勝ち。** 残っているのは R10a だけ。

---

## 2. 判定

### ① 共通修正のうち2つは効いた

`R12b_START_AFTER_DEADLINE` 1 → **0**（着手期限を指示文に出した）
`A24_REALIZE_MERGED` 2 → **0**（費目を組の配列で申告させた）

### ② 字数の仮説は棄却された

⑥の字数分布が答えを出している。

```
Arm 0（上限450）  447 449 449 449 450 450 450 450   ← 全部が上限
Arm 1（上限450）  448 449 450 451 561 564 754 806   ← 半分が自分で破った
Arm 2（上限900）  885 889 894 895 903 905 908 911   ← 全部が新しい上限
```

**枠を与えると、枠まで書く。** Arm 2 は450を900に広げたのに成績が落ちた（stop 4 → 7、紙側 0 → 2）。
増えた450字は、必須要素ではなく別のことに使われた。

対して Arm 1 は、字数を一言も変えていないのに**必要な3セルだけが自分で上限を破った**（806／754／564）。
E1-P1 の self_report にこう書いてある——「⑥に必須とされた要素を全部入れると450字を物理的に超えるため、
**要素を落とすより字数の側を破った**」。

**要求を具体的に書けば、枠は生成器が勝手に調整する。枠を先に広げても、要求は満たされない。**

### ③ 申告欄は、指示より先に効く

予測では Arm 0 の座席申告は 0/8 のはずだった。**実測は 8/8。**
Arm 0 の指示文には座席ごとの要求が【書き方の制約】の1行しかないのに、全セルが申告している。

理由は出力スキーマである。`s6_kappa_by_seat` という**欄がスキーマに在る**だけで、生成器は埋めた。
`s3_form_mapping` が③で守られていたのと同じ機序で、**欄そのものが指示として働く。**

ただし Arm 0 は `A23_SEAT_WORD_ABSENT` が2件。**申告はしたが、本文には書いていない。**
Arm 1（枚仕様にも書き下ろした）は 0件。

```
欄だけ              → 申告する。本文には書かないことがある
欄 ＋ 枚ごとの仕様   → 本文にも書く
```

紙側の検査（A23_SEAT_WORD_ABSENT）を入れておかなければ、Arm 0 は「A16 が落ちた」と誤って読めた。

### ④ R10a はどのアームでも動かない ── 提示の問題ではない

4／4／5。指示文に載せても減らない。中身は2種類だった。

| 内訳 | 件数 | |
|---|---|---|
| s4=12か月, s6=12か月 | 4 | 年1回の問題に年額契約 |
| s4=12か月, s6=1か月 | 5 | 年1回の問題に月額課金 |

後者は、指示文（「**同じ周期で**同じ問題を作り直さない」）では許されるが、
検査（`s6 <= s4` ＝「同じ頻度**以上**」）では落ちる。**指示と検査の文言がずれている。**

より根本的に、扱っている商材は 3〜12か月契約と SaaS である。
`s6_period_months = 0`（単発）以外に R10a を満たす道がない。**現状の R10a は、この商材では充足不能かもしれない。**

比べるべきは課金周期ではなく〈**提案後に④の問題が残る周期**〉ではないか。
毎年来る問題を毎年止め続けるサービスは、問題を再生産していない。宣言欄が1つ足りない。
**→ A26 候補として次版へ。**

---

## 3. 予測との差

| | 予測 | 観察 | |
|---|---|---|---|
| arm0 stop | 17 | 10 | 外れ −7（A25 の誤検出を含んでいた） |
| arm0 A16 | 7 | 2 | 外れ −5（スキーマの欄で申告が入った） |
| arm0 座席申告 | 0/8 | **8/8** | **最大の外れ** |
| arm1 stop | 10 | 4 | 外れ −6 |
| arm1 A16 | 1 | 0 | ほぼ的中 |
| arm2 stop | 2 | **7** | 外れ +5（**方向が逆**） |
| arm2 A16 | 0 | 0 | 的中 |

**方向まで外したのは arm2 だけ。**「字数を広げれば良くなる」は誤りだった。

---

## 4. 次にすること

1. **Arm 1 を採用**し、`prompts8_v12.py` として確定させる。
2. **R10a を作り直す**（A26）。宣言に〈提案後に問題が残る周期〉を追加し、比較対象を課金周期から切り離す。指示文と検査の文言も揃える。
3. **第2段を走らせる** ── Arm 1 の生成物に予測者と盲検買い手を付ける（16体）。ここまでは「仕様を満たすか」しか測っていない。**満たした資料が実際に通るかは、まだ一度も測っていない。**
4. **既存の検査を A25 の目で洗い直す。** 文字列の完全一致で照合している箇所が他にないか。`check_seat_words` は複数候補を回しているので同型の穴はないが、`check_insult` の `d in v or v in d` などは要確認。

---

## 5. 回帰

`test_sales_logic.py` **97項目 全通過**（第10版時点の81 ＋ A24 5 ＋ 伝達漏れ 4 ＋ A23紙側 3 ＋ A25 4）。
`sales_logic.py` 1,200行、87μs/セル。`decisions8_v10.json` は 8/8 生成可を維持。

生の採点（A25 修正前）は `verified8_v11_RAW.json`、修正後は `verified8_v11.json` に残してある。
