# 第12.1版 ―― §4-1「A25 の目で既存検査を洗い直す」の修正予測

走る前に置く。外れたものをアノマリーとして扱う（引き継ぎ書 §5）。
基準は現状の採点 **arm0 stop 10 / arm1 stop 4 / arm2 stop 7**。

## 入れる修正（物差しの誤りが確定しているものだけ）

| # | 修正 | 根拠（実測） |
|---|---|---|
| F1 | `unit_tokens` に「の」区切りを足し、候補が空なら**停止でも通過でもなく要判断**へ | E2-P2 arm0：宣言が文（「担任1人あたりが年間に募集へ充てる日数（担任工数の日数）」）で照合語が取れず、⑥に「担任工数」も「日数」も在るのに R10b が発火。**A25 の取りこぼし** |
| F2 | `R10b_UNIT_ABSENT` と `R10b_UNIT_REPLACED` の二重発火をやめる | E2-P2 arm0：同じ一つの事実を2回数えている |
| F3 | ⑥が Σ に無ければ R10b を検査しない | 潜在（現8セルは全部⑥を持つ） |
| F4 | `check_v0` の `\bD[1-7]\b` を ASCII 境界へ（`\b` は日本語隣接で効かない） | 潜在。実本文への漏洩は 0件だが、**検査が走っていなかった** |
| F5 | 基準（κ）の連結を照合側で正規化。診断コード `A25_KAPPA_MERGED` を残す | E2-P1・R2-P2 arm0：`s6_kappa="価格・財源"`。指示文が「価格・財源」と4回書き、欄は単数。**A24・A25 と同じ型の第3例** |

## 予測（この5本を入れたあとの採点）

| | 現状 | 予測 | 内訳の予測 |
|---|---|---|---|
| arm0 stop | 10 | **6** | R10a 4 ＋ A23紙側 2（A16 2 と R10b 2 が消える） |
| arm1 stop | 4 | **4** | R10a 4（変化なし） |
| arm2 stop | 7 | **7** | R10a 5 ＋ A23紙側 2（変化なし） |
| arm0 A16 | 2 | **0** | 連結の正規化で消える |
| arm0 R10b | 2 | **0** | F1・F2 で消える |
| EXPR_TABLE_MISS | 2 | **0** | 同上 |
| 回帰テスト | 97 | **97 ＋ 新規** 全通過 | 既存項目は1つも落とさない |

## この予測が当たった場合に言えること

- **アーム間の差の実体が変わる。** 「Arm 1 は A16 を消した」ではなく、
  「A16 は物差しの誤りで、アーム差に残るのは **A23 の紙側（申告はするが本文に書かない）だけ**」になる。
  引き継ぎ書 §3 の「欄だけ → 申告する／欄＋枚仕様 → 本文にも書く」は**生き残り、むしろ唯一の根拠になる**。
- 逆に「Arm 1：stop 10→4」という**幅は誇張だった**ことになる（6→4 が正しい）。

## 入れない修正（モデルの意味の決定なので人間へ回す）

| # | 内容 | 採点への影響（実測） |
|---|---|---|
| G1 | `check_seat_words` が座席の由来を見ていない。Form が閉じているのは**制度由来だけ**（SPEC §10.1）なのに、個人・組織由来の座席にも様式語の一致を要求している | 発火4件すべてが非制度座席。直すと **arm0 4 / arm1 4 / arm2 5** ＝ **アーム差が消える** |
| G2 | 生成後検査が `EXPR_OK`（較正表）を使い、`expr_ok_of(商材座標)` を使っていない。R1-P1・R2-P1 では表が現に相違（実務性↔価格が座標では開く） | 今回の24件では 0/24。潜在 |
| G3 | `check_insult` が `∃χ∈Γ^own` を検査していない（`hit` を計算して捨てている）。Γ^own={} のセルでも停止する | 24件とも `s5_denies_own=""` なので未発火。潜在 |
| G4 | `Nu.A` と `prod.nu` の二重の真実。R1-P1・R2-P1 で食い違い、点灯ブロックが変わっている | 生成前に実害あり（B_case_numbers が落ち、B_mechanism/B_certification が立っている） |
