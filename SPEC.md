# 営業資料生成モデル 第7版 ―― 中心命題・軸・形式系

CLAUDE.md に据える骨格制約と、その形式化を1本にまとめたもの。実装は `sales_logic.py`。
対象は業界・商材横断の**基本骨格のみ**。低関与・反復購買（オフィス用品型）は §3.2 で対象外に落ちる。

---

## 0. 直してきた履歴

### 第5版（医療・物流 8セル）

| # | 症状 | 直した場所 |
|---|---|---|
| A1 | ④の量が最終裁定点の通貨に両替できない | **§1.5 の適用範囲。**通貨は⑤限定ではなく階段全体を貫く |
| A2 | ⑤はカテゴリまでしか絞れないのに⑥で自社が要る | **⑥の推論規則。**所属を含意と偽っていた。⑤を強化し **D7** を新設 |
| A3 | R7 が真偽しか要求していない | **世界の述語と承認条件の分離。**形式系全体に効く |
| A4 | ⑥が④の問題を反復させ、②の単位を戻す | **§1.1 に無矛盾性。**R10 はその特殊ケース |

### 第6版（食品・建設 8セル）

8セルが**全部⑥で落ちた**。8人が同じことを言った——「金額に直して持ってこい」。

| # | 症状 | 再現 | 直した場所 |
|---|---|---|---|
| **A5** | ⑥に通貨の条件がない | 8/8 | **推論規則 (6)。**A1 を④にしか実装していなかった。§1.5 の直し残し |
| **A6** | R10b を守ると A1 を破る | 2/8 | **R10b の禁止対象。**禁じるのは単位の**置換**であって**併記**ではない |
| **A7** | 消去次元が座席構造と無関係に立つ | 3/8 | **D軸。**$\delta$ が向きのない集合を返していた。拘束者を伴う関係へ |
| **A8** | ④の日付が「この買い手に効く日か」を見ていない | 3/8 | **T軸。**出典階層は「誰が決めた日か」しか見ていなかった |
| **A9** | ③で名づけた対象と⑥が消す対象の同一性検査がない | 3/8 | **推論規則 (6)。**外延の一致が仮定されているだけだった |
| **A10** | 縮退で段を落とすと残った段の前提が未定義 | 2/8 | **$\Sigma$ の定義。**落とした段の $\Gamma$ の供給元がなかった |
| **A11** | 名づけ（③）が移送で消える | 2/8 | **§1.2。**減衰するのは承認だけではない |

### 第7版（同じ8セルの再走行）

第6版の修正は効いた。上申は 0/8 → 3/8。⑥に金額が載り、「金額に直して持ってこい」は**一度も出なかった**。
代わりに8人全員が、一段深いところで同じ問いを残した。

| # | 症状 | 再現 | 直した場所 |
|---|---|---|---|
| **A12** | 両替**できる**ことと、両替**される**ことは別 | 8/8 | **§1.7 を新設。**$J$ に執行座席 $W$ を追加。中心命題の変更 |
| **A13** | ④の周期と⑥の量の型（ストック／フロー）が合わない | 3/8 | **R14。**R10a の対称形 |
| **A14** | ⑤で他手段に課した期限を、⑥の自社案が満たしていない | 2/8 | **R12 を資料全体へ。**$\Gamma_5 \cup \{P\} \nvdash \bot$ の具体形 |
| **A15** | 「御社データ」の実在が検査されていない | 4/8 | **T軸に受領記録** |

---

## 1. 中心命題

### 1.1 合意は、無矛盾な資本である

論証は買い手が無料で差し出せるものから始まり、有料のもので終わる。各段の仕事はただ一つ、ある命題を「係争中・不可視」から「承認済み」へ変換し、その承認を次段の前提として供出すること。

$$\Gamma_s = \Gamma_{s-1} \cup \{\phi_s\}, \qquad \max_{\phi \in \Gamma_{s}} \mathrm{cost}(\phi) \;\leq\; \max_{\phi \in \Gamma_{s'}} \mathrm{cost}(\phi) \quad (s < s')$$

**消費財ではない**とは、$\Gamma$ が contraction をもつこと（承認は何度使っても減らない）。線形論理ではない。

**そして無矛盾でなければならない。**（第5版の追加）

$$\boxed{\;\Gamma_s \nvdash \bot \quad (\forall s)\;}$$

単調増加だけを言って無矛盾性を要求しないのは、古典論理では致命的である。爆発律により、⑥が①〜⑤のどれかと矛盾した瞬間に**承認が全額失効する**。買い手の「9,282万円という数字まで付けてある分、余計に受け取れない」はこれである。

### 1.2 承認は減衰しながらしか伝播しない

減衰率は段数ではなく**基準の異なる裁定点の個数**で決まる。

$$T_{k \to k+1}(\Gamma) = \{\, \phi \in \Gamma : \mathrm{expressible}(\phi, \kappa_{k+1}) \wedge \mathrm{valid}(\phi, \chi_{k+1}) \,\}$$

$\kappa$ が変わると**両替所の強制通過**、$\chi$ が変わると**切断**。

$$\text{承認が届く} \iff \bigl(T_{n-1\to n} \circ \cdots \circ T_{1\to 2}\bigr)(\Gamma_6) \vdash_{\gamma_n} \mathrm{adopt}(P)$$

**減衰するのは承認だけではない。語彙も減衰する。**（第6版の追加 / A11）

第5版まで、$T_{k\to k+1}$ は $\Gamma$ の**命題**を濾す関数として書かれていた。実際には、命題を運ぶ**語**が先に落ちる。

$$T^{\mathrm{lex}}_{k \to k+1}(L) = \{\, \ell \in L : \ell \in \mathrm{Form}(j_{k+1}) \,\}$$

$\mathrm{Form}(j)$ は座席 $j$ の**文書様式に既にある語**（科目名・様式の欄名・会議で通る語）である。③で与えた新語 $\ell_Q$ は定義上どの $\mathrm{Form}$ にも無いので、$n \geq 2$ なら必ず落ちる。

$$n \geq 2 \;\Longrightarrow\; \ell_Q \notin \mathrm{Form}(j_n)$$

買い手はこれを正確に言う——「社内の会話では使う、対外文書では使わない」「その言葉は、うちの会議じゃ通らん」。**内容は承認されているのに、語が運べない。** 承認の減衰（$\kappa$ の不一致）とは別の機序であり、$\kappa$ が一致していても起きる。

したがって③には、新語と**$\mathrm{Form}(j_n)$ に既にある語との対応**を併記する条件が課される（R11）。新語は起案者の座席までの作業言語であって、最終裁定点まで運ぶ通貨ではない。

### 1.3 論証を完結させた者が、結論の責任を負う

$$\Delta\mathrm{resp}(x) = 0 \iff x \in \mathrm{judges}(J)$$

$$\mathrm{gap}(w_6) = \top \text{ が許容される} \iff n = 1$$

エンテュメーメは廃止されない。**一寸の位置が、責任を元々負っている座席へ移る。**

### 1.4 否定は内から、暦は外から

②の欠如は買い手の世界の内から引き出す。$\mathrm{facts}_2 \subseteq \mathrm{facts}_1$。

④の「今」は買い手の世界の外から持ち込む。外部性の判定は4条件。

$$\neg\mathrm{controllable}(d,\text{buyer}) \;\wedge\; \neg\mathrm{revocable}(d) \;\wedge\; \exists q.\,\mathrm{uncomputed}(q,\text{buyer}) \;\wedge\; \mathrm{applies}(d,\text{buyer})$$

**第4条件は第7版の追加（A8 を中心命題へ引き上げる）。** 暦は外から来ただけでは足りない。**その買い手に効いていなければならない。** 第6版は A8 を §2.2・§3.3 の形式系にだけ置き、§1.4 は「外部性の判定は3条件」のまま残していた。これは A5（枠は $\forall s$ なのに実装が④だけ）と逆向きの同じ誤りで、**形式系にあって中心命題にない条件**である。

**日付のない「今」は「今」ではない。既知の日付だけの「今」も「今」ではない。他人に効く日付も「今」ではない。**

### 1.5 通貨は、階段全体を貫く（第5版の改訂）

第4版は「**⑤の**失敗は通貨の取り違えである」と書いた。**「⑤の」が誤りだった。**

- **①** 買い手が頷ける事実は、その座席の $\kappa$ で表現できるものに限られる
- **②** 「別の単位で数え直す」とは、**通貨の変換そのもの**である
- **③** 名づけも $\kappa$ 依存（「配置の探索コスト」は実務性、「給与費比率」は財源）
- **④** 量が $\kappa_n$ で表現できなければ、最終裁定点に届かない（A1）
- **⑤** 証拠が $\delta$ に合わなければ支払えない
- **⑥** **提案そのものが $\kappa_n$ で表現できなければ、④までの承認は全部無駄になる**（A5・第6版）

$$\boxed{\;\forall s.\ \forall \phi \in \Gamma_s.\ \mathrm{expressible}(\phi, \kappa_n)\;}$$

**第5版はこの枠を書きながら、形式化は④にしか置いていなかった。** 8セル全部が⑥で落ちたのはそのためである。枠が正しく、実装が枠より狭かった。$\forall s$ の $s$ には $6$ が入っている。

**誤った通貨は、単に通じないのではなく侮辱として届く。** これは選好 $\prec$ 上の後退であって、偽なる主張ではない。

**単位の保持と両替は排他ではない。**（A6・第6版）R10b は「②で数え直した単位を⑥で元に戻す」ことを禁じるが、これを「⑥で $\kappa_n$ の量を書くな」と読むと A5 と正面から矛盾する。禁じられるのは単位の**置換**であって**併記**ではない。②の単位を主として保持したまま、$\kappa_n$ の量を並置する——これが両替所の正しい通り方である。

### 1.6 世界について真であることと、承認されていることは別（第5版の追加）

形式系のいくつかの条件は「世界について真」の形で書かれていたが、階段が扱うのは $\Gamma$ に入っているかである。

$$\underbrace{\neg\mathrm{blocked}(\delta(M_i), \mathsf{seller}, Q)}_{\text{世界について真}} \qquad\neq\qquad \underbrace{\Gamma_6 \vdash \neg\mathrm{blocked}(\delta(M_i), \mathsf{seller}, Q)}_{\text{買い手が承認}}$$

**売り手が拘束に掛かっていないことが真でも、買い手がそれを承認していなければ $\Gamma$ には入らない。** これが R7 が実数を要求する理由であり、$\mathrm{blocked}$・$\mathrm{allowed}$・$\mathrm{expressible}$ すべてに同じ区別が要る。

### 1.7 両替できることと、両替されることは別（第7版の追加）

第6版で $\mathrm{expressible}(P,\kappa_n)$ を⑥に入れた。8枚すべてに金額と回収年数が載り、前回8人が言った「金額に直して持ってこい」は一度も出なかった。**代わりに8人全員が、同じ一段深い問いを残した。**

> 人日が110人日減っても、要員表から落とすか、残業・応援手当・外注加工費の支払いを実際に止めない限り、加工単価は1円も動かない。**人が浮くだけで支払いは減らない、というのが現場で起きる普通の結末だ。**
> うちは人を減らさん。減らさん以上、**給料は一円も減らんのだ。**
> 480時間が浮いても、**金は一銭も浮かんのや。**——で、その浮いた手待ち、**誰が返しに来るんや。**

$\mathrm{expressible}$ は**写像の存在**を言うだけで、写像が**適用される**ことを言っていない。

$$\mathrm{expressible}(q, \kappa_n) \;\nRightarrow\; \mathrm{realizable}(q, \kappa_n)$$

$$\mathrm{realizable}(q,\kappa_n) \equiv \exists (w, d, a).\ w \in W \wedge \mathrm{authority}(w, a) \wedge \mathrm{act}(w, d, a) \wedge \mathrm{reduces}(a, \kappa_n)$$

**両替所は表があるだけでは機能しない。窓口に立って両替する者が要る。** $w$ は座席、$d$ は日付、$a$ は費目（要員／残業手当／応援労務費／外注加工費／常用の人工／請求）である。この三つ組が書かれていなければ、⑥の金額は**算術としては正しく、事実としては起こらない**。

**そして $W \not\subseteq \mathrm{set}(J)$ である。**（第7版の中心的な変更。$J$ は列なので、集合演算には要素の集合 $\mathrm{set}(J) = \{j_1,\dots,j_n\}$ を使う）

> 工場長は人を減らさない、調達本部は人の話を聞かない。**この間を埋める人間がこの資料のどこにも出てこない。**

第2版以来、$J$ は〈承認を裁定する座席の列〉として定義されてきた。しかし合意が資本であるためには、**その資本を実際に取り崩す座席**が要る。裁定と執行は別の座席であり、執行座席は裁定列に現れないことがある。

$$J = \langle j_1,\dots,j_n\rangle \quad\text{（裁定）}, \qquad W = \{w : \mathrm{authority}(w,a),\ a \in \mathrm{Acct}\} \quad\text{（執行）}$$

$$\mathrm{adopt}(P) \wedge W = \emptyset \;\Longrightarrow\; \Delta q_{\kappa_n} = 0 \qquad (q_{\kappa_n} = \text{基準 } \kappa_n \text{ で測った量})$$

$\kappa_n$ は基準の**集合**なので、それ自体に差分は取れない。動くのはその基準で測った量 $q_{\kappa_n}$ である。

**承認されても、$W$ が空なら $\kappa_n$ は動かない。** これは $V$（決裁権なき拒否権）の鏡像である。$V$ は決めないが止められる座席、$W$ は決めないが**動かす**座席。どちらも $J$ の外にいて、どちらも欠ければ取引は成立しない。

$$\text{取引の成立} \iff \mathrm{adopt}(P) \;\wedge\; \forall v \in V.\ \neg\mathrm{veto}(v) \;\wedge\; W \neq \emptyset$$

---

## 2. 階段と層

### 2.1 推論規則

> 数式内では段を $1$〜$6$ と表記する（本文の①〜⑥に対応）。丸数字は数式フォントに存在しないため。

$$\frac{}{\Gamma_1 = \{S\}}\;(1)\ \qquad \frac{\Gamma_1 \vdash S \quad \mathrm{facts}_2 \subseteq \mathrm{facts}_1}{\Gamma_2 = \Gamma_1 \cup \{\Diamond\neg\mathrm{suff}(S)\}}\;(2)$$

$$\frac{\Gamma_2 \vdash \Diamond\neg\mathrm{suff}(S) \quad \mathrm{diff}(Q, \mathrm{Lex}(\text{buyer})) \neq \emptyset}{\Gamma_3 = \Gamma_2 \cup \{Q\}}\;(3)$$

$$\frac{\Gamma_3 \vdash Q \quad \mathrm{real}(Q) \quad \mathrm{persist}(Q) \quad \mathrm{now}(Q)}{\Gamma_4 = \Gamma_3 \cup \{\Box\,\mathrm{address}(Q)\}}\;(4)$$

$$\frac{\Gamma_4 \vdash \Box\,\mathrm{address}(Q) \quad \forall M_i \in M\setminus\{M_0\}.\ \Gamma_4 \vdash \mathrm{blocked}(\delta(M_i), M_i, Q) \wedge \mathrm{allowed}(\delta(M_i), \mathrm{type}(M_i)) \quad |C| = 1}{\Gamma_5 = \Gamma_4 \cup \{\forall x.\,\Box(\mathrm{resolve}(x,Q) \to x \in C)\}}\;(5)$$

$$\frac{\Gamma_5 \vdash \forall x.\,\Box(\mathrm{resolve}(x,Q) \to x \in C) \quad p \in C \quad |C| = 1 \quad \Gamma_5 \cup \{P\} \nvdash \bot \quad \mathrm{expressible}(P, \kappa_n) \quad \mathrm{realizable}(P, \kappa_n) \quad \mathrm{ext}(\mathrm{covered}(P)) \subseteq \mathrm{ext}(Q)}{\Gamma_6 = \Gamma_5 \cup \{P\}}\;(6)$$

> **記号の分離（第7版の検証）。** $p$ は手段（項）、$P = \mathrm{resolve}(p, Q)$ はそれについての命題である。$p \in C$・$\mathrm{LT}(p)$・$\psi[p/M_i]$ は項として、$\Gamma_6 = \Gamma_5 \cup \{P\}$・$\nvdash \bot$・$\neg P$ は命題として扱う。初出時この二つを同じ $P$ で書いていた。

**⑤⑥の $|C| = 1$ が第5版の変更点。** $|C| > 1$ なら $P$ は含意されておらず、導けるのは所属だけである。$|C|=1$ は**世界についての条件**であって無条件に要る。それを買い手が**承認しているか**（$\Gamma \vdash |C|=1$）が問われるのは、買い手がカテゴリ内を比較する局面だけである——§1.6 の区別がここにも当たる。A2 が $B_1$ と比較検討中に限定しているのはこの意味であり、規則の側条件を緩めているのではない。「これ、御社じゃなくても成り立つ話だよね」は買い手がこの欠陥を正確に指摘した言葉であり、**⑤を強化して $C$ を単元集合まで絞る**ことで中心命題を守る。

**(6) の後半2条件が第6版の変更点。**

$\mathrm{expressible}(P, \kappa_n)$（**A5**）。提案が最終裁定点の基準で読めなければ、④までに積んだ承認は $j_n$ へ渡らない。8セルで例外なくここが落ちた。

$\mathrm{ext}(\mathrm{covered}(P)) \subseteq \mathrm{ext}(Q)$（**A9**）。**$P$ が消す集合は、③で名づけ④で数えた $Q$ の部分集合でなければならない。** 第5版までこれは仮定されているだけだった。買い手の指摘が正確である——「この装置が消すのは定常作業であって、②で数えたのは立ち上がりの上乗せだ。**数えた人日と装置が置き換える人日が同じ集合だという説明がどこにもない**」。単位が合っていても外延が違えば、$P$ は $Q$ を解消しない。$\mathrm{covered}(P) \subsetneq Q$ のときは**被覆率を⑥で開示する**（対応表を必須点灯）。

$\mathrm{realizable}(P, \kappa_n)$（**A13 ではなく A12**・第7版）。§1.7 のとおり、$\kappa_n$ で書けることと $\kappa_n$ が動くことは別である。⑥に〈執行座席・日付・費目〉の三つ組がなければ (6) は導けない。

**$\Gamma_5 \cup \{P\} \nvdash \bot$ の具体形として、自己適用が加わる**（**A14**・第7版）。⑤で $M_i$ を落とすのに使った条件は、$P$ 自身にも課される。

$$\forall M_i \in M\setminus\{M_0\}.\ \forall \psi \in \mathrm{grounds}\bigl(\mathrm{blocked}(\delta(M_i), M_i, Q)\bigr).\ \Gamma_6 \vdash \neg\psi[p/M_i]$$

> **第7版の検証で直した。** 初出時この式は $\Gamma_6 \nvdash \neg\psi[p/M_i]$（自社が同じ試験を通ることを**証明してはならない**）と書かれており、直後の本文・R12b・§5⑥ の三つすべてと反対を向いていた。要求すべきは $\vdash$ である。$\mathrm{blocked}$ は3項述語なので $\mathrm{grounds}$ の引数も3項で書く。

期限で他手段を落としたなら、自社の着手日が同じ期限を通ること。責任分界の欠落で落としたなら、自社の契約に当該条項があること。**落とすのに使った刃は、自分にも当たる。** 買い手はこれを一行で言う——「40日しかないという話で始まって、着手が11月末になってるでしょう」。

### 2.2 now(Q) の展開

$$\begin{aligned} \mathrm{now}(Q) \equiv \exists (f,d,s,k,q) \in \tau.\ & d > t_{\mathrm{gen}} && \text{R6a 未来性} \\ \wedge\ & d - \mathrm{LT}^{*}(P) \geq 0 \quad (f \in \{A,C\}) && \text{R6b リードタイム整合} \\ \wedge\ & \Box(\mathrm{at}(d) \wedge \neg P \to \mathrm{loss}) && \text{R6c 因果接続} \\ \wedge\ & s \neq \text{売り手都合} && \text{出典階層} \\ \wedge\ & (k = \text{未知} \vee \exists q.\ \mathrm{uncomputed}(q,\text{buyer})) && \text{既知度} \\ \wedge\ & \mathrm{expressible}(q, \kappa_n) && \textbf{A1（第5版）} \\ \wedge\ & \mathrm{applies}(d, \text{buyer}) && \textbf{A8（第6版）} \\ \wedge\ & (f \neq D \vee \exists (f',d',s',k',q') \in \tau^{\mathrm{ok}}.\ f' \in \{B,C\}) && \text{T-D単独禁止} \end{aligned}$$

$\mathrm{LT}^{*}(P) = \mathrm{LT}(P) + \max\bigl(\{0\} \cup \{\mathrm{acquire}(q) : (f,d,s,k,q) \in \tau\}\bigr)$（買い手データの取得月数を加算）。単位元 $0$ を入れておく——買い手データを一件も要しない提案で $\max$ が空集合上になると $\mathrm{LT}^{*}$ が未定義になり、R6b が判定不能になる。
R6b は終端日型（A/C）にのみ適用する。B は窓の開閉、E は解禁日で、いずれも既に行動期限だから二重計上になる。

**$\mathrm{applies}$ の展開（A8）。** 出典階層は「**誰が決めた日か**」を見る述語であって、「**誰に効く日か**」を見ていない。第5版まで、この二つが混同されていた。

$$\mathrm{applies}(d,\text{buyer}) \equiv \mathrm{scope}(d) \ni \text{buyer} \;\wedge\; \bigl(\mathrm{binder}(d) = \bot \vee |\mathrm{binder}(d) \cap \mathrm{Upstream}(\text{buyer})| = 1\bigr)$$

- $\mathrm{scope}(d)$ ―― その日付が効く範囲（業種・規模区分・許可区分・適用開始年）。**制度の適用開始日と、当該買い手への適用開始日は別である。** 猶予・経過措置・規模別適用がある制度で、売り手はほぼ必ずここを外す。
- $\mathrm{binder}(d)$ ―― その日を握っている当事者。契約由来・第三者承認由来の日付は、**どの当事者の日か**が一意でなければ日付として機能しない。「うちは元請が複数あって、審査の窓もばらばらだ」は $|\mathrm{binder} \cap \mathrm{Upstream}| > 1$ の状態である。

$\mathrm{scope}$ に外れた日付は $\tau$ から**棄却**する（$M_0$ 消去力を持たないどころか、①の信用まで落とす）。$\mathrm{binder}$ が一意でない場合は**当事者を同定するまで④は立たない**（人間記入スロットへ回す）。

### 2.3 層

| 層 | 担当 | 実行 |
|---|---|---|
| **層1 構造** | $\Sigma$、$\mathrm{on}(b)$ | コード（生成前） |
| **層2 主張型** | 推論規則、$\mathrm{now}$、$\delta$ 照合 | コード（生成前） |
| **層2.5 機械判定** | R1〜R8、A1、A2 | コード（生成前） |
| **層3 語彙** | 買い手の文化に合わせた書き分け | LLM |
| **層3.5 提示変換** | R9 禁止語彙、段ごとの提示形態、R10 整合 | コード（生成後） |

**形式系は生成器に渡さない。** $V_0 = \mathrm{Vocab}(層1) \cup \mathrm{Vocab}(層2)$ を出力から排除するのに、それをプロンプトに入れては漏れる。LLMに渡すのは導出の**結果**（この段では何を書け／何を書くな）だけ。

---

## 3. 軸

### 3.1 入力の層

| 層 | 誰が | 項目 |
|---|---|---|
| **商材軸** | マーケター（SKU登録時1回） | $A$ 効果が分かる時点／$I$ 回収年数／$S_1$ 単価帯／$S_2$ 反復性／$S_3$ 制度手続経由／$C_{\text{move}}$ 移行の重さ |
| **顧客軸** | 営業（顧客登録時1回） | $J$ 裁定点の列／$V$ 拒否点／procedural／downward／seg |
| **商談軸** | 営業（商談ごと） | $E_r$／$E_j$／$\tau$／$\delta$／②の帰責先 |

$$J = \langle j_1,\dots,j_n\rangle, \quad j_k = (\kappa_k, \chi_k, \gamma_k, \omega_k, \mathrm{reads}_k), \quad \kappa_k \subseteq \{\text{実務性},\text{財源},\text{説明可能性},\text{価格},\text{政治的可視性}\}$$

**$\kappa$ は集合である**（第5版）。一人の裁定者が複数の基準を持ちうる（理事長は医師かつ経営者）。

$$V = \{v : \text{決裁権はないが実質的に事業を止められる}\}, \qquad \mathrm{adopt}(P) \text{ の条件に } \forall v \in V.\ \neg\mathrm{veto}(v)$$

**$W$ を新設する**（第7版 / A12）。

$$W = \{w : \exists a \in \mathrm{Acct}.\ \mathrm{authority}(w, a)\}, \qquad \mathrm{Acct} = \{\text{要員}, \text{残業手当}, \text{応援労務費}, \text{外注加工費}, \text{常用の人工}, \text{請求}, \dots\}$$

$W$ は**決めないが動かす**座席の集合である。$V$ が「決裁権はないが止められる」であるのに対し、$W$ は「決裁権はないが、費目を実際に減らす権限を持つ」。三つの集合の関係は次のとおりで、いずれも他の二つに含まれない。

| 集合 | 持つもの | 欠けると |
|---|---|---|
| $J$ 裁定 | 承認を与える権限 | $\Gamma$ が積み上がらない |
| $V$ 拒否 | 事実上の停止力 | 買っても使えない |
| **$W$ 執行** | **費目を減らす権限** | **承認されても $\kappa_n$ が動かない** |

営業は $J$ を聞き取り、$V$ をときどき聞き取り、$W$ を**まず聞かない**。第6版の8セルはすべて $W$ が空欄のまま生成され、8人全員がそこを突いた。$W$ は顧客軸の必須入力である。

### 3.2 縮退

$$\sigma_{\mathrm{prod}}(\nu) = \begin{cases} \{1,\dots,6\} & S_3 = \text{あり} \\ \{1,6\} & \neg S_3 \wedge S_2 \geq \text{四半期} \wedge S_1 < 100\text{万} \quad\textbf{← 本モデルの対象外} \\ \{1,4,6\} & \neg S_3 \wedge S_2 \leq \text{年次} \wedge S_1 < 100\text{万} \\ \{1,\dots,6\} & \text{otherwise} \end{cases}$$

$$\Sigma = \begin{cases}\sigma_{\mathrm{prod}}(\nu) & \sigma_{\mathrm{prod}}(\nu) \neq \{1,\dots,6\} \\ \sigma_{\mathrm{read}}(E_{j^*}) & \text{otherwise}\end{cases}, \qquad j^* = \max\{\,k : \mathrm{reads}_k \,\}$$

$j^*$（$\sigma_{\mathrm{read}}$ 用）と $j_n$（$\kappa_n$ 用）は**別の座席**である。

低関与・反復購買（②の欠如が自明で、⑤が買い手の過去の決定への評価になる商材）は、$\{1,6\}$ に落ちた時点で**本モデルの対象外**として扱う。

> **$\sigma_{\mathrm{prod}}$ の位置づけ（第7版の検証）。** $\sigma_{\mathrm{read}}$ が段を落とせる根拠は $\Gamma^{\mathrm{pre}}$ の存在という §1.1 由来の条件だが、$\sigma_{\mathrm{prod}}$ の閾値（100万円・四半期・年次）に理論的根拠はない。これは $\Gamma^{\mathrm{pre}}$ の存在を予測する**ヒューリスティック**であって、中心命題から導かれた条件ではない。閾値の較正は経験に委ねる。

#### 縮退後の定義域（A11 とは別 / A10・第6版）

$\Sigma$ は**書く段**の集合であって、**成立している段**の集合ではない。第5版はこの区別を書いておらず、$\Sigma$ から落ちた段の $\Gamma$ をどこから得るかが未定義だった。推論規則 (2) の側条件 $\mathrm{facts}_2 \subseteq \mathrm{facts}_1$ は $\Gamma_1$ の存在を前提にしているので、$1 \notin \Sigma$ のとき側条件の定義域が空になる。実際、①を落としたセルの②は「①でお示ししたのは」と、存在しない枚を参照して始まった。

$$\Gamma_{s}^{\mathrm{init}} = \begin{cases} \emptyset & s = 1 \in \Sigma \\ \Gamma_{s'} & s' = \max\{s'' \in \Sigma : s'' < s\} \text{ が存在} \\ \Gamma^{\mathrm{pre}}_{s-1} & \text{otherwise} \end{cases}, \qquad \Gamma^{\mathrm{pre}}_{s} = \text{買い手が資料の外で既に承認している } \Gamma_s$$

**穴は先頭だけでなく内側にも空く。** $\sigma_{\mathrm{prod}}$ は $\{1,4,6\}$ を返しうる。このとき $\min\Sigma = 1$ なので「$\min\Sigma > 1$ のとき」という条件では発火せず、②③⑤ が抜けたまま $\Gamma_3 \vdash Q$（規則 4 の前提）と $\Gamma_5 \vdash \forall x.\,\Box(\cdots)$（規則 6 の前提）の供給元が定義されない。**$\Gamma^{\mathrm{pre}}$ は $\Sigma$ から落ちた全段について要求する。**

**落とした段は消えるのではない。資料の外で既に成立しているものとして、明示的に与える。** $\sigma_{\mathrm{read}}$ が段を落とせるのは $E_{j^*}$ が「もう分かっている」と言っているからであり、それは $\Gamma^{\mathrm{pre}}$ が存在するという主張にほかならない。$\Gamma^{\mathrm{pre}}$ を書けないなら、その段は落とせない。

同時に、**宣言（Declared）の定義域も縮む。** $2 \notin \Sigma$ のとき $s_2\text{-}\mathrm{unit}$ は「空文字」ではなく**未定義**であり、R10b の検査対象から外れる。0 と未定義を同じ値で表してはならない（同じ型の誤りで R10a が6件を誤検出した）。

### 3.3 T軸

| 形式 | 必須の日付 | $M_0$ 消去力 |
|---|---|---|
| A 閉扉 | 終端日 | 不可逆 |
| B 開扉 | 次の窓の開閉日＋**逃した場合の待ち時間** | 機会喪失 |
| C 逆算 | 終端日／LT／**着手期限日** | 機会喪失 |
| D 漸増 | **次の段の日付**＋増分 | コスト逓増のみ（単独禁止） |
| E 禁制 | 型別（a購買行為／b実行稼働／c第三者承認／d恒常） | 不可逆 |

各元は **出典階層**（法令／公的暦／自然・需要／契約／売り手都合＝生成禁止）と**既知度**を必須フィールドに持つ。既知の暦は日付単独では使えない。

**第6版で必須フィールドを2つ追加する（A8）。**

| フィールド | 内容 | 空欄のときの扱い |
|---|---|---|
| **適用対象** $\mathrm{scope}(d)$ | その日付が効く業種・規模区分・許可区分と、**当該区分への適用開始年** | 棄却（生成に入らない） |
| **拘束者** $\mathrm{binder}(d)$ | 契約・第三者承認由来の日付を握っている当事者。法令・公的暦由来なら $\bot$ | 一意でなければ人間記入スロット |
| **受領記録** $\mathrm{receipt}(q)$（第7版 / A15） | $q$ を「買い手データ」と名乗るときの、**誰から・いつ・どの抽出条件で**受け取ったかの記録 | 「買い手データ」を名乗れない |
| **量の幅** $[q_{\min}, q_{\max}]$ | 未計算の量の推定幅。**$q_{\min} \leq 0$ は不可**（幅が④の成立を跨ぐ量は、あってもなくてもよいことになる） | 停止 |

$\mathrm{binder} \neq \bot$ のとき、$\mathrm{scope}$ の粒度は**その拘束者が使っている区切り**に一致しなければならない。$\mathrm{binder} = \bot$（法令・公的暦）のときは、**制度の条文自身が使う区分**で書く。 第6版の実装は買い手属性（業態・規模）で照合したが、買い手はそこを正確に突いた——「第三者認証の更新期は事業所ごとの登録日起算で決まる。**300〜999名という区切りは認証機関が使う区切りではない**」。適用対象の粒度は、その日を握っている当事者が使っている粒度に一致しなければならない。

$\mathrm{receipt}$ が空欄の $q$ を「御社データ」と書くと、買い手は**検算ではなく読解だけ**で落とす——「うちの部からデータを出した記憶がない。誰が、いつ、どのデータを渡したのか」。4/8 で発生した。さらに悪い形として、②で「その粒度のデータは存在しない」と書いた資料が④で同じ粒度の数を出す**出所の循環**があった。$\mathrm{receipt}$ を必須にすれば両方止まる。

出典階層と適用対象は**別の軸**である。出典が「法令」でも、その法令が当該買い手に適用されていなければ④は立たない。第5版までこの二つが同じ欄に潰れていたため、施行済み・猶予終了済みの制度を「今」として出す誤りが機械的に止まらなかった。

$\tau$ が複数の元を持つとき、**元どうしの順序**も検査する（R12）。C の着手期限日は、同じ $\tau$ にある A の終端日より後にあってはならない。

### 3.4 D軸（第5版で D7 を新設）

$$\delta : M\setminus\{M_0\} \to \mathcal{P}\bigl(\{D_1,\dots,D_5, D_{6a},D_{6b},D_{6c}, D_{7a},\dots,D_{7d}\} \times (\mathcal{A} \cup \{\bot\})\bigr)$$

**$\delta$ の値は集合である**（第5版）。1つの $M_i$ に複数次元が同時に成立しうる。主従の順序を持つ。

**$\delta$ の値は〈次元, 拘束者〉の対である**（第6版 / A7）。第5版の $\delta$ は向きのない集合を返していたので、拘束が**誰から誰へ**かかるかを持てなかった。D6 系は本質的に向きを持つ関係であり、向きを落とすと成立しない主張が機械的に通ってしまう。

$$\forall M_i \in M\setminus\{M_0\}.\ \forall (D, a) \in \delta(M_i).\ D \in \{D_{6a}, D_{6c}\} \;\Longrightarrow\; a \in \mathcal{A} \;\wedge\; \mathrm{above}(a, \text{buyer})$$

拘束者は**対の第2成分として束縛する**。第6版はこれを $\exists a$ と書いたため「上位の誰かがどこかにいる」しか言えておらず、$\delta(M_i) = \{(D_{6a}, \bot)\}$ でも $J$ に誰かいれば充足された。ひとつの $M_i$ に $D_{6a}$ と $D_{6c}$ が同時に立つときは、**次元ごとに**拘束者が要る。

（$D_{6b}$ の拘束者は市場であって人格を持たないので $\bot$ を許す。$D_{6a}$ の拘束者は所轄庁、$D_{6c}$ は取引上位者。）

- **拘束者が実在しなければ立たない。** 買い手に親会社がないのに「親会社の承認が要る」と書けば、資料は最初の一行で終わる（「うちに親会社はないよ」）。
- **向きが逆なら立たない。** 買い手が元請であれば、承認するのは買い手の側である。$\mathrm{above}(\text{buyer}, a)$ の関係を $\mathrm{above}(a, \text{buyer})$ と書くのは、座席の取り違えであって程度の問題ではない（「当社が元請だ。承認するのはこちら側だ」）。

$\mathcal{A} = \mathrm{set}(J) \cup V \cup W \cup \mathrm{Upstream}(\text{buyer})$。D1〜D5・D7 の拘束者は $\bot$（買い手の内部または手段の内部に閉じている）。

| 次元 | 拘束の所在 | 効く証拠 | 効かない証拠 |
|---|---|---|---|
| D1 能力 | 手段の内部 | スケール、限界の構造 | ― |
| D2 帰責 | 手段と買い手の関係 | 責任分界表、監査権、報告義務 | **性能・精度** |
| D3 権限 | 決定の主体 | 「最終判断は資格者」仕様固定、手順書 | **精度** |
| D4 手続 | 決定の正当化 | **同型かつ実名の前例**、制度上の後ろ盾 | 効果の主張のみ |
| D5 資源配分 | 買い手の資源 | 機会費用、人材需給、未達データ | **能力の否定** |
| D6a | 制度が行為・主体を許さない | 条文照合、登録番号 | 責任分界表、他社実績 |
| D6b | 市場に供給が存在しない | 需給統計、**売り手の別チャネルの実数** | 手段の性能比較 |
| D6c | 取引上位者が決定権を持つ | 上位者の承認取得実績 | 買い手向けROI |
| **D7a** | **カテゴリ内：同型の実名事例** | 同一所轄庁・同規模・同業態・同商圏 | 匿名・非同型の事例 |
| **D7b** | **カテゴリ内：認定・登録の非対称** | 自社が持ち他社が持たない登録 | 機能比較表 |
| **D7c** | **カテゴリ内：責任分界の引受範囲** | どこまで引き受けるかの明文 | 性能訴求 |
| **D7d** | **カテゴリ内：価格構造の開示** | 総額と逓増の開示 | 単年度の見かけ単価 |

**D1〜D6 はカテゴリ外の手段を消す次元、D7 はカテゴリ内の他社を消す次元。** $|C| = 1$ を達成するには D7 が要る。

#### $M_i$ 類型 × 使用可能次元

| 類型 | 使用可能 | 禁止（侮辱または原理的不成立） |
|---|---|---|
| $M_0$ 現状維持 | ― （⑤で扱わない。消すのは④の日付） | 全部 |
| 内製 | D5 主／D2・D6b 従 | **D1**（提案を評価する能力の否定＝自己矛盾） |
| 既存外注 | D2・D6c 主／D4・D6b 従 | **D1**（買い手の過去の選定判断の否定） |
| 競合 | **D7 主**／D1・D2・D3・D4・D6 従 | D5 |
| 取引上位者の指定 | D6c のみ | 他全部 |

### 3.5 セグメント既定値

$$\hat\nu : \mathcal{K} \to \Delta(\mathcal{V}), \quad c(x) = \max_v \hat\nu(x)(v), \quad \theta_{\mathrm{auto}} = 1 - \frac{u_{\mathrm{ask}}}{u + h}$$

$h$（誤りの損害）が大きい軸ほど $\theta_{\mathrm{auto}} \to 1$。$\delta$ は侮辱を生むので実質自動採用しない。移送入力は安全側があるので既定を持てる。

$$\hat\nu_{t+1}(x) = (1-\alpha)\hat\nu_t(x) + \alpha\,\mathbf{1}_{v_{\mathrm{sel}}}$$

選択ログがそのまま更新則になる。初期は全セル候補提示から始まる。

---

## 4. 依存規則

| # | 内容 | 層 |
|---|---|---|
| R1 | $\exists M_i.\,(D_5, \cdot) \in \delta(M_i) \to \tau^{\mathrm{ok}} \neq \emptyset \wedge \mathrm{now}(Q)$ | 2 |
| R2 | $D_2 \to$ 責任分界表を必須点灯、当該 $M_i$ ブロック内で性能訴求を減格 | 1 |
| R3 | $D_3 \to$ 判断権の所在を必須点灯、精度訴求を禁止 | 1 |
| R4 | $D_4 \to$ 前例を必須。判定は〈同型性 × 実名開示〉。非同型・匿名なら別次元へ落とす。停止条件は「他社事例と制度的後ろ盾がともに空」 | 1 |
| R5 | $T\text{-}E$ 型別置換：a 申請待ち／b **発注は今・稼働日を窓に固定**／c 承認申請の共同作成／d **不成立通知** | 1 |
| R6 | a 未来性／b リードタイム整合（A/C のみ）／c 因果接続 | 2 |
| R7 | $D_6 \to \Gamma_6 \vdash \neg\mathrm{blocked}(\delta(M_i), \mathsf{seller}, Q)$。**真偽ではなく実数**を⑥に出す。次元ごとに要る実数は**2つ**（下表） | 2 |
| R8 | 縮退（§3.2）。$\Sigma$ から落ちた**全段**について $\Gamma^{\mathrm{pre}}$ を必須入力にする（A10） | 1 |
| R9 | 提示変換。$V_0$ の排除と段ごとの提示形態 | 3.5 |
| **R10** | **無矛盾性の検査。**a ⑥→④（反復の再生産）／b ⑥→②（単位の**置換**。併記は禁じない＝A6）／**c ⑥→③④（外延の一致。$\mathrm{covered}(P) \subsetneq Q$ なら被覆率の開示を必須点灯＝A9）** | 3.5 |
| **R11** | **$n \geq 2 \to$ ③の新語に $\mathrm{Form}(j_n)$ の既存語との対応を併記（A11）** | 1 |
| **R12** | **順序整合。**a $\tau$ 内（C の着手期限日 $\leq$ A の終端日）／**b 資料全体（⑥の着手日 $\leq$ ④の着手期限日）** | 2 / 3.5 |
| **R13** | **$\mathrm{realizable}$。**⑥に〈執行座席 $w \in W$・日付・費目〉の三つ組を必須。$W = \emptyset$ なら生成停止（A12） | 1 / 3.5 |
| **R14** | **量の型整合。**$s_4$ の周期が $0$（単発）なら、⑥の $\kappa_n$ 量に flow 型（年額・回収年数）を使えない（A13） | 3.5 |
| **R15** | **出所の実在。**$q$ が「買い手データ」なら $\mathrm{receipt}(q) \neq \bot$（A15） | 2 |
| **R16** | **自己適用。**⑤で $M_i$ を落とした根拠は $P$ にも課す（A14）。$\Gamma_5 \cup \{P\} \nvdash \bot$ の具体形 | 3.5 |
| **A1** | $\mathrm{expressible}(q, \kappa_n)$。不通なら**両替経路を同枚に明記**、書けないなら別の量 | 2 |
| **A2** | $|C|=1$ は規則 (5)(6) の側条件として**無条件**。ただし $\Gamma \vdash |C|=1$（買い手が承認すること）が問われるのは $B_1$（$\kappa$ に価格を持つ座席がある）または $E_j \in \{$比較検討中, うちも知っている$\}$ のときだけ。問われる局面で売り手マスタが空なら生成停止 | 1 |
| **A5** | $\mathrm{expressible}(P, \kappa_n)$。⑥に $\kappa_n$ の量（回収年数・粗利の変動・手元資金の増減）を必須 | 2 |
| **A7** | $D_6 \to \exists a.\ \mathrm{above}(a,\text{buyer})$。拘束者が不在または向きが逆なら $\delta$ から落とす | 2 |
| **A8** | $\mathrm{applies}(d,\text{buyer})$。$\mathrm{scope}$ 外は棄却、$\mathrm{binder}$ 非一意は人間記入スロット | 2 |
| **A12** | $\mathrm{realizable}(P,\kappa_n)$。$W$ は顧客軸の必須入力（R13 として実装） | 1 |

#### R7 が要求する実数（第6版で 2 つに揃える）

| 次元 | ⑥に必須の実数 ① | ⑥に必須の実数 ② |
|---|---|---|
| D6a 制度が許さない | 登録番号・許可番号・認定の実物 | 有効期限と次回更新日 |
| D6b 市場に供給がない | 接触可能母数 | 接触→面接→入社の歩留まり |
| D6c 取引上位者 | 承認取得の実績件数 | **承認リードタイムの実測値** |

第5版は D6c について「実績と実測の2つ」と書きながら、売り手マスタに実測日数の欄がなく、検査は件数だけを見ていた。**形式系が正しく、実装が半分だった。** 実数が1つでも欠ければ R7 は充足しない。

---

## 5. 提示形態（層3.5）

| 段 | 提示形態 | 禁じ手 |
|---|---|---|
| ① | 買い手の事実の列挙（数字・固有名・日付） | 評価語 |
| ② | ①と同じ事実を**別の単位で数え直す**／**別の形式で書き出す** | 「〜が問題です」、新しい事実 |
| ③ | 名前をつけた一文（買い手の語彙で）＋**$\mathrm{Form}(j_n)$ の既存語との対応**（A11） | 「問題」「課題」で名づけること、新語を対応なしで置くこと |
| ④ | **日付と、買い手がまだ計算していない量**＋日付の**適用対象**（A8） | 日付のみ、量のみ、危機感の形容、適用範囲を書かない制度日 |
| ⑤ | **制約原理の開示**（構造と、それがどこで止まるか） | **列挙して否定形で並べる**、3つ以上、$M_0$ の明示否定、過去の選定判断への言及 |
| ⑥ | 空隙に嵌る具体。$n \geq 2$ なら結論文・金額レンジ・期日・想定反論。**$\kappa_n$ の量**（A5）と、**④で数えた量のうち本提案が消す部分**（A9）と、**〈誰が・いつ・どの費目を減らすか〉**（A12） | 命令法で締める、**約束法**（必ず〜します／保証します＝発話行為の天井を越える）、②の単位を $\kappa_n$ の量で**置き換える**（併記は必須であって禁じ手ではない＝A6）、④が単発なのに回収年数を出す（A13）、⑤で他手段に課した期限を自社が破る（A14） |

**禁止語彙は導出できる。** $V_0 = \mathrm{Vocab}(層1) \cup \mathrm{Vocab}(層2)$。手で保守しない。

**R10 は宣言で閉じる。** ②に導入単位を、⑥に課金周期と $\kappa_n$ の量と被覆率を宣言させれば、推論せずに厳密判定できる。ただし**宣言の値域に「未定義」を用意すること。** 0 と空文字を「無し」の代用にすると、比較の定義域に入り込んで誤検出になる（第5版の R10a が6件を誤検出した）。

---

## 6. 出力形式

1. **資料**（1本。開始点は $j^*$。対面での縮退はトークガイドで）
2. **不成立通知**（T-E(d) 恒常禁制／⑤生成不能／R7 未充足／A2 未充足／**執行座席 $W$ が実在しない**）
   ※ $W$ が**実在しない**（この買い手には費目を動かせる者がいない）と、$W$ を**まだ聞き取っていない**は別である。前者は不成立通知、後者は人間記入スロット。0 と未定義を同じ値で表さない（§3.2 の注と同じ型の誤り）。
3. **トークガイド**（$\Sigma \setminus \sigma_{\mathrm{read}}(E_r)$ ＝ 対面で飛ばしてよい段）

---

## 7. 検証の場

| | 何を測るか |
|---|---|
| 閲覧ログ | 段別滞在時間。**④の「まだ計算していない量」が最長になるか**（8セルで 8/8 実証済み）。②の滞在が①より長いか（内在的否定の成否） |
| 選択ログ | 上書き率 $= 1 - \hat\nu(v_{\mathrm{default}})$。高いセルは既定が誤っている |

$\mathrm{expressible}$・$\mathrm{valid}$・$\prec$ の中身は経験的に推定するほかない。$\vdash$ は買い手のものであって、我々が定義できるのは左辺の構成規則だけである。

**予測は生成器に出させない**（第6版）。食品・建設 8セルで、最長滞在段の予測的中は 2/8 まで落ちた。買い手が数字を検算して落としたのに対し、生成器の予測にその検算が織り込まれていない。**自分の書いたものの通過可否を自分で予測させると、系統的に楽観へ寄る。** 予測は第三のエージェント（生成物だけを見て、生成の意図を知らない座席）に出させる。

---

## 8. 未踏 ―― 被覆範囲の地図

修正の対象にしないが、観察されたので積む。

| # | 内容 | 出どころ |
|---|---|---|
| U1 | **⑤に挙げる手段 $M$ の完全性を誰が確定するか。**⑤は $\forall M_i \in M\setminus\{M_0\}$ を要求するが、$M$ の同定主体が規定されていない。買い手が持つ第三の選択肢（グループ内応援など）を売り手は知らない。人間記入スロットは⑥にしかない | 食品 F1-P1 |
| U2 | **$B_3$（最も保守的な一人）と $\kappa_n$（読まない座席）と $j^*$（読む中で最も遠い座席）の三者関係。**可搬層の基準をどれに取るかが未規定 | 生成器の自己申告 |
| U3 | **段と枚の対応が 1:1 と暗黙に仮定されている。**$\mathrm{on}(b)$ は段にブロックを割り当てるが、1段が何枚になるかを決めていない。⑥に必須ブロックが集中して字数が破綻する | 4セルで発生 |
| U4 | **検収・返金・不承認時の扱いが必須ブロックにない。**「判断して駄目だったら金は戻るのか」「拒否権者が首を振ったとき、この投資は誰が持つのか」を3人が訊いた | 食品 F2-P1、建設 K2-P1・K2-P2 |
| U5 | **$V$（拒否権者）は開示するだけでは足りない。**「あの人が何て言えば通るのか、それを先に調べてから来い。順番が逆だ」——$V$ の**受諾条件**を $\Gamma$ に入れる規定がない | 建設 K2-P2 |
| U6 | **量の導出過程の開示。**R6b は $d - \mathrm{LT}^*$ を機械的に計算するが、その引き算を買い手に見せる規定がない。説明できない数が1つあると爆発律で全体が落ちる | 建設 K2-P2 |
| U7 | **$\kappa$ の下位区分（稟議の様式）。**「2,800万円を実行予算で見るのは無理です。うちは本社の償却資産、これは何現場で回すかの話。**稟議の形が違う**」。$\kappa_n$＝財源は当たっていたが、費用か資本的支出かで決裁ルートも書式も変わる | 建設 K1-P1 |
| U8 | **②の単位を、買い手の支払単位に従わせる規定がない。**「うちは時間やのうて**日で払っとる**。480時間が浮いても金は一銭も浮かん」。②の数え直しは自由だが、$W$ が減らせる費目の単位と揃っていなければ A12 が最初から不可能になる | 建設 K2-P1 |
| U9 | **$V$ の受諾条件（U5 の再掲・強化）。**「元請A社の承認を前提条件として置き続けたが、**その承認を出す側の人間が何を見て可否を決めるかには一度も触れなかった**」。$V$ を開示するだけでなく、$V$ が何を見るかを $\Gamma$ に入れる規定が要る | 建設 K2-P1 |

---

## 9. 第7版の内部検査 ―― 直したものと、残したもの

生成走行ではなく、**文書とコードそのもの**を4つの独立した視点（型・カバレッジ・規則間の衝突・仕様と実装の一致）で読み直した結果。買い手の反応ではなく、机上で見つかる欠陥だけを扱う。

### 9.1 直した ―― 論理の誤り

| 箇所 | 何が壊れていたか |
|---|---|
| **R16 の式** | $\Gamma_6 \nvdash \neg\psi[p/M_i]$ と書いていた。これは「自社が同じ試験を通ることを**証明してはならない**」であり、直後の本文・R12b・§5⑥ の三つすべてと反対を向いていた。$\vdash$ が正しい |
| **A7 の $\exists a$** | 拘束者が対の第2成分として束縛されておらず、「上位の誰かがどこかにいる」しか言えていなかった。$\delta(M_i) = \{(D_{6a}, \bot)\}$ でも $J$ に誰かいれば充足された |
| **$\Gamma^{\mathrm{init}}$** | $\min\Sigma$ の1点でしか定義されず、$\Sigma = \{1,4,6\}$ のような**中抜け**で②③⑤の $\Gamma$ が未定義のまま残っていた。A10 を先頭の穴にしか当てていなかった |
| **$\mathrm{img}(\delta)$ の記法** | 第6版で $\delta$ の値域を対の集合にしたのに、R1・A7 は $D_5 \in \mathrm{img}(\delta)$ のまま。この前件は恒偽で、R1 は**一度も発火しない**書き方だった |
| **$\Sigma$ の場合分け** | 条件節が関数 $\sigma_{\mathrm{prod}}$ そのものを未定義の $\text{full}$ と比較していた。$\sigma_{\mathrm{read}}$ が到達不能になる |
| **$|C|=1$ と A2** | 規則 (5)(6) は無条件、A2 は $B_1$ 限定で、両者が矛盾して見えた。§1.6 の区別（世界について真 ≠ 承認されている）で解ける——$|C|=1$ は無条件に要り、$\Gamma \vdash |C|=1$ が問われるのは比較の局面だけ |
| **$\max_q$ の範囲・$\Delta\kappa_n$・裸の $\Gamma$・自由変数 $x$・$P$ と $p$・$J$ の集合演算** | いずれも型または定義域の誤り。§2.1・§2.2・§1.7 で修正 |

### 9.2 直した ―― 仕様と実装の食い違い

| # | 内容 |
|---|---|
| 1 | **R13 が対で検査していなかった。**$\mathrm{authority}(w,a)$ は〈人と費目〉の対なのに、実装は「実行者が $W$ にいる」「費目が $W$ 全体のどこかにある」を別々に見ていた。工場長が外注加工費を止める、が通っていた |
| 2 | **A9 の門がなかった。**$\mathrm{covered}(P) \subseteq Q$ を宣言させる欄がなく、$Q$ と**交わらない**ものを消す提案も「一部だから」で通った |
| 3 | **R4 の停止条件が未実装。**前例ゼロのまま D4 で他手段を消す資料が生成できた |
| 4 | **T-D 単独禁止が棄却済みの日付で救われていた。**判定を生き残った $\tau^{\mathrm{ok}}$ に変更 |
| 5 | **拘束者を E 型すべてに要求していた。**第三者承認は Ec だけである |
| 6 | **適用対象が未入力なら黙って通っていた。**降格（主位置に使えない）へ |
| 7 | **R10b が②のない資料でも発火していた。**検査側の定義域を $\Sigma$ で絞る |
| 8 | **$\Gamma^{\mathrm{pre}}$ は実装のほうが正しかった。**仕様の $\min\Sigma > 1$ を実装に合わせて全段へ広げた |
| 9 | **トークガイドが未実装で $E_r$ が死んでいた。**$\Sigma \setminus \sigma_{\mathrm{read}}(E_r)$ を出力に追加 |

### 9.3 残した ―― 次の版で決めること

いずれも「§1 に書いてあるのに形式系にない」型で、**A5 と同じ構造**である。実装で塞ぐ前に、どこまでを本モデルの守備範囲とするかの判断が要る。

| # | 内容 | 重さ |
|---|---|---|
| **N1** | **$\forall s$ の残り。**§1.5 の枠は $s \in \{1,\dots,6\}$ だが、$\mathrm{expressible}$ を要求するのは A1（④）と A5（⑥）だけ。②③⑤ は素通り。§8 の U7・U8（「うちは時間やのうて日で払っとる」）はこの事例であり、*未踏*ではなく**§1.5 の直し残し**である | 大 |
| **N2** | **$\Gamma \vdash \mathrm{realizable}$。**§1.6 は $\mathrm{expressible}$ にも $\Gamma \vdash$ の区別が要ると明言しているのに、R13 は三つ組が**紙に書いてある**ことしか要求していない。第6版の「金額は載ったが $\kappa_n$ は動かない」が、そのまま「三つ組は載ったが $w$ は動かない」へ再現する | 大 |
| **N3** | **$V$ に規則がない。**§1.7 は $V$ と $W$ を等位の必要条件として書いたが、$W$ は R13 を得て $V$ は何も得ていない。§8 の U5・U9 は*未踏*として積まれているが、正しくは**§1.7 の直し残し** | 大 |
| **N4** | **$\chi$（切断）が形式系にない。**§1.2 は減衰の二機序を書き、$\kappa$ 側だけが A1・A5 になった。$\chi$ の帰結（切断）のほうが破壊的なのに未実装 | 中 |
| **N5** | **$\neg\mathrm{revocable}$ が $\mathrm{now}$ にない。**猶予延長・施行延期のありうる制度日が全条件を通過する。買い手は「あれは毎年延びる」で落とせる | 中 |
| **N6** | **無矛盾性の検査が⑥起点しかない。**$\Gamma_s \nvdash \bot$ は $\forall s$ だが、R10・R12b・R16 はすべて⑥または⑤から後ろ向き。②③④ が先行段と矛盾する場合を見ていない | 中 |
| **N7** | **減衰の合成が終端一点に潰れている。**§1.2 は $T_{n-1\to n} \circ \cdots \circ T_{1\to 2}$ の合成だが、形式系が見るのは $\kappa_n$ と $\mathrm{Form}(j_n)$ だけ。中間座席で濾される命題を止められない。「基準の**異なる**裁定点の個数」と言いながら、規則の発火は生の $n$ | 中 |
| **N8** | **$\mathrm{cost}$ 単調性と $\prec$ に対応物がない。**§1.1 の $\mathrm{cost}$、§1.5 の $\prec$（侮辱）はどちらも形式系に対象がなく、§3.4・§3.5 が根拠名だけ借りている。§7 の測定チャネルでも測れない | 小 |
| **N9** | **A7 と R7 の適用順序が未規定。**A7 は R7 の前件（$\delta$）を書き換えるので、順序で「資料が出る」か「不成立通知」かが変わる。実装は A7 先だが、層表に A7・A8・A12 が載っていない | 小 |
