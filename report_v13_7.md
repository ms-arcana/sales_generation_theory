# 第13.7版 走行レポート ―― 1業界・1商材・1ケース（E1-P1 学校法人 × 広告）

版：`sales_logic.py` sha256[:12] `31d2367e07af`（走行後の修正込み）／回帰 **277項目 全通過**／N₆監査 8
生成：`gen137/out_E1-P1.json`（arm0・Σ＝②③④⑤⑥・38,180トークン・10分18秒）
予測：`predict_v13_7.md` ／ `predict_v13_8.md`（**走行前に置いたもの。書き換えていない**）

> **断り。**コードには第13.7版（A41b）だけでなく第13.8版（R20〈式〉・A45系）も入っています。
> 「13.7 だけ」に戻して走らせることはしていません。**分離は理由コードで効きます** ――
> A41b は日付の文字列そのもの、R20〈式〉と A45 は専用のコードで数えています。

---

## 1. 結論

**A41b は的に当たりました。**第13.6版で買い手が名指しした二つが、両方とも消えています。

| 買い手が第13.6版で言ったこと | 第13.6版 as-run（新コードで再採点） | 第13.7版 |
|---|---|---|
| 「着手 2027-03-01 が一巡の締め 2027-05-31 より前で順序が逆」 | `A41B_START_BEFORE_GATE` **1件** | **0件**（着手 2027-09-01 ≥ 窓 2027-05-31） |
| 「④の逆算 2026-12-30 と⑥の決定 2026-09-30 が三か月ずれている」 | `A41B_S4_FOREIGN_DATE` **1件**（④2026-12-30） | **0件**。④に出た日付は 2027-06-30 のみ |
| ―― | 本文に 2026-12-30 が**出ていた** | **一度も出ない** |

決定 2027-05-31（＝窓＝実効の決定期限）／着手 2027-09-01／実現 2028-03-31〜04-30。
**同じ資料に決定期限は一つだけ**になり、生成器は自分で期限を作らなくなりました。
生成器自身が第13.6版で「書き分けの規定が無い」と申告していた欠落が閉じています。

**停止 0 件。**第13.6版の同セルは 3件停止でした。

---

## 2. 予測との突合

| # | 予測 | 実測 | |
|---|---|---|---|
| M1 | `R20_RETURN_MISSING` 0〜3 | **0** | 的中 |
| M2 | `R20_RETURN_AS_EXPR` が立つ | **3件**（3座席すべて） | 的中（**ただし検査を直してから**・下記） |
| M3 | `R20_EXPR_COEF_UNSOURCED` 1〜4 | **0** | **外れ**。生成器は3座席とも係数の出所を書いた |
| M4 | `A45B_BREAKDOWN_MISSING` 0〜2 | **0**（内訳4件） | 的中 |
| M5 | `A45_RANGE_TOO_WIDE` 1〜4 | **0**（600万〜720万＝**1.20倍**） | **外れ**。予測より良い |
| M6 | `A45B_BREAKDOWN_MISMATCH` 0〜1 | **0**（内訳の和 600万＝下限） | 的中 |
| M7 | `A45C_UNIT_PRICE_NOT_MONOTONE` 0〜1 | **0**（月60万 → 月50万で単調減少） | 的中 |
| M9 | `A41B_*` 0/0 | **0/0** | 的中 |

**7/9 的中。外れた2件はどちらも「予測より生成器が良かった」方向です。**
n=1 なので、これを一般化しません。

---

## 3. 走行が見つけたもの ―― こちらのほうが重い

### (a) 検査の欠陥：`R20` の状態を二分で書いていた（**浅い一致の6件目**）

最初の採点で `R20_RETURN_AS_EXPR` が **0件**でした。予測 M2 は「立つ」。
走行前に置いた規律 ―― **「M1 が減って M2 が立たなければ、まず検査を疑う」** ―― に従って疑ったら、検査の欠陥でした。

```
生成器は式を ret の欄に直接書いていた
  "ret": "〈貴学の月あたり媒体別実績突き合わせ人時〉× 1.0"
  "ret_basis": "貴学の月あたり媒体別実績突き合わせ人時"   ← 成分の欄も埋めていた

is_bottom(ret) が False  →  「値が在る」と読む  →  式の検査が一度も走らない
```

**状態は三つ（値／式／⊥）なのに、`is_bottom` の二分で書いていました。**
`is_expr()` を足して三状態に直し、回帰に固定。ついでに、式の文字列に混ざる単位語
（「× 1.0（円／円）」）を値の単位ずれと数えていたのも止めました。

### (b) 配管の欠陥：価格の欄が検査へ届いていなかった（**V3 と同型・二版連続**）

`A45_PRICE_UNDECLARED` が立ちました。ところが生成器は
`s6_price_low` `s6_price_high` `s6_price_items` `s6_price_tiers` を**現に申告しています**。

**`validate8_v12.to_declared()` が新しい5欄を落としていました。**
**A45／A45b／A45c は一度も走っていなかった**ということです。
第13.7版で `decide_deadline_tau` を白名簿に手で足したときと同じ形で、**二版連続**です。
`V3`（`compile_deal` の鍵と受け手を突き合わせる回帰）を入れない限り、必ず三度目が来ます。

直したうえで同じ生成物を測り直した結果が §1・§2 です。

### (c) 新しいアノマリー2件

| 番号 | 中身 |
|---|---|
| **A49** | **〈出所〉の欄が二つの担体を持つ。**生成器が「払う＝自社の運用手順に基づく試算／**戻る＝式**（係数は…）」と連結して申告し、`A28_SOURCE_UNKNOWN` が **3/3 座席**で立った。列挙値と一致しないので機械は読めない。**A37／A37b／A41／A48／A48b に続く6件目の〈一つの語／欄が二つの担体〉。**処置は欄を割る（`pay_source` / `ret_source`） |
| **A45d** | **内訳の金額が〈幅〉を持つとき、和の突合が下限だけになる。**`parse_amount` は最初の数しか取らない（「240万〜360万円」→ 240万）。今回はたまたま下限の和＝総額の下限で一致した。**幅は値ではない**（三状態と同じ形）。下限の和・上限の和の両方で突き合わせるべき。**7件目の浅い一致（未然）** |

---

## 4. まだ言えないこと

- **買い手には当てていません。**⑥が通るかどうかは、この走行では測っていません。
  `predict_v13_8.md` の B1〜B5 は未検証のままです。
- **n=1 です。**E1-P1 は 8セル中**唯一〈決定の窓〉を持つ系統**で、A41b が効くように選びました。
  窓を持たない6セルでは、A41b の効果は原理的に 0 のはずです。
- **停止 0 は「良い資料」を意味しません。**⑦の検査は買い手の判定を予測していません（φ=+0.055）。
  言えるのは「送る前に潰せる不備は潰した」までです。

---

## 5. 次の一手（この走行を受けて）

1. **`V3` の回帰を入れる。**配管が二版連続で落ちた。`compile_deal` の鍵と
   `cells8_v10.run()` の白名簿・`to_declared()` の三者を突き合わせる（**VS Code**）
2. **A49 の処置** ―― `source` を `pay_source` / `ret_source` に割る（**Cowork**）
3. **A45d の処置** ―― 幅を持つ金額を三状態で扱う（**Cowork**）
4. **買い手へ当てる** ―― この1ケースの⑥を盲検買い手3座席（入試広報課長・学部長会・理事会）へ。
   **B1〜B5 はここで初めて測れる**（**Cowork**）
